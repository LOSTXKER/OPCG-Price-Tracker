# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-28 (ค่ำ) — **หน้าเทียบเก็บงาน navbar รอบ 2 พร้อมให้เบสเลือกแล้วที่ `/proto/navbar`**

## ผลลัพธ์ — หน้าเทียบ navbar 3 แนว (session นี้)

เบสดู navbar โครง CoinGecko ที่เพิ่ง merge (PR #121) แล้วถาม "มีแนวทางทำให้สวย ดูดี เหมาะใช้งานจริงกว่านี้มั้ย" → วินิจฉัยจากจอจริง + โค้ดจริง + VISION ได้ปัญหา 4 ข้อ (สถิติเป็นชิปเลยดูเป็นปุ่ม · **มูลค่ารวมสีเขียวบน chrome ผิดกติกา VISION §1** · มุมโค้งปน 3 ตระกูล · เส้นแบ่ง 3 เส้น) แล้วทำตามกติกา VISION "งานเปลี่ยนทิศ UI = ทำหน้าเทียบให้เห็นภาพจริงก่อน":

- **เขียนทับ `src/app/proto/navbar/page.tsx` ทั้งไฟล์** (หน้าเทียบรอบเก่าตัดสินจบไปแล้ว) — สลับดู 4 แบบในกรอบเดียว พร้อมสลับสว่าง/มืด:
  - **ปัจจุบัน** — จำลองคลาสจริงจาก header-market-ticker/header เป๊ะ ไว้เป็นตัวตั้งเทียบ
  - **A · โต๊ะเทรดนิ่ง** — ทึบ+เส้นล่างตลอดแบบ TradingView · สถิติตัวหนังสือเปล่า · มุม rounded-lg ตระกูลเดียว · สูงเท่าเดิม (40+60=100px ไม่แตะ `--chrome-h`)
  - **B · อุ่นขัดเงา (ฉันแนะนำ)** — คงโปร่งใส+แคปซูล · **เกม→ชุดรวมเป็นแคปซูลเดียวมีรูปซองจริง** (กดวนดูสถานะเลือกชุดได้) · สถิติคั่นจุด · ค้นหา/เมนูเป็น rounded-full หมด · สูงเท่าเดิม
  - **C · แถบชีพจรแยก** — สถิติขึ้นแถบบาง h-7 บนสุดแบบ CMC แท้ (+ "ชุด 51" + "อัปเดตล่าสุด") · แลกกับสูงขึ้น ~28px และต้องแก้ `--chrome-h` + sticky 6 จุด
  - ทุกแบบรวม hygiene บังคับ: เอาเขียวออกจากมูลค่ารวม · tabular ทุกตัวเลข · ตัดเส้นแบ่ง · มุมโค้งตระกูลเดียว
- ตัวเลขบนหน้าเทียบ = ชุดเดียวกับจอจริงของเบส (3,838 / 2,688,706 ฿ / 0.296 / Honey 20) · เนื้อหน้าแรกจริงย่อส่วนวางใต้แถบให้เห็นบริบท
- **ผลพลอยได้: full lint เหลือ 0 errors ทั้งโปรเจค** — จุด block ที่ PLAN.md เคยจดไว้ (`proto/navbar:583`) หายไปกับการเขียนทับไฟล์

## หลักฐานตรวจรับ

- ESLint ทั้งโปรเจค **0 errors** (เหลือ 33 warnings เดิมในไฟล์อื่น) · `tsc --noEmit` ผ่าน · test **151 ไฟล์ / 906 ข้อผ่าน** · build ผ่าน
- ตัวตรวจดีไซน์ (impeccable detect) บนไฟล์ใหม่: 0 findings
- Browser จริง (viewport 1512): ทั้ง 4 แบบ × สว่าง/มืด render ครบ · สลับแบบ/ธีม/กดเลือกชุดใน B ทำงาน · console ไม่มี error
- **ไฟล์ header จริงยังไม่ถูกแตะแม้แต่บรรทัดเดียว** — งานนี้คือหน้าเทียบล้วนๆ

## สถานะ git

- branch ใหม่ `feat/navbar-direction-r2` (แตกจาก master หลัง merge PR #121) · commit หน้าเทียบ + PROGRESS นี้ · push แล้ว
- ไม่มี schema / migration / dependency / config change

## NEXT

1. **เบสเปิด `http://localhost:3000/proto/navbar`** (server รันอยู่แล้ว) → สลับดู ปัจจุบัน/A/B/C สองธีม → เคาะแบบ
2. เคาะแล้วบอก Nami → เฟส 2: ลงของจริง in-place (`header-market-ticker.tsx` · `header.tsx` · `command-search.tsx` · `header-catalog-control.tsx` · C เท่านั้นแตะ `--chrome-h`+sticky 6 จุด) + อัปเดต literal ในเทสต์ 2 ไฟล์ (`header-search-preferences.test.tsx` · `header-catalog-control.test.tsx`) โดยคงเจตนา — แผนเต็มอยู่ที่ `~/.claude/plans/navbar-goofy-peach.md`
3. คิวเดิมที่ยังค้าง (อย่าลืม): กวาดคำ "อัปเดตทุกวัน" ~35 จุดทั้งเว็บ · `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
