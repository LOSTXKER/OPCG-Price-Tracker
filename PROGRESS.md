# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-28 (รอบดึก · ต่อจาก commit 64258f8) — **แถวไฮไลต์หน้าแรกกลับเป็นแบบเดิม + เพิ่มช่องโฆษณาคอลัมน์ขวา**

## ผลลัพธ์

### แถวไฮไลต์หน้าแรก — คืนของเดิม + ช่องโฆษณา

เบสส่งภาพแถวไฮไลต์ตอนที่ยัง commit ไว้ (การ์ดมูลค่าสูงสุด · ราคาขึ้นมากสุด · ราคาลงมากสุด แบบไม่มีกรอบ ไม่มีกล่องตัวเลข) แล้วสั่ง **"ส่วนนี้ขอแบบเดิม แต่ขอเพิ่มคอลัมด้านขวาไว้เป็นช่องสำหรับโฆษณา"**

- **คืนแถวไฮไลต์เป็นแบบเดิม:** ถอดกล่อง `HomeMarketStatus` (การ์ด + ตัวเลข 3 ตัวในกรอบเดียว) ออก → กลับเป็น `HomeFeaturedCard` เปล่าๆ ไม่มีกรอบ, การ์ดใหญ่เท่าเดิม (รูป 100px, รูปซ้าย–ข้อความขวา) ตามภาพ
- **แก้ปัญหาตัวเลขซ้ำ 2 ที่** ที่ค้างจากรอบก่อน: ตัวเลขรวมอยู่บนแถบ ticker ที่เดียว
- **เพิ่มช่องโฆษณาเป็นคอลัมน์ที่ 4:** zone ใหม่ `home-highlight-rail` ในทะเบียนโฆษณากลาง (RECTANGLE · route HOME · GOOGLE_ONLY) — ใช้ `AdInventorySlot` ตามกฎ AGENTS.md ไม่ได้ hand-roll กล่องเอง จึงได้ระบบซ่อนโฆษณาสำหรับสมาชิกที่จ่ายเงินฟรี
- **แสดงเฉพาะจอ ≥1280px:** ต่ำกว่านั้นแถวกลับเป็น 3 คอลัมน์เหมือนเดิม โฆษณาไม่เรนเดอร์ (ไม่ไปเบียดบล็อกเนื้อหา)
- **ตัดงานเปล่าออกจากฐานข้อมูล:** `lib/data/home.ts` เคยยิงคิวรีรวมราคาการ์ดทั้งตาราง + ดึงอัตราแลกเปลี่ยน ทุกครั้งที่เรนเดอร์หน้าแรก ตอนนี้ไม่มีใครใช้แล้ว → ถอดออก
- **ลบไฟล์ที่ไม่มีใครใช้ (เบสอนุมัติ):** `home-market-status.tsx` + test, `header-preferences-menu.tsx`

## ⚠️ เหตุการณ์ระหว่างทาง — ทีมตรวจถูกตัดกลางคันขณะแก้ไฟล์

ส่ง subagent 4 ตัวไปตรวจงานแบบ adversarial (สั่งให้ mutation-test = แก้โค้ดชั่วคราวดูว่า test จับได้ไหม แล้วคืนไฟล์) แต่ **ทั้ง 4 ตัวถูกตัดด้วย usage limit ขณะกำลังแก้ไฟล์อยู่ จึงไม่ได้คืนไฟล์**

- ไล่ตรวจ working tree ทีละไฟล์ → เจอ mutation ค้าง 1 จุด: `lib/data/home.ts` ถูกใส่คิวรี aggregate กลับเข้าไปโดย **เปลี่ยนชื่อตัวแปรเป็น `valueAgg`** → คืนไฟล์แล้ว ยืนยันตรงกับของเดิมเป๊ะ
- ส่วน `page.tsx` และ `inventory.ts` ตรวจแล้วถูกคืนทัน ไม่มีร่องรอย
- **ได้ของดีจากอุบัติเหตุนี้:** การเปลี่ยนชื่อตัวแปรพิสูจน์ว่า test ฉันดักแค่ชื่อ `totalValueAgg` ซึ่งเปลี่ยนชื่อก็รอด → แก้เป็นดักตัวคิวรีจริง (`prisma.card.aggregate`, `_sum`, `prisma.exchangeRate`) แทน
- **ทีมตรวจรอบนี้ไม่ได้ผลลัพธ์** (0 finding เพราะตายก่อนรายงาน ไม่ใช่เพราะไม่มีปัญหา) — งานนี้จึงผ่านแค่ด่านอัตโนมัติ + ตาฉันเอง ยังไม่ผ่านการตรวจอิสระเหมือนรอบ navbar

## หลักฐานตรวจรับ

- ESLint 0 errors (26 warnings เดิม) · TypeScript ผ่าน · full test **151 files / 902 tests** · build **211 หน้า**
- **Mutation test 3 ด่าน ทำเองและผ่านครบ:** ใส่คิวรี aggregate กลับ (เปลี่ยนชื่อตัวแปรด้วย) → test ตก · ถอด `AdInventorySlot` ออก → test ตก · เปลี่ยน route ของ zone → test ตก · คืนไฟล์แล้วกลับมาผ่าน 49/49
- Browser จริง 1920px: 4 คอลัมน์ละ 310px, กล่องโฆษณาเรนเดอร์จริง 310×280, กล่องตัวเลขหายไปแล้ว, ไม่มี overflow, console error 0 · ที่ 1274px: 3 คอลัมน์ โฆษณา `display:none` · ตรวจทั้งโหมดสว่างและมืด

## สถานะ git

- branch `chore/next-16.3` · commit ล่าสุด `64258f8` (navbar revert) **push แล้ว**
- งานรอบนี้ยังไม่ commit — รอเบสดูก่อน
- ไม่มี schema / migration / dependency / config / scrape / seed change

## NEXT

1. เบสรีเฟรช `http://localhost:3001` ดูแถวไฮไลต์ + ช่องโฆษณาคอลัมน์ขวา (ต้องเปิดจอกว้าง ≥1280px ถึงจะเห็นช่องโฆษณา)
2. ถ้าโอเค → commit + push
3. ยังไม่ได้เปิด PR ไป master — ถ้าอยากรวมเข้า master เหมือนรอบ #119/#120 สั่งได้
4. งานรอบนี้ยังไม่ผ่านการตรวจอิสระ (ทีมตรวจตายกลางทาง) — ถ้าอยากให้ส่งตรวจซ้ำก่อน merge บอกได้
