# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-28 (บ่าย) — **หัวตารางราคาหน้าแรกเปลี่ยนเป็นแถบ "ชุดการ์ดวันพีชล่าสุด" แถวเดียว** (คำสั่งเบสรอบบ่าย ต่อจากงานรอบดึก hero/navbar)

## รอบนี้ทำอะไร (session นี้)

เบสสั่ง: เอาหัวข้อ "ตารางราคาการ์ด" ออก แล้วเอา "ชุดการ์ดวันพีชล่าสุด · เลือกดูราคาการ์ดทุกใบ แยกตามชุด" กลับขึ้นมาแทน + จัด UI เป็นแถวเดียวให้สวย:

- **หัวตาราง "ตารางราคาการ์ด" ลบทิ้งทั้งยวง** — ลบไฟล์ `home-market-intro.tsx` + `buildHomeMarketHeading` ใน `lib/seo/copy/home.ts` (ไม่มีใครใช้แล้ว)
- **แถบชุดการ์ดย้ายจากใต้ตาราง (กติกา 08-26 เดิม) ขึ้นมาเป็นหัว section ตลาด** — h2 "ชุดการ์ดวันพีชล่าสุด" + ประโยค "เลือกดูราคาการ์ดทุกใบ แยกตามชุด" (โชว์ทุกจอ ไม่ซ่อนบนมือถือแล้ว) + ลิงก์ "ดูชุดทั้งหมด"
- **rail แถวเดียวทุกความกว้าง** — เลิก grid 2 แถว (twoRowVisibility ลบทิ้ง) เป็นแถวเลื่อนแนวนอน snap ซ่อน scrollbar ปาดขอบจอบนมือถือ + fade ขอบขวาบอกว่าเลื่อนต่อได้ · pill ใหม่ 2 บรรทัด (รหัสชุด/ชื่อชุด) + รูปกล่องใหญ่ขึ้น hover ซูมนิดๆ — ลิงก์ครบ 12 ชุดใน HTML แรกเหมือนเดิม (SEO ไม่เสีย)
- test `home-seo.test.tsx` ปักความจริงใหม่: strip อยู่เหนือตาราง · ไม่มี HomeMarketIntro · ประโยคขยายต้องโชว์

**Verify**: tsc 0 · ESLint 0 error · vitest 151 ไฟล์/906 ผ่าน · build ผ่าน · เปิดดูจริง (dev :3000): desktop 12 pill แถวเดียวเลื่อนได้ + มือถือ 375px หัวข้อ→แถบชุด→ตารางต่อเนื่อง · console ไม่มี error

## ⚠️ งานคู่ขนานใน tree (ไม่ใช่ของ session นี้ — อย่าเหมารวม)

ตอน commit มีไฟล์แก้ค้างของอีก session (กวาดคำ "อัปเดตทุกวัน" ~35 จุด: blog/guide/most-expensive/layout/og-image + lib/seo/copy หลายไฟล์) — session นี้ **commit เฉพาะไฟล์ของงานตัวเอง** ของเขายังค้างใน working tree ให้เขาปิดเอง

## สถานะ git

- branch `chore/next-16.3` — งานรอบนี้ commit แยกก้อน push แล้ว (ต่อคิวจาก `30a8b19` ของรอบดึก)
- ไม่มี schema / migration / dependency / config change

## NEXT

1. เบสรีเฟรชหน้าแรกดู: แถบ "ชุดการ์ดวันพีชล่าสุด" แถวเดียวเหนือตาราง (desktop + มือถือ)
2. พอใจแล้วสั่งเปิด PR `chore/next-16.3` → master ได้ (แบบ #119/#120)
3. คิวงานค้างเดิม: กวาด "อัปเดตทุกวัน" ~35 จุดทั้งเว็บ (กำลังวิ่งในอีก session) · `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive (ย้ายมาใช้ pattern map 3 locale ของหน้าแรก)
