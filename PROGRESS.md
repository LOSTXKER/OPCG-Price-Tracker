# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบสิบเอ็ด · ลองยุบสองแถว)

**⚠️ ดูที่ `http://localhost:3008/proto/navbar-ecom`** — พอร์ตเปลี่ยนอีกแล้ว: 3000 ค้าง route (ของอีก session) · **3007 ที่เปิดไว้ crash เพราะบั๊กภายในของ Turbopack เอง** (panic นอก per-task boundary ไม่ใช่โค้ดเรา — lint/tsc ผ่านตลอด) → เปิดใหม่ที่ **3008**

**เบสสั่ง:** ย้ายเลือกเกม/ชุด + อัปเกรด/แชท/แจ้งเตือน/โปรไฟล์ ขึ้นแถวบน · แถวล่างคือแถวเมนูเดิมแต่เอาโลโก้มาไว้ซ้ายสุด (เหลือ 2 แถว) · และเก็บแบบ A ไว้แต่ปรับให้ช่องค้นหาไปชิดขวา

### 2 แบบที่เหลือ
| แบบ | โครง | สูง | โลโก้อยู่ |
| --- | --- | --- | --- |
| **D · เหลือสองแถว** ← ค่าเริ่มต้น | ชีพจร 32 · **แถวบน 48** = เกม›ชุด ⟷ ของฉัน ‖ บัญชี · **แถวล่าง 56** = โลโก้ · เมนู ⟷ ค้นหาชิดขวา | 136px | แถวล่าง |
| A · สามแถว (ปรับแล้ว) | ชีพจร 32 · แถว 2 = โลโก้ · เกม›ชุด ⟷ ของฉัน ‖ บัญชี · แถว 3 = เมนู ⟷ ค้นหาชิดขวา | 144px | แถว 2 (บนสุด) |

**ทั้งคู่ช่องค้นหาชิดขวา กว้าง 480px** ห่างขอบ 32px เท่ากับ padding ของแถว (ของจริงตอนนี้ 320px)

**ข้อแลกของ D ที่ต้องดู:** โลโก้ไม่ได้อยู่บนสุดของแถบแล้ว มีเกม › ชุด อยู่เหนือมันหนึ่งชั้น — ขัดกับความเคยชินว่าแบรนด์อยู่มุมบนซ้ายเสมอ และแถวบนซ้ายเริ่มด้วยปุ่มเกม อาจอ่านเหมือนแถบเครื่องมือมากกว่าหัวเว็บ

**ตรวจรับแล้ว:** eslint 0 · tsc ผ่าน · ทุกปุ่มควบคุม ≥40px · ค้นหาชิดขวาจริงทั้งคู่ (วัดระยะห่างขอบ = 32px) · ไม่ล้นที่ 1360px

### ประวัติที่เบสเคาะมาแล้ว (อย่าเสนอซ้ำ)
- ❌ ทาสีแบรนด์ทั้งแผง · ❌ แถวคำค้นยอดนิยม · ❌ ชั้นวางชุดเรียงยาว · ❌ แตะแถบชีพจร · ❌ ซ่อนชื่อของฉัน · ❌ ปุ่ม <40px · ❌ ค้นหายืดเต็มแถว
- ✅ พื้นสีเดิม · ✅ ชุดเป็น dropdown มีรูปกล่อง · ✅ ชีพจรอยู่บนสุดเสมอ · ✅ ของฉันชิดขวาติดบัญชี · ✅ **ค้นหาชิดขวา 480px**

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3008/proto/navbar-ecom`** เคาะ D หรือ A
2. เคาะแล้วลงของจริง: `header.tsx` + `header-market-ticker.tsx` in-place · `--chrome-h` 8.25rem → **8.5rem** (D) หรือ **9rem** (A) · `HeaderCatalogControl` ต้องรับ `boxImageUrl` (มีใน `/api/sets` แล้ว) · ย้าย `MyStuffCluster` ขึ้นแถวบน
3. มือถือยังไม่ได้ออกแบบใหม่ — ทำต่อหลังเคาะ desktop
