# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบห้า · เบสเคาะแบบ B แล้ว + ขอรูปกล่อง)

**⚠️ ดูที่ `http://localhost:3007/proto/navbar-ecom` ไม่ใช่ 3000** — dev server ที่ 3000 (ของอีก session, PID 94622) ค้าง route manifest เสิร์ฟหน้านี้เป็น 404 ทั้งที่ compile ผ่าน เลยเปิดตัวใหม่ที่ 3007 ให้ · ถ้าอยากใช้ 3000 ต้องปิด-เปิดใหม่

### เคาะแล้ว: แบบ B + มีรูปกล่อง
โครงที่ล็อกแล้ว (สูง 136–152px):
1. **แถบชีพจร 32px** — สถิติ + สายพานการ์ดขยับแรง (ไม่แตะ เหมือนเดิม)
2. **แถวกลาง 56px** — โลโก้ · เมนูเว็บ 4 ลิงก์ · ช่องค้นหา 44px · อัปเกรด · ข้อความ · แจ้งเตือน · โปรไฟล์
3. **แถว 3 (แคตตาล็อกล้วน)** — ปุ่มเลือกเกม › ชั้นวางชุดเลื่อนได้ ‖ พอร์ต · รายการโปรด · Honey

**เหตุผลที่เลือก B:** เมนูเว็บอยู่คนละแถวกับชุด → เพิ่มปุ่มเมนูในอนาคตไม่แตะพื้นที่แคตตาล็อกเลย

### รูปกล่องที่เพิ่มรอบนี้
- ใช้ **`CardSet.boxImageUrl` ของจริง** (R2 `/sets/<code>.webp`) — ตัวเดียวกับที่แถบชุดหน้าแรกใช้ · ดึงมา bake เป็นค่าตายตัว 14 ชุด ไม่มี fetch ตอน render ตามกติกา proto
- `object-cover` ตัดขอบใสรอบกล่องทิ้ง ให้กล่องดูใหญ่กว่า `contain` (เหตุผลเดียวกับ home-set-strip)

### เหลือเลือกอย่างเดียว: ขนาดเม็ดยา
| แบบ | เม็ดยา | สูงรวม | เห็นกี่ชุดต่อหน้าจอ |
| --- | --- | --- | --- |
| **รูปกล่อง + รหัสชุด** ← ค่าเริ่มต้น | รูป 32px + "OP15" บรรทัดเดียว | 136px | ~8–9 ชุด |
| รูปกล่อง + ชื่อชุด | รูป 40px + รหัส + ชื่อชุด 2 บรรทัด (เหมือนแถบหน้าแรกเป๊ะ) | 152px | ~5–6 ชุด |

**ตรวจรับแล้ว (วัดในเบราว์เซอร์):** eslint 0 · tsc ผ่าน · รูปกล่อง **14/14 โหลดสำเร็จ ไม่มีรูปเสีย** · ทุกปุ่ม ≥40px · ชั้นวางเลื่อนได้จริง · ชื่อพอร์ต/รายการโปรด/Honey ครบ · ไม่ล้นที่ 1360px · ไม่มี console error

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3007/proto/navbar-ecom`** เลือกขนาดเม็ดยา: รูป+รหัส (กะทัดรัด) หรือ รูป+ชื่อชุด (อ่านง่ายกว่าแต่สูงกว่า 16px)
2. เคาะแล้วลงของจริง:
   - `header.tsx` + `header-market-ticker.tsx` แก้ in-place ตามโครง B
   - `--chrome-h` ใน `globals.css`: 8.25rem → **8.5rem** (ถ้าเลือกรูป+รหัส) หรือ **9.5rem** (ถ้าเลือกรูป+ชื่อชุด)
   - แถบชุดควรแยก component ใช้ร่วมกับ `home-set-strip.tsx` (เม็ดยา + กลไกเลื่อนเหมือนกันเป๊ะ)
   - ต้องต่อข้อมูลจริง: `/api/sets` มี `boxImageUrl` อยู่แล้ว
3. **คิดต่อ:** ถ้าแถบชุดขึ้น navbar ทุกหน้าแล้ว แถบชุดหน้าแรกยังต้องมีอีกไหม (เคยเป็นประเด็น ticker ซ้ำมาแล้ว)
4. มือถือยังไม่ได้ออกแบบใหม่ — ต้องทำต่อหลังเคาะ desktop
