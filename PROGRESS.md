# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-28 (ดึก รอบ 2) — **แถบชีพจรตลาดมีชีวิตแล้ว: การ์ดที่ราคาขยับวิ่งต่อเนื่อง มีสีเขียว/แดงจริง รอเบสดูบน preview**

## เรื่องราวทั้งสาย (จบใน session เดียว)

คำถามตั้งต้น "มีแนวทางทำ navbar ให้สวย/เหมาะใช้จริงกว่านี้มั้ย" → หน้าเทียบ 2 รอบ → เบสเคาะ **แบบ C** → เคาะ **ช่องค้นหาขวาสุดหลัง Honey** → ลงของจริง → เบสสั่งต่อ **"ชีพจรตลาดขอเพิ่มสี เพิ่มข้อมูล และให้มันเลื่อน"** → ทำเสร็จแล้ว

## รอบล่าสุด — แถบชีพจรมีชีวิต

**โครงแถบชีพจร (h-8 = 32px):** ซ้าย = ตัวเลขคงที่ปักนิ่ง · ขวา = **สายพานการ์ดวิ่ง**

- **เพิ่มสี (ถูกกติกา):** การ์ดในสายพานมี % ขึ้น/ลงจริง → เขียว/แดง + **ลูกศร ▲▼ คู่กันเสมอ** (VISION §4 กฎ 3: สีเป็นตัวเสริม ไม่ใช่ความหมายเดียว) · ตัวเลขรวมของเว็บฝั่งซ้ายยังเป็นสีกลาง เพราะ "มูลค่าคลังทั้งเว็บ" ไม่ใช่กำไร-ขาดทุน
- **เพิ่มข้อมูล:** การ์ดที่ราคาขยับแรงสุด 24 ชม. 12 ใบ (ขึ้น 6 + ลง 6 สลับฟันปลา) — ชื่อ + รหัสชุด + ราคา + % · กดไปหน้าการ์ดได้
- **การเลื่อน:** วิ่งต่อเนื่องไร้รอยต่อด้วย `.animate-ticker` (CSS ที่มีในระบบอยู่แล้ว ไม่ได้เขียนใหม่) — 56px/วินาที · **หยุดเมื่อเอาเมาส์ชี้** และ**หยุดเมื่อ tab โฟกัสด้วยคีย์บอร์ด** (กดลิงก์ทัน) · `prefers-reduced-motion` → หยุดวิ่งแล้วกลายเป็นแถบเลื่อนมือแทน (ไม่ซ่อนข้อมูล) · ขอบสองข้างจางแบบ mask ให้การ์ดวิ่งเข้า-ออกนุ่ม
- **ทิศทาง:** ขวา→ซ้าย (มาตรฐาน ticker การเงินทุกเจ้า — ข้อความใหม่เข้าจากขวา ตามทิศการอ่าน) · เบสอยากได้กลับด้าน แก้ keyframe บรรทัดเดียวใน globals.css

**คุณภาพข้อมูล (จุดที่เจอตอนดูของจริง):** รอบแรกสายพานโชว์การ์ด 120 JPY (~35 บาท) ที่ −20.0% ติดกัน 3 ใบ — ขยับจริงแค่ 7 บาท และเลขซ้ำจนดูเหมือนข้อมูลเสีย → ใส่**เกณฑ์ราคาขั้นต่ำ 500 JPY (~150 บาท)** ตามหลัก VISION §5.3 (movers ต้องวัดที่ขนาดการขยับ ไม่ใช่ %) ตอนนี้ทุกใบมีความหมายจริง (Sabo −80,675 JPY · Bonney −13,799 JPY · Luffy +4,933 JPY)

**API:** สร้าง `/api/cards/ticker` ใหม่ คืน ตัวเลขรวม + วันที่อัปเดต + movers ในคำขอเดียว **แทนที่ `/api/cards?limit=1` เดิม** (ของเดิมดึงการ์ดทั้งใบ + ตารางเชื่อม เพื่ออ่านเลขรวม 2 ตัว) → **จำนวน request ของ header เท่าเดิม (2 ครั้ง) แต่ payload เบาลงและได้ข้อมูลเพิ่ม** · เคยเผลอเอา query วันที่ไปฝังใน `/api/cards` (ซึ่งถูกเรียกทุกครั้งที่เปลี่ยนหน้า/ตัวกรองในตาราง) แล้วถอนออก
**ประหยัดเพิ่ม:** ลิงก์ในสายพาน 24 ตัวใช้ `prefetch={false}` (ตาม pattern เดิมของ header-catalog-control) ไม่กิน bandwidth ผู้ใช้

**ความสูง chrome:** 128 → **132px** (`--chrome-h` md: 8.25rem) — แถบชีพจร 32 + แถวโลโก้ 44 + แถวเมนู 56

**ตัวเลขที่ยุบตามความกว้าง** (ให้สายพานมีที่หายใจเสมอ): ชุดการ์ด+อัปเดตล่าสุด โผล่ที่ `xl` · JPY/THB ที่ `lg` · การ์ดทั้งหมด+มูลค่ารวม แสดงตลอด

## หลักฐานตรวจรับ

- ESLint **0 errors** · tsc ผ่าน · test **151 ไฟล์ / 907 ข้อผ่าน** (+1 เทสต์ใหม่ล็อกสายพาน: สี+ลูกศรคู่กัน · โคลน aria-hidden · หยุดตอน hover/focus · reduced-motion เป็นแถบเลื่อน · ไม่มีข้อมูลต้องไม่เหลือรางเปล่า) · build ผ่าน (`ƒ /api/cards/ticker` ขึ้นแล้ว) · ตัวตรวจดีไซน์ 0 findings
- Browser จริง: **พิสูจน์ animation ด้วยการวัด** — วิ่งเป็นเส้นตรง 0 → −3378px = ครึ่ง track เป๊ะ ⇒ **วนต่อเนียนไร้รอยต่อจริง** · 24 ลิงก์ (2 ชุด) · โคลน `aria-hidden="true"` · chrome สูง 133px · sticky ทุกจุดเกาะ 132px (หน้าชุด/หน้าการ์ด) · ไม่มี overflow ที่ 768/1024/1512 · โหมดสว่าง+มืดอ่านออกทั้งคู่ · ราคาแปลงบาทถูก
- ⚠️ **เจอ error ที่ไม่ใช่ของงานนี้:** console ขึ้น `Router action dispatched before initialization` (E668) ทุกหน้า **รวมหน้าที่ไม่มี navbar เลย** (`/login`, `/proto/navbar`) = ปัญหาเดิมระดับแอป ไม่ได้เกิดจาก navbar/ticker — แยกเป็น task ให้ตามต่อแล้ว (ยังไม่รู้ว่ากระทบ production build ไหม)

## สถานะ git

- branch `feat/navbar-direction-r2` · **ไม่มี** schema/migration/dependency/config change (route ใหม่ 1 ไฟล์ + field ใน response)
- ⚠️ **commit `06c9aa5` มีงานของ session อื่นติดไปด้วย** — ฉันใช้ `git add -A` เลยดูดไฟล์ที่ session คู่ขนานกำลังทำค้าง (`api/cards/spotlight/route.ts` + `route.test.ts` · `hooks/use-search-spotlight.ts` — งานหน้าค้นหาตอนยังไม่พิมพ์) เข้ามาในคอมมิตแถบชีพจร · **ไฟล์ไม่หาย ไม่พัง** (ผ่าน lint/test/build ทั้งหมด ยังไม่มีใคร import = ไม่กระทบหน้าเว็บ) แต่ประวัติปนสองงาน · ไม่แก้ให้เพราะ push แล้วและการถอดออกอาจทำลายงานค้างของเขา — **ห้ามใครไปลบไฟล์ชุด spotlight**
- หมายเหตุ: `/proto/navbar` = ภาพตอนตัดสินใจ (ยังไม่มีสายพาน) เก็บไว้เป็นบันทึก ไม่ใช่ของล่าสุด — ของจริงดูที่เว็บ

## NEXT

1. **เบสรีเฟรช `localhost:3000`** ดูสายพานวิ่ง — ลองเอาเมาส์ชี้ (ต้องหยุด) และกดการ์ดสักใบ (ต้องเข้าหน้าการ์ดได้)
2. อยากปรับได้ทันที: **ความเร็ว** (`ticker-scroll 60s` ใน globals.css) · **ทิศทาง** (สลับ from/to) · **จำนวนการ์ด** (`PER_DIRECTION` ใน api/cards/ticker) · **เกณฑ์ราคาขั้นต่ำ** (`MIN_PRICE_JPY = 500`)
3. พอใจแล้วเปิด PR `feat/navbar-direction-r2` → master
4. คิวเดิมค้าง: กวาด "อัปเดตทุกวัน" ~35 จุด · `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
