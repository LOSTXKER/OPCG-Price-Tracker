# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบเจ็ด · เสนอทางที่เปลี่ยนสมมติฐาน)

**⚠️ ดูที่ `http://localhost:3007/proto/navbar-ecom` ไม่ใช่ 3000** — dev server ที่ 3000 (ของอีก session, PID 94622) ค้าง route manifest เสิร์ฟหน้านี้เป็น 404 · ถ้าอยากใช้ 3000 ต้องปิด-เปิดใหม่

**เบสถาม "มีแบบอื่นอีกมั้ย"** → รอบก่อนทั้งสามแบบเถียงกันแค่ว่า "แถว 3 ควรมีอะไร" รอบนี้ถอยมาตั้งคำถามที่ใหญ่กว่า

| แบบ | เปลี่ยนสมมติฐานข้อไหน | ผลลัพธ์ | ช่องค้นหา |
| --- | --- | --- | --- |
| **D · ชุดเข้าช่องค้นหา** ← ค่าเริ่มต้น | ปุ่มชุดไม่ต้องเป็นปุ่มลอย — ยกเกม › ชุด (พร้อมรูปกล่อง) เข้าไปเป็นหัวช่องค้นหาแบบ Lazada/Amazon | แถว 3 ไม่เหลือแคตตาล็อกให้จัดเลย เหลือแค่นำทางซ้าย · ของฉันขวา **แถบมีของน้อยที่สุดในทุกแบบที่ทำมา** · ความหมายดีขึ้นด้วย: ชุดที่เลือก = ขอบเขตที่จะค้น | **958px** (กว้างสุด) |
| **K · สายพานลงล่างสุด** | แถบชีพจรไม่ต้องอยู่บนสุด — ย้ายลงล่างสุดติดเนื้อหา | ลำดับใหม่: ตัวตน+ค้นหา → นำทาง → ตลาด ซึ่งไหลต่อเข้าตารางราคาพอดี สายพานกลายเป็นบทนำของเนื้อหา ไม่ใช่แถบลอยเหนือหัว | 575px |
| B · สถิติลงแถว 3 | (จากรอบก่อน เก็บไว้เทียบ) | สายพานได้เต็มความกว้างแถบบน | 576px |

ทุกแบบสูง **136px** เท่ากันหมด เทียบกันได้ตรงๆ · แบบที่เคยเสนอแล้วไม่ถูกเลือก (A โล่งสองฝั่ง · C สองแถวจบ) ตัดออกจากหน้าแล้ว

**ตรวจรับแล้ว (วัดในเบราว์เซอร์):** eslint 0 · tsc ผ่าน · ทุกปุ่มควบคุม ≥40px · รูปกล่องโหลดครบ · ชื่อพอร์ต/รายการโปรด/Honey ครบ · ไม่ล้นที่ 1360px · ไม่มี markdown หลุด

### ประวัติที่เบสเคาะมาแล้ว (อย่าเสนอซ้ำ)
- ❌ ทาสีแบรนด์ทั้งแผงแบบ Shopee · ❌ แถวคำค้นยอดนิยม · ❌ ชั้นวางชุดเรียงยาว (รก)
- ✅ พื้นสีเดิม · ✅ ค้นหาใหญ่กลางแถว · ✅ ชุดเป็น dropdown ที่มีรูปกล่อง · ✅ ปุ่ม ≥40px · ✅ พอร์ต/รายการโปรด/Honey ห้ามซ่อนชื่อ · ✅ เมนูเว็บต้องแยกจากแคตตาล็อก (เผื่อเพิ่มปุ่ม)

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3007/proto/navbar-ecom`** ดู D กับ K — ถ้ายังไม่ใช่ บอกได้ว่าติดตรงไหน (โครง? ลำดับ? ความรู้สึก?) จะได้ยิงตรงจุด
2. เคาะแล้วลงของจริง: `header.tsx` + `header-market-ticker.tsx` in-place · `--chrome-h` 8.25rem → 8.5rem · `HeaderCatalogControl` ต้องรับ `boxImageUrl` (มีใน `/api/sets` แล้ว)
3. มือถือยังไม่ได้ออกแบบใหม่ — ทำต่อหลังเคาะ desktop
