# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🧰 หน้าลองมีบ้านแล้ว — หน้ารวม `/proto` + สกิลกลางใน bestos

**เบสสั่ง:** "ชอบการทำ proto แบบเว็บนี้ ช่วยบรรจุเป็น skill ให้ bestos · ให้เป็นพื้นฐานของ repo ใหม่ · อัปเดตทุก repo ที่มี"

- **หน้ารวม `/proto`** — เปิดที่เดียวเห็นหน้าลองทุกหน้า + คำถามที่ต้องเคาะ + สถานะ (รอเคาะ / เคาะแล้ว / เก็บอ้างอิง)
  ทะเบียนอยู่ที่ `src/app/proto/_registry.ts` — **ทำหน้าลองใหม่ต้องเพิ่มแถวที่นี่ด้วย ไม่งั้นเบสหาไม่เจอ**
- **ลิงก์พกตัวเลือกได้** — `/proto/search-header?v=cancel` เปิดแล้วได้แบบนั้นเลย · เบสก๊อปลิงก์ส่งกลับมาบอกว่าชอบอันไหนได้
  ต่อครบ 5 หน้าเดิม (search-header · search-ai · navbar · navbar-ecom · header-nav) · **`photo-scan` ยังไม่ได้ต่อ**
- **สกิล `/proto` ใน bestos** (`global-skills/proto/` → `~/.claude/skills/proto/`) = ทุก repo บนเครื่องเรียกใช้ได้
  · `/new-repo` §A5 สั่งให้ repo เว็บใหม่วางโครง `/proto` ตั้งแต่ตอน scaffold

**ตรวจรับ:** eslint 0 error · tsc ผ่าน · test 924 ผ่าน · เปิดดูจริงทั้งคอมและมือถือ · ยืนยัน `noindex` ยังอยู่

## 🆕 proto ใหม่ — ภาษาภาพของส่วน AI (เบสส่งภาพ reference มา)

**ดูที่ `http://localhost:3001/proto/ai-look`**

**เบสสั่ง:** ส่งภาพกล่องค้นหาที่มีเงาไล่สีรุ้งรอบกล่อง + ปุ่ม "Try" ประกาย + toggle "Ask AI" มา แล้วบอก "ความเป็นปุ่ม AI ยุคนี้เขาทำประมาณนี้ ลองปรับหน่อย"

### คำถามที่ต้องเคาะ
**ยอมให้สีม่วง-ฟ้าที่ไม่มีอยู่ในเว็บเราเลย โผล่มาที่จุด AI ไหม** — ท่าเอามาทั้งชุด (เงาฟุ้ง + ไอคอนประกาย) ต่างกันแค่ใช้สีอะไร

| แบบ | สี | ข้อแลก |
| --- | --- | --- |
| ปัจจุบัน | แถบไล่สีน้ำตาลทึบ | บอกว่าเป็น AI ด้วยตัวหนังสือ ไม่ได้บอกด้วยภาษาภาพ |
| **A · เรืองแสงโทนเรา** ← ค่าเริ่มต้น | ทอง-ส้มอบ (สีแบรนด์) | เงาทองบนพื้นครีมโหมดสว่างจะจางกว่าเงารุ้ง |
| B · รุ้งแบบที่ส่งมา | ม่วง-ฟ้า-ชมพู | สีชุดนี้ไม่มีในเว็บเลย จะอ่านเป็น "ของนอก" หรือ "ของพิเศษ" ก็ได้ — **จุดที่ต้องเบสตัดสิน** |
| C · ขอบไล่สีวิ่ง | ทอง เป็นเส้นขอบหมุน | เนียนกว่า กินที่น้อยกว่า แต่ขยับตลอดเวลา |

หน้า proto โชว์ **ทั้งในช่องค้นหา (มือถือ + คอม) และปุ่มค้นหาแถบล่างพร้อมกัน** · มีปุ่มสลับ "ดูตอนกำลังสแกน" เพราะของแบบนี้ต้องดูตอนขยับ · ลิงก์พกตัวเลือกได้ (`?v=aurora`)

**หมายเหตุ:** ไอคอนประกายในภาพคือ `Sparkles` ซึ่งเว็บเราใช้ไปแล้ว 3 ที่ — ใน proto ใช้ `WandSparkles` ที่หน้าตาใกล้กันแต่ยังว่าง

**ตรวจรับ:** eslint 0 · tsc ผ่าน · เปิดดูจริงครบ 4 แบบ × ทั้งนิ่งและกำลังสแกน · ลงทะเบียนใน `/proto` แล้ว

## ✅ จอสแกนการ์ดเต็มจอ — **ลงเว็บจริงแล้ว** (เบสเคาะแบบ B)

**เบสสั่ง:** "เอาแบบนี้เลย และเอา B" → ลงของจริงเรียบร้อย

### เปลี่ยนจากอะไรเป็นอะไร
| เดิม | ใหม่ |
| --- | --- |
| dialog เล็ก มีปุ่ม 2 ปุ่ม (ถ่ายรูป/อัปโหลด) → ส่งงานให้แอปกล้องของระบบ | **จอกล้องเต็มจอของเราเอง** (`getUserMedia`) · กรอบสัดส่วนการ์ด 63:88 · กริดจุด + มุมเรืองแสง |
| ต้องเลือกรูป → กดปุ่ม "วิเคราะห์" อีกที | **กดชัตเตอร์/เลือกรูป = ส่งเลย** ไม่มีขั้นกลาง |
| หมุนรอเฉยๆ | เส้นกวาดในกรอบ + การ์ดสถานะบอกว่ากำลังเทียบกับอะไร |
| ผลลัพธ์อยู่ใน dialog เล็ก | **sheet เลื่อนขึ้นจากล่าง** ทับภาพที่ถ่าย |

### ⚠️ สิ่งที่ทำไม่ได้ตาม proto (บอกไว้ให้ตรง)
proto โชว์ "มั่นใจ 97%" แต่ **API ไม่ได้ส่ง % ความมั่นใจมา** — ไม่ใส่ตัวเลขปลอม · ส่วน**ตัวเลือกรองมีจริง** เพราะ API คืน `cards[]` หลายใบอยู่แล้ว (ใบแรกตรงสุด)

### กันทางตัน
ถ้ากล้องเปิดไม่ได้ (ปฏิเสธสิทธิ์ / เบราว์เซอร์ไม่รองรับ / ไม่ใช่ HTTPS) → **fallback เป็นปุ่มถ่ายรูป (แอปกล้องระบบ) + อัปโหลด** พร้อมข้อความบอกเหตุผล · **ทดสอบจริงแล้ว** (พาเนลทดสอบบล็อกกล้อง → fallback ขึ้นถูกต้อง)

### รายละเอียดที่ใส่ใจ
- **ปล่อย stream ทันทีที่ปิดจอ** — หน้าเว็บที่ยังเปิดกล้องค้างคือทางที่เร็วที่สุดที่จะโดนถอนสิทธิ์ถาวร
- animation อยู่ใน `globals.css` (`.scan-sweep` · `.scan-corner`) + honor `prefers-reduced-motion`
- เพิ่มคำแปล 4 คำ ครบ 3 ภาษา (ไทย/อังกฤษ/ญี่ปุ่น)

**ตรวจรับ:** eslint 0 errors · tsc ผ่าน · test **924 ผ่าน** · build ผ่าน · เปิดมือถือจริง: จอเต็ม 375×812 · กรอบ 63:88 · ปุ่มอัปโหลดซ้ายล่าง · fallback ทำงาน

## ✅ ค้นหาด้วยรูป · มาแรง 12 ใบ · ปุ่มค้นหาเรืองแสง — **ลงเว็บจริงแล้ว**

**เบสเคาะจาก `/proto/search-ai`:** แถวรูป = **B (แถบเน้น)** · ปุ่มค้นหา = **C (เรืองแสง)** · มาแรง = **ยาวเต็มจอ**

### 1 · แถวค้นหาด้วยรูป → แบบ B
ไอคอน `ScanSearch` ในวงกลม + พื้นไล่สีจาง + **2 บรรทัด** (หัวข้อ + ป้าย "AI" + คำอธิบาย)
**ต้นเหตุที่มันดูไม่เป็น AI:** คำอธิบาย ("ระบบจะใช้ AI วิเคราะห์ให้") มีอยู่แล้ว แต่ `hidden sm:inline` → **มือถือไม่เคยเห็น** เหลือแค่ไอคอนกล้อง + 4 คำ ที่อ่านเป็นปุ่มอัปโหลดธรรมดา · ตอนนี้เห็นทุกความกว้าง

### 2 · ปุ่มค้นหาแถบล่าง → แบบ C
เก็บไอคอนแว่นที่คนคุ้นไว้ (ไอคอนบนแถบล่างคือสิ่งที่ไม่ควรต้องถอดรหัส) แล้วเพิ่ม**วงเรืองแสงไล่สี**หลังปุ่ม + ปุ่มเป็น gradient — บอกว่า "มีอะไรฉลาดกว่าการจับคำ" โดยไม่เปลี่ยนความหมาย

### 3 · มาแรง 6 → **12 ใบ**
แยก `POPULAR_COUNT = 6` (ชิปแนวนอน 6 พอ) กับ `MOVERS_COUNT = 12` (รายการแนวตั้งในจอเต็ม) ใน `/api/cards/spotlight`

**⚠️ ไอคอน:** เลี่ยง `Sparkles` เพราะถูกใช้ไปแล้ว 3 ที่ (แพ็กเกจ · หน้าแรก · ท้ายเว็บ) — ความหมายที่ 4 จะทำให้มันไม่แปลว่าอะไรเลย

**ตรวจรับ:** eslint 0 · tsc ผ่าน · test **924 ผ่าน** (อัปเดตเทสต์ API ให้ล็อก `[6, 12]`) · build ผ่าน · **วัดจริงบนมือถือ: แถวรูปมีป้าย AI + คำอธิบายเห็นแล้ว · มาแรง 12 ใบ · รวม 18 รายการในจอเดียว**

## ✅ เอาส่วน "หน้า" ออกจากช่องค้นหา (เบสสั่ง 2026-08-29)

**เบสสั่ง:** "ส่วนหน้าขอเอาออก" (เม็ดยาทางลัด 12 หน้าใน popup ค้นหา)

- **ตอนยังไม่พิมพ์ → เอาออกหมด** เหลือแค่ ค้นหาล่าสุด · ยอดนิยม · มาแรง
- **ตอนพิมพ์แล้ว → ยังอยู่** พิมพ์ "พอร์ต" ก็ยังเจอหน้าพอร์ต (พฤติกรรม command palette ที่มีประโยชน์จริง)

**เหตุผล:** 12 เม็ดยาสำหรับหน้าที่แถบเมนูกับแถบล่างมีอยู่แล้ว ดันของที่เป็นเหตุผลให้เปิด popup (ค้นหาล่าสุด/ยอดนิยม/มาแรง) ตกจอ — โดยเฉพาะบนมือถือที่เพิ่งทำเป็นเต็มจอ

**ตรวจรับ:** eslint 0 · tsc ผ่าน · test **924 ผ่าน** (อัปเดตเทสต์ที่ล็อกว่าเม็ดยาต้องอยู่ ให้ล็อกว่า**ต้องไม่อยู่ตอนว่าง แต่ยังอยู่ตอนพิมพ์**) · build ผ่าน · เปิดมือถือดูจริงทั้งสองสถานะ

## ✅ ช่องค้นหาบนมือถือ — เต็มจอแล้ว (เบสสั่ง 2026-08-29)

**เบสสั่ง:** "ค้นหาในมือถืออยากให้เต็มจอไปเลยอะ" → ทำแล้ว

- ใช้ **`ResponsiveDialogContent`** ที่มีอยู่แล้วใน kit (`ui/responsive-dialog-content.tsx`) — เต็มจอบนมือถือ / การ์ดลอยกลางจาก `md` ขึ้นไป · **ไม่สร้าง component ซ้ำ** ตามกฎ Component Kit
- **รายการผลลัพธ์ยืดเต็มความสูงที่เหลือ** บนมือถือ (`flex-1` ในคอลัมน์ flex ของ shell) แทน `max-h` ตายตัว — ไม่หยุดสั้นก่อนถึงคีย์บอร์ด · จาก `md` ขึ้นไปกลับไปเป็น `max-h-[min(60vh,34rem)]` เหมือนเดิม

**เหตุผล:** บนมือถือ "ค้นหา" คือทั้งงาน การ์ดลอยที่ 15vh ทิ้งแถบหน้าเว็บโผล่ทั้งบนและล่าง ขณะที่คีย์บอร์ดกินพื้นที่ล่าง → รายการมีที่น้อยกว่า chrome ที่มันบังเสียอีก

**วัดจริง:** มือถือ 375×812 → dialog **375×812 เต็มจอพอดี มุมไม่โค้ง** รายการยืดถึงขอบล่าง (811px) · คอม 1400 → การ์ดลอย 672px มุมโค้ง อยู่ที่ 15vh เหมือนเดิม ปุ่ม "ยกเลิก" อยู่ครบ

**ตรวจรับ:** eslint 0 errors · tsc ผ่าน · test **924 ผ่าน** · build ผ่าน · เปิดดูจริงทั้งมือถือและคอม

## ✅ หัวช่องค้นหา — เบสเคาะแบบ C **ลงเว็บจริงแล้ว**

**เบสสั่ง:** "Ui อันนี้ออกแบบดีๆหน่อย แล้วจะมี esc เพื่อ" → ทำ proto (`/proto/search-header`) เสนอ 3 ทาง → **เบสเคาะ C แล้วสั่งลงเว็บจริง**

### เปลี่ยนอะไร (`command-search.tsx`)
| เดิม | ใหม่ |
| --- | --- |
| ป้าย `ESC` (เป็นปุ่มปิดจริง แต่หน้าตาเป็นป้ายคีย์ลัด) | **ปุ่ม "ยกเลิก"** สีแบรนด์ สูง 40px — ท่าเดียวกับช่องค้นหา iOS |
| ปุ่ม "ค้นหา" สีทึบ (จาง 30% ตอนยังไม่พิมพ์) | **ตัดทิ้ง** |
| ปุ่มล้างคำ สูง 24px | สูง 36px กดได้จริง |

**เหตุผล 3 ข้อ:**
1. ESC เป็นปุ่มปิดจริงแต่ใส่เสื้อป้ายคีย์ลัด → ไม่มีใครอ่านออกว่ากดได้
2. **บนมือถือไม่มีปุ่ม Esc บนคีย์บอร์ด** → ป้ายนั้นโฆษณาทางลัดที่ไม่มีจริง
3. ปุ่ม "ค้นหา" ซ้ำกับแถว **"ดูผลลัพธ์ทั้งหมดสำหรับ …"** ที่อยู่ท้ายรายการผลลัพธ์อยู่แล้ว (ตรวจแล้วยังอยู่ครบ)

Esc ยังกดปิดได้เหมือนเดิม แค่ไม่ต้องมีป้ายบอก · คำว่า "ยกเลิก" ใช้ key `cancel` ที่มีครบ 3 ภาษาอยู่แล้ว

**วัดจริง:** คอม — ไม่มี ESC · ไม่มีปุ่มค้นหา · ปุ่มยกเลิก 40px · ช่องพิมพ์ได้ 541px (เดิมโดนสองปุ่มกินไป) · มือถือ 375 — เหมือนกัน ช่องพิมพ์ 244px

**ตรวจรับ:** eslint 0 errors · tsc ผ่าน · test **924 ผ่าน** · build ผ่าน · เปิดดูจริงทั้งคอมและมือถือ ทั้งตอนพิมพ์แล้วและยังไม่พิมพ์

## 🔧 แก้ต่อ — แถบตัวกรองมือถือลอยทับการ์ด (เบสทัก · แก้แล้ว)

**อาการ:** เลื่อนลงบนมือถือ แถบตัวกรอง (Raw/PSA · ราคา · เปลี่ยนแปลง) **ลอยห่างจากหัวเว็บ 48px** เห็นแถวการ์ดโผล่ตรงกลาง

**ต้นเหตุ (regression จากงานตัวเอง):** `--chrome-h` มือถือ = 104px (แถว 1 = 56 + แถวบริบท = 48) แต่ฉันทำให้แถวบริบท**หายตอนเลื่อน** → chrome จริงเหลือ 56px ส่วนแถบตัวกรองที่ `sticky top-[var(--chrome-h)]` ยังยึด 104px

**แก้ด้วยการเปลี่ยนวิธีคิด — ให้ layout จัดการแทน JS:**
- แถว 1 กับแถวบริบทเป็น **พี่น้องกัน ไม่ห่อในกล่องเดียว** (sticky child เลื่อนได้แค่ในกรอบ parent — ถ้าห่อรวม แถว 1 จะหลุดตามไปด้วย)
- แถว 1 sticky · **แถวบริบทไม่ sticky เลื่อนหายเอง** ตามธรรมชาติ ไม่ต้องซ่อนด้วย flag
- `--chrome-h` มือถือ → **3.5rem (56px)** = ความสูง sticky จริง ซึ่งคงที่เสมอ

**วัดจริงหลังแก้:** ตอนบนสุด header 0–56px · แถวบริบทต่อที่ 56 · ตอนเลื่อน header ยังเกาะ 0–56 · แถวบริบทหายไป · **แถบตัวกรองติดที่ 56px พอดี (gap = 0)**

**ตรวจรับ:** eslint 0 · tsc ผ่าน · test 924 ผ่าน · build ผ่าน · เปิดมือถือ 375 ดูจริงทั้งตอนบนสุดและตอนเลื่อน

## ✅ ปิดงาน — **navbar D2 ขึ้น master แล้ว** (`54bfa2d` · เบสสั่ง push เอง)

Vercel deploy อัตโนมัติ · เบสสั่ง "commit push main ให้หมดเลย" จึง push เข้า master ตรง (ปกติกฎห้าม push master ของ repo ที่ต่อ Vercel)

### แถบเมนู desktop ที่ขึ้นเว็บ — 132px → **104px**
```
[48] เกม▾ › ชุด▾(รูปกล่อง) ‖ สถิติ · สายพาน ‖ อัปเกรด · แชท · แจ้งเตือน · โปรไฟล์
[56] โลโก้ · เมนู 4 ลิงก์ ⟷ พอร์ต · รายการโปรด · Honey · ค้นหา 320px
```
**คืนพื้นที่ให้เนื้อหา 28px ทุกหน้า**

### ⚠️ merge สองสายที่ชนกัน — แก้แล้ว
master มีงาน **แถบเมนูมือถือใหม่** (สองแถว: ชื่อหน้า + แถวบริบทเกม›ชุด พร้อม `SetArtwork`) จากอีก session ที่แตะไฟล์เดียวกัน · รวมกันแล้วได้ทั้งคู่:
- **มือถือ** ใช้โครงของ master (ดีกว่าและใหม่กว่า) — เจตนา "เลื่อนลงแล้วชุดไม่ตามมา" ที่เบสสั่ง ย้ายไปใช้กับ**แถวบริบททั้งแถว** (ซ่อนทั้งแถบ ไม่ใช่แค่ปุ่ม เพราะแถบจางเปล่าๆ แย่กว่าไม่มี)
- **desktop** ปุ่มชุดใช้ `SetArtwork` ตัวเดียวกับมือถือ **ไม่สร้างซ้ำ** ตามกฎ Component Kit

### responsive ที่ต้องมี (เจอตอนลงจริง)
ที่ 1024px แถบเคยล้นจนสายพานหาย → ให้ของหลบตามความกว้าง:
| จอ | สถิติ | ชื่อชุดในปุ่ม | **สายพาน** |
| --- | --- | --- | --- |
| 1024 | มูลค่ารวม | ซ่อน | 230px |
| 1280 | + การ์ดทั้งหมด | โชว์ | 304px |
| 1440 | เหมือน 1280 | โชว์ | 464px |
| 1536+ | ครบ 3 ตัว | โชว์ | 422px+ |

### ตรวจรับก่อนขึ้น master
`npx eslint src` **0 errors** · `npm run test` **924 ข้อผ่าน / 154 ไฟล์** · `npm run build` ผ่าน · วัดจริงบน production server ที่ 1024/1280/1440/1600 + มือถือ 375

### ไฟล์ที่แตะ
`header.tsx` · `header-market-ticker.tsx` · `header-mobile.tsx` · `header-catalog-control.tsx` · `set-picker.tsx` · `globals.css` (`--chrome-h` → 6.5rem) · เทสต์ 2 ไฟล์ (อัปเดต topology ไม่ได้ลบ) · `proto/navbar-ecom/page.tsx` (หน้าทดลอง เก็บไว้)

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเช็คเว็บจริงหลัง Vercel deploy** — ถ้าแถบยังสูงแบบเดิมให้ hard reload (CSS cache)
2. branch `feat/navbar-ecom-proto` ยังอยู่บน remote ถ้าอยากลบสั่งได้
3. มือถือ: แถวบริบทหายตอนเลื่อนแล้ว แต่ผังมือถือโดยรวมเป็นของอีก session ไม่ได้รื้อเพิ่ม
4. ของค้างเดิม: `priceValidUntil` ใน `json-ld.ts` · `getHomeData().rarityRows` query ทิ้งเปล่า
