# สถานะสด — meecard

> เขียนทับทุกครั้งที่จบงาน · อ่านไฟล์นี้ก่อนเริ่ม session ใหม่

**อัปเดต:** 2026-08-29 (คืนนี้)
**branch ที่ทำอยู่:** `master` (local ahead 1) · งานล่าสุด push ไว้ที่ `proto/photo-desktop`

## ทำอะไรไปคืนนี้

1. **แถบเมนูโฉมใหม่ (D2)** — ขึ้นเว็บจริงแล้ว: สองแถว 104px (จากเดิม 132px) · แถว 1 = เลือกเกม›ชุด + สถิติ + ชีพจรตลาด + บัญชี · แถว 2 = โลโก้ + เมนู + ของฉัน + ช่องค้นหา · มือถือ แถวเลือกชุดไม่ตามลงมาเวลาเลื่อน
2. **หัวช่องค้นหา** — ปุ่ม "ยกเลิก" แบบ iOS แทนป้าย ESC · มือถือเปิดเต็มจอ · ตัดทางลัดไปหน้าต่างๆ ออกตอนยังไม่พิมพ์ — ขึ้นเว็บแล้ว
3. **ค้นหาด้วยรูป** — แถวแบนเนอร์ยาวเต็มความกว้าง + ป้าย AI · มาแรงยาวขึ้นเป็น 12 ใบ — ขึ้นเว็บแล้ว
4. **จอสแกนการ์ด** — เปิดกล้องเต็มจอแบบแอปธนาคาร (อัปโหลดซ้ายล่าง · ชัตเตอร์กลาง · กรอบสัดส่วนการ์ด · ลายกริดเทคตอนสแกน) — ขึ้นเว็บแล้ว
5. **ภาษาภาพของส่วน AI** (`/proto/ai-look`) — ยังอยู่ในหน้าลอง: แบบ A เรืองแสง · C ขอบหมุน · D ขอบไล่สีไหลวนสามชั้น (เงาใต้ขอบ + เส้นขอบ + ประกายพาดเฉียง)
6. **ปุ่มค้นหาที่แถบล่าง** — เบสเคาะว่า **เอาแบบปัจจุบัน** (ปุ่มทองกลมเรียบ ไม่มีวงแหวน) เพราะทองซ้อนทองจมกันเอง

## ค้างตรงไหน

- **`/proto/ai-look` รอเบสเคาะ** ว่าจะเอา A / C / D สำหรับช่องค้นหา — เคาะแล้วค่อยลงของจริงที่แถวค้นหาด้วยรูปใน `src/components/shared/command-search.tsx`
- **`/proto/photo-desktop` รอเบสเคาะ** (ใหม่คืนนี้) — เบสถามว่าค้นด้วยรูปบนคอมควรให้อัปโหลดอย่างเดียวไหม · มีสามทางให้เลือก: ปัจจุบันเปิดกล้องเต็มจอ · ก อัปโหลดอย่างเดียว (ลากวาง/Ctrl+V) · ข อัปโหลดเป็นหลัก + ปุ่มกล้องสำรอง
- `master` local นำหน้า remote อยู่ 1 คอมมิต — ยังไม่ push เข้า master ตรงๆ ตามกฎ (งานเดียวกันอยู่บน branch `proto/photo-desktop` แล้ว)

## NEXT

1. เบสเปิด `/proto/photo-desktop` แล้วบอกว่าเอาแบบไหน (ฉันแนะนำ **ข** — ได้ท่าที่คนคอมใช้จริงคือวางรูป และไม่ตัดคนที่มีกล้องต่ออยู่)
2. เคาะ `/proto/ai-look` ให้จบ แล้วลงของจริงทั้งสองเรื่องพร้อมกัน
