# สถานะสด — meecard

> เขียนทับทุกครั้งที่จบงาน · อ่านไฟล์นี้ก่อนเริ่ม session ใหม่

**อัปเดต:** 2026-08-31 (เช้ามืด)
**branch:** `master` (เบสสั่ง merge + push ขึ้น master เอง 2026-08-30 · Vercel กำลัง deploy)

## ทำอะไรไปรอบนี้ — เว็บติดตั้งลงหน้าจอโฮมได้แล้ว (PWA)

เบสสั่ง: "navbar มุมมือถือ อยากให้ข้างบนมีกดเพื่อเพิ่มไปยังหน้าโฮมด้วย"
ของเดิมยังไม่มีพื้นฐาน PWA เลย จึงทำทั้งก้อน: ตัวเว็บติดตั้งได้จริง + ปุ่มกด

### 1. พื้นฐานทำให้ติดตั้งได้จริง ✅ (ไม่มีทางเลือกให้เคาะ ทำเลย)

| ของ | ไฟล์ | ได้อะไร |
|---|---|---|
| บัตรประจำตัวแอป | `src/app/manifest.ts` | แอนดรอยด์รู้จักเว็บเป็นแอป · ชื่อ "Meecard" · เปิดเต็มจอไม่มีแถบที่อยู่เว็บ · ทางลัดกดค้างที่ไอคอน 3 อัน (รายการโปรด · พอร์ต · ค้นหา) |
| ไอคอนหมี | `public/icons/*.png` | 192 · 512 · maskable 512 · apple 180 — หมี Kuma บนพื้นสีแบรนด์ (ทำจาก `public/meecard.png` ด้วย `sips`) |
| ไอคอน iPhone | `src/app/apple-icon.tsx` | เดิมเป็นตัวอักษร "M" → เปลี่ยนเป็นหมี · อ่านไฟล์ตอน build แล้วส่งไบต์ตรงๆ (ไม่ผ่าน next/og เพราะรูป 567KB ทำ satori ล้ม) |
| สีแถบสถานะ | `src/app/layout.tsx` (`viewport`) | ขาว/ดำตามธีม ตรงกับ `--background` · `viewportFit: cover` เผื่อรอยบาก |
| หน้าจอตอนเน็ตหลุด | `public/offline.html` + `public/sw.js` | เห็นหมี + ปุ่ม "ลองใหม่" แทนไดโนเสาร์ของเบราว์เซอร์ |

**service worker ตั้งใจให้เกือบว่างเปล่า** — ไม่ cache หน้าเว็บ/ราคาเลย
(เว็บนี้ขายความสด · worker ที่ cache ผิดจะโชว์ราคาเก่าโดยไม่มีใครรู้ และปลดยาก)
มีไว้แค่ 2 อย่าง: ทำให้แอนดรอยด์ยอมเสนอติดตั้ง + หน้าจอตอนเน็ตหลุด ·
**ไม่ register บน localhost** (worker ค้างบน :3000 จะไปกวน project อื่น)

### 2. สมองกลางของฟีเจอร์ ✅

`src/hooks/use-install-prompt.ts` — ที่เดียวที่ตอบว่า "ตอนนี้ชวนติดตั้งได้ไหม"

- แอนดรอยด์ = กดปุ่มเดียวเด้งแผ่นติดตั้งจริง · iPhone = ไม่มี API เลย ต้องสอนวิธี
- เก็บ event ไว้ที่ระดับโมดูล + สคริปต์เล็กๆ ใน `<head>` — เพราะ Chrome ยิง
  `beforeinstallprompt` **ครั้งเดียว** และมักยิงก่อน React จะ hydrate เสร็จ
  (ถ้าดักใน component จะพลาด แล้วปุ่มจะไม่โผล่เลย — บั๊กที่หาไม่เจอทีหลัง)
- ปิดแล้วเงียบ 30 วัน · ติดตั้งแล้วไม่ชวนอีก · Chrome/Firefox บน iOS ถูกคัดออก
  (มันเพิ่มหน้าจอโฮมไม่ได้ สอนไปก็ทำตามไม่ได้)
- เทส 10 ข้อคุม UA จริง + วันหมดอายุ (`use-install-prompt.test.ts`)

### 3. ปุ่มที่เบสขอ ✅ เบสเคาะแบบ ก แล้ว + จัดแถวใหม่ตามที่สั่ง

- **แถบบนมือถือ** (`InstallHeaderButton` ใน `header-mobile.tsx`) — ไอคอน **สี่เหลี่ยมมีบวก**
  ข้างซ้ายของหัวใจ · **โผล่เฉพาะคนที่ติดตั้งได้และยังไม่ติดตั้ง** คนอื่นแถวกว้างเท่าเดิม
- **"ดูเพิ่มเติม"** (`InstallAppRow` ใน `/more`) — ทางกลับถาวร เผื่อกดปิดไปแล้วเปลี่ยนใจ
- **กล่องสอนวิธี iPhone** (`InstallGuideDialog`) — 3 ขั้น + ไอคอนแชร์ · ใช้ร่วมทั้งสองที่
- คำแปลครบ 3 ภาษา (`installApp*` ใน th/en/jp)

### 4. รอบจัดใหม่ (เบสสั่ง 2026-08-30: "เอา ก แต่จัดดีๆ · navbar เอาคำว่า Meecard")

- **navbar ไม่โชว์ชื่อหน้าแล้ว → เป็นคำว่า "Meecard"** ข้างหมี · หมีกับคำเป็น **ลิงก์เดียว**
  (สองปุ่มติดกันที่ไปที่เดียวกัน = กดพลาดแน่นอน) · ฟังก์ชัน `resolvePageTitle` ลบทิ้งแล้ว
  ผู้ใช้ยังรู้ว่าอยู่ไหนจากแถบล่างที่ไฮไลต์ + หัวข้อใหญ่ของหน้า + แถวเกม/ชุด
- **ปุ่มเครื่องมือ 3 ตัวใส่ทรงเดียวกัน** (`TOOL_BUTTON` ประกาศครั้งเดียว) —
  ต้นเหตุที่แถวดูไม่จบคือ **กระดิ่งไม่มีวงกลม** ในขณะที่อีกสองปุ่มมี (ของเดิม ไม่ใช่ของใหม่)
  · `NotificationBell` รับ `className` เพิ่ม เพื่อไม่ให้กระทบแถบบนคอม
- **ไอคอนติดตั้งเปลี่ยน 3 รอบกว่าจะลงตัว** (เบสทัก: "ไม่สื่อ และไม่ค่อยชวนน่ากด")
  1. มือถือ + ป้ายบวกแปะมุม → ป้ายล้นออกนอกขอบวงกลมที่ 18px ปุ่มเสียทรง
  2. `SquarePlus` (ไอคอนที่ iPhone ใช้จริง) → คนที่ไม่เคยติดตั้งเว็บเป็นแอป
     อ่านว่า "เพิ่มอะไรสักอย่าง" เฉยๆ
  3. **ของตอนนี้: มือถือมีลูกศรลงอยู่ในจอ** (`InstallGlyph` วาดเอง — lucide
     ไม่มีไอคอนแบบนี้) บอกครบทั้งสองท่อนในรูปเดียว "ของชิ้นนี้" + "ลงไปในเครื่อง"
     และไม่มีอะไรล้นขอบ · วาดด้วยภาษาเส้นเดียวกับ lucide (เส้น 2px ปลายมน กรอบ 24)
     ตัวเครื่องกว้างกว่าของ lucide และตัดขีดปุ่มโฮมออก ไม่งั้นที่ 18px กลายเป็นก้อนเทา
- **ปุ่มติดตั้งใช้พื้นทอง** (`bg-primary/15`) ต่างจากปุ่มเครื่องมือที่พื้นเงียบ —
  ตั้งใจ: มันคือคำชวนครั้งเดียวที่หายไปถาวรเมื่อกดแล้ว ถ้าหน้าตาเหมือนเฟอร์นิเจอร์
  ข้างๆ คนก็อ่านว่าเป็นเฟอร์นิเจอร์ · ทรงยังเท่ากันเป๊ะ แถวจึงยังเป็นจังหวะเดียว
- **`/proto/pwa-install` มีที่เทียบไอคอน 9 แบบ** วางซ้อนในแถวจริง (เทียบไอคอนต้อง
  เห็นพร้อมกัน สลับทีละอันแล้วจำไม่ได้) — รวมแบบ "ปุ่มมีคำว่าติดตั้ง" ที่เห็นผลจริงว่า
  คำว่า Meecard จะโดนตัดเป็น "Meeca…" ที่จอ 360px
- **ใส่ระยะห่างระหว่างปุ่ม 6px** (`gap-1.5`) — เบสทักรอบสองว่า "มันติดไป":
  วงกลม 44px เรียงชนกันพอดีอ่านเป็นโซ่ต่อกัน ไม่ใช่ปุ่ม 3 ตัว (ทรงกลมไม่ทนการชิด
  แบบทรงเหลี่ยม) · เส้นคั่นเลิกมี margin ของตัวเอง ให้ระยะมาจาก gap ทางเดียว
- วัดที่จอ 360px หลังจัดใหม่: ปุ่มทุกตัวยัง 44×44 ครบ · ระยะห่างเท่ากันหมด 6px ·
  แบรนด์ได้ที่ 137px และคำว่า Meecard ยังไม่ถูกตัด
- เทสโครงแถว (`header-catalog-control.test.tsx`) อัปเดตให้ล็อกกติกาใหม่:
  ห้ามกลับไปโชว์ชื่อหน้า · ปุ่มเครื่องมือต้องใช้ `TOOL_BUTTON` ตัวเดียว

`/proto/pwa-install` — 4 ทางยังอยู่ให้เทียบ (ปัจจุบัน · **ก ที่เคาะแล้ว** · ข แถบเชิญชวน ·
ค แผ่นเด้งจากล่าง) · กล่องสอนวิธี iPhone ในหน้านี้เป็นของจริง ไม่ใช่ของจำลอง

ตรวจครบ: lint (ไฟล์ใหม่สะอาด) · typecheck · เทส 934 ข้อ · `npm run build` ผ่าน ·
เปิดดูจริงที่ 360px และ 375px ทั้งสองธีม · กดปุ่มแล้ว flow ครบ (กดปิด → หาย → `/more` ยังมี)

## แถบควบคุมหน้าแรกมือถือ — ลองแล้วย้อนกลับ (2026-08-30 → 31)

เบสสั่งย้าย 3 อย่าง (ชุดเข้าตัวกรอง · มุมมองไปขวาแคปซูลเรียง · เกรดไปซ้ายตัวกรอง)
→ ทำหน้าลอง `/proto/mobile-toolbar` 4 แบบ → เบสถามหาแบบที่ดีกว่า → เสนอ **D**
(ปุ่มตัวกรองพูดชื่อชุด) → เบสให้ลองของจริง → **ลองบนเว็บจริงแล้วเบสขอย้อนกลับ**

**สถานะตอนนี้: แถบหน้าแรกมือถือเป็นแบบเดิมทุกอย่าง** — ชุดอยู่นอกแถบ ·
เกรดอยู่ในแถวที่เกาะตอนเลื่อน · มุมมองอยู่แถวบน · `AGENTS.md` กลับเป็นกติกาเดิม
("set อยู่นอก modal") · เทสที่ล็อกโครงกลับไปคุมโครงเดิมแล้ว

ที่ยัง**คงอยู่**จากรอบนี้ (เบสไม่ได้สั่งย้อน): แคปซูลเรียงที่รวมปุ่ม "ราคา"
เข้ากับ "เปลี่ยนแปลง · 7d" เป็นกรอบเดียว

**ถ้าจะรื้อฟื้นแบบ D:** โค้ดครบอยู่ในคอมมิต `16a33b3` — revert คอมมิตที่ย้อนมัน
เอาแทนการเขียนใหม่ · หน้าลอง `/proto/mobile-toolbar` ยังอยู่ กดดูเทียบได้ทั้ง 4 แบบ

## ตัวกรองทั้งเว็บใช้ชิปตัวเดียวกันแล้ว (เบสสั่งตรวจให้ครบทุกหน้า 2026-08-31)

ตรวจ **9 หน้าที่มีกล่องตัวกรอง** — กล่อง (`FilterModal`) เป็นตัวเดียวกันหมดอยู่แล้ว
แต่ **ชิปข้างในเขียนกัน 5 แบบ** → ยุบเหลือ `FilterFacetGroup` ตัวเดียว

| หน้า | เดิม | ตอนนี้ |
|---|---|---|
| หน้าแรก · ค้นหา | ตัวกลางอยู่แล้ว | คงเดิม |
| ตลาด | `FacetChips` local ในไฟล์ | ยุบแล้ว |
| หน้าชุด | `facetChip` เขียนเอง | ยุบแล้ว |
| เครื่องคำนวณดรอป | ปุ่มเขียนมือ | ยุบแล้ว |
| เพิ่มการ์ดเข้าพอร์ต | ปุ่มเขียนมือ 4 หมวด | ยุบแล้ว |
| หน้าการ์ด (สภาพ) | `SegmentedControl` pill | **ไม่ยุบโดยตั้งใจ** — ต้องเลื่อนแนวนอนได้ มีเทสล็อกเจตนา |
| รายการโปรด | ชุดในกล่อง + ค้นหาในแถบ | ตรงตามที่เบสสั่งอยู่แล้ว · แก้ปุ่มตัวกรองให้สะกดคำ |

**ยุบแบบยกของดีขึ้นมาเป็นมาตรฐาน ไม่ใช่ยุบแล้วถอยหลัง:**
`selectionMode="single"` ใช้ `<input type="radio">` จริง (ยกมาจากตลาด) —
หน้าชุดกับเครื่องคำนวณดรอปเลยได้ a11y ดีขึ้นฟรี · เพิ่ม `hint` (บรรทัดกติกาใต้หัวข้อ)
· หัวข้อ/คำอธิบายผูก aria ในตัว ผู้เรียกไม่ต้องคิดเรื่อง id

**เจอระหว่างตรวจ ยังไม่แตะ (คนละเรื่อง — เป็นการเรียง ไม่ใช่ตัวกรอง):**
หน้ารายการโปรดบนมือถือมี **5 แถวควบคุม** (เกม · ค้นหา+ตัวกรอง · เกรด · ช่วงเวลา · เรียง)
ขณะที่หน้าแรกมี 2 แถว — ถ้าเอา `MobileSortCluster` ของหน้าแรกไปใช้ จะยุบ 2 แถวท้าย
เหลือแถวเดียวได้ · รอเบสสั่ง

## ค้างตรงไหน

- **ข้อแลกที่ยังเหลือของแบบ ก**: ไอคอนไม่มีคำอธิบาย คนที่ไม่เคยติดตั้งเว็บเป็นแอป
  อาจไม่กด — ถ้าเบสอยากได้คนติดตั้งเยอะกว่านี้ ค่อยเสริมแบบ ข หรือ ค (ชิ้นส่วนพร้อมแล้ว)
- **ต้องลองบนมือถือจริงหลัง deploy** — บน localhost แอนดรอยด์ไม่ยิง event ติดตั้ง
  (worker ไม่ register บน localhost โดยตั้งใจ) จึงเห็นปุ่มจริงได้เฉพาะบน preview/prod
- **รอเบสตรวจรายชื่อ 56 ใบ** ที่ `/proto/manga-badge` (ค้างจากรอบก่อน)
- **⚠️ คีย์ Gemini ใน `.env` ใช้ไม่ได้** → "ค้นหาด้วยรูป" ยังพังอยู่ (ค้างจากรอบก่อน)

## NEXT

1. รอ Vercel deploy master เสร็จ แล้วลองกด "เพิ่มไปหน้าจอโฮม" บนมือถือจริง
   (แอนดรอยด์ควรเด้งแผ่นติดตั้ง · iPhone เปิด Safari แล้วกดปุ่มแชร์)
   — ปุ่มโผล่เฉพาะบนเว็บจริง localhost ไม่ขึ้นเพราะปิด service worker ไว้ตั้งใจ
2. เบสตรวจรายชื่อการ์ดมังงะที่ `/proto/manga-badge`
3. เปลี่ยนคีย์ Gemini แล้วลองค้นหาด้วยรูปอีกครั้ง
