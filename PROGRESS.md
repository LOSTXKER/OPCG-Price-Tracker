# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบสิบสาม · จัด D2 ใหม่)

**⚠️ ดูที่ `http://localhost:3008/proto/navbar-ecom`** (3000 ค้าง route · 3007 crash จากบั๊ก Turbopack)

**เบสสั่ง:** D2 หดช่องค้นหาหน่อย · เลือกเกม/เลือกชุดไปอยู่ด้านซ้ายสุด · ชีพจรต่อท้าย

### D2 ผังล่าสุด (สูง 104px)
```
[48] OPCG▾ › OP15▾ ‖ สถิติ · สายพาน(กินที่ที่เหลือ) ‖ อัปเกรด · แชท · กระดิ่ง · โปรไฟล์
[56] Meecard · เมนู 4 ลิงก์ ⟷ พอร์ต · รายการโปรด · Honey · [ค้นหา 384px]
```

**ผลพลอยได้จากการจัดใหม่:** พอเกม › ชุด ไปซ้ายสุดแล้วให้สายพานกินที่ที่เหลือ **สายพานได้เพิ่มจาก 309px → 337px**

### ตัวเลขล่าสุด (วัดจริงในเบราว์เซอร์)
| | D2 | D (ชีพจรแยกแถว) | ปัจจุบัน |
| --- | --- | --- | --- |
| สูงรวม | **104px** | 136px | 132px |
| ที่วิ่งสายพาน | 337px ⚠️ | 781px | 781px |
| ช่องค้นหา | 384px | 480px | 320px |

**ยังต้องซ่อนสถิติ 2 ตัว** (จำนวนชุด · อัปเดตล่าสุด) ใน D2 · สายพานยังแคบกว่าของจริง 57% — เป็นราคาที่แลกกับความเตี้ย 28px **ต้องให้เบสตัดสิน**

**ตรวจรับแล้ว:** eslint 0 · tsc ผ่าน · แถวบนเริ่มด้วยปุ่มเลือกเกมจริง (ตรวจด้วยสคริปต์) · ทุกปุ่มควบคุม ≥40px · ไม่มีแถวไหนล้น/โดนตัดที่ 1360px · ตัวเลขในหน้า proto ตรงกับที่วัดจริงทุกตัว

### ประวัติที่เบสเคาะมาแล้ว (อย่าเสนอซ้ำ)
- ❌ ทาสีแบรนด์ · ❌ แถวคำค้นยอดนิยม · ❌ ชั้นวางชุดเรียงยาว · ❌ ซ่อนชื่อของฉัน · ❌ ปุ่ม <40px · ❌ ค้นหายืดเต็มแถว
- ✅ ชุดเป็น dropdown มีรูปกล่อง · ✅ ค้นหาชิดขวา · ✅ ของฉันซ้ายของช่องค้นหา · ✅ โลโก้นำแถวล่าง · ✅ เกม › ชุด ซ้ายสุดแถวบน ชีพจรต่อท้าย

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3008/proto/navbar-ecom`** ดู D2 ผังใหม่ — ถ้าช่องค้นหายังไม่พอดี บอกได้ว่าอยากได้กว้างเท่าไหร่
2. เคาะแล้วลงของจริง: `header.tsx` + `header-market-ticker.tsx` in-place · `--chrome-h` 8.25rem → **6.5rem** (D2) · `HeaderCatalogControl` ต้องรับ `boxImageUrl` · ต้องซ่อน `StripFigure` สองตัว
3. มือถือยังไม่ได้ออกแบบใหม่ — ทำต่อหลังเคาะ desktop
