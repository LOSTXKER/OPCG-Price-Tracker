# สถานะสด — meecard

> เขียนทับทุกครั้งที่จบงาน · อ่านไฟล์นี้ก่อนเริ่ม session ใหม่

**อัปเดต:** 2026-08-30 (ดึก)
**branch:** `proto/row-favorite` (งานรอบนี้อยู่บน branch นี้ทั้งหมด · ยังไม่ merge เข้า master)

## ทำอะไรไปรอบนี้

### 1. ปุ่มรายการโปรดในแถวรายการการ์ดบนมือถือ — ลงเว็บจริงแล้ว ✅

เบสเคาะแบบ **A (ขวาสุดของแถว)** จากหน้าลอง `/proto/row-favorite` (4 ทาง: ปัจจุบัน/A/B/C)

- ปุ่มหัวใจอยู่นอกลิงก์ กดแล้วไม่เปิดหน้าการ์ด · กล่องกด 32×44px ห่างขอบจอ 12px
- ต้องบีบปุ่มจาก 36→32px และดึงชิดขอบ เพื่อคืนที่ให้บรรทัด "รหัส + ป้ายความหายาก"
  ไม่ให้ตัดเป็นสองบรรทัด (วัดจริง: บรรทัดนั้นต้องการ 111px ตอนนี้ได้ 116px)
- โครงตอนโหลดก็เว้นที่ปุ่มไว้ด้วย แถวจะได้ไม่กระตุก
- ได้ทั้งหน้าแรกและหน้าค้นหาพร้อมกัน เพราะใช้แถวเดียวกัน
- ตรวจแล้ว: lint + typecheck ผ่าน · เทส 924 ข้อผ่าน · เปิดดูจริงบนมือถือทั้งโหมดมืด/สว่าง

### 2. ป้ายมังงะ · มังงะแดง — รอเบสตรวจรายชื่อ + เคาะตำแหน่งป้าย

**เจอปัญหาก่อน:** ฐานข้อมูลไม่มีอะไรบอกได้เลยว่าใบไหนเป็นการ์ดลายมังงะ —
ป้ายที่ขูดมาจากร้านญี่ปุ่นมีแค่ SP/SEC/P-SEC และชื่อการ์ดญี่ปุ่นไม่มีคำว่า 漫画/コミック สักใบ

**เบสเคาะ:** ให้ AI เดาจากรูปก่อนแล้วเบสตรวจ · ป้ายมังงะเป็น**ป้ายที่สอง**ข้างป้ายเดิม (ไม่แทนที่)

**ทำแล้ว:** ไล่ดูรูปการ์ด SP + SEC + P-SEC ครบ **233 ใบ** ด้วยตา (Gemini ใช้ไม่ได้ ดูข้อ 3)
แยกได้ 3 กลุ่ม เก็บเป็นทะเบียนไฟล์เดียว `src/lib/constants/card-art-style.ts`:

| กลุ่ม | จำนวน | คือ |
|---|---|---|
| มังงะ | 35 ใบ | พื้นหลังเป็นแผงการ์ตูนขาวดำจากมังงะต้นฉบับ |
| มังงะแดง | 5 ใบ | แผงมังงะแบบเดียวกัน แต่พิมพ์ด้วยหมึกแดงทั้งใบ |
| ใบประกาศจับ | 16 ใบ | ลาย WANTED · DEAD OR ALIVE (เจอเพิ่มระหว่างทาง — ตัดทิ้งได้ถ้าไม่เอา) |

หน้าลอง `/proto/manga-badge` มีสองเรื่องให้เคาะ: (ก) ป้ายวางตรงไหนของแถวมือถือ — **6 ทาง**
(ก ต่อท้ายป้ายเดิม · ข บนรูปการ์ด · ค ต่อท้ายชื่อ · ง รวมเป็นป้ายเดียว · จ ไอคอนแทนคำ ·
ฉ แถบสีขอบแถว) พร้อมตารางเทียบว่าแบบไหนทำให้รหัสการ์ดในแถวหดแค่ไหน
และ (ข) กริดรูปการ์ดทั้ง 56 ใบให้ตรวจว่าจัดถูกไหม

เกณฑ์ตัดสินที่เห็นจากของจริง: แบบ ก กับ ง ทำให้รหัสการ์ดเหลือ "OP0…" (อ่านไม่ออกว่าใบไหน) ·
แบบ จ หดนิดเดียว · แบบ ข/ค/ฉ รหัสอยู่ครบ แต่แลกด้วยความชัดเจนของป้ายแทน

**ยังไม่แตะฐานข้อมูล** — รายชื่ออยู่ในโค้ดไฟล์เดียว แก้เพิ่ม/ลบได้ทันที

### 3. ⚠️ คีย์ Gemini ใน `.env` ใช้ไม่ได้แล้ว

รันสคริปต์ให้ AI ดูรูปแล้วได้ `API key not valid` ทุกใบ — แปลว่า **ฟีเจอร์ "ค้นหาด้วยรูป"
ที่เพิ่งขึ้นเว็บเมื่อคืนน่าจะใช้ไม่ได้จริงบนเครื่องนี้** (ผู้ใช้จะเจอ "วิเคราะห์ไม่สำเร็จ")
ต้องเบสไปเอาคีย์ใหม่จาก Google AI Studio แล้วใส่ทั้งใน `.env` และใน Vercel
(ตรวจฝั่ง Vercel ให้ไม่ได้ เพราะโฟลเดอร์นี้ยังไม่ได้ลิงก์กับโปรเจกต์บน Vercel)

สคริปต์ที่เขียนไว้ใช้ได้ทันทีเมื่อมีคีย์ใหม่: `scripts/classify-manga-art.ts`

## ค้างตรงไหน

- **รอเบสตรวจ** ที่ `/proto/manga-badge` — (ก) เอาป้ายแบบไหนใน 6 แบบ (ข) รายชื่อ 56 ใบถูกไหม
  (ค) เอาป้าย "ใบประกาศจับ" ด้วยไหม
- เคาะแล้วค่อยเอาป้ายลงของจริง (แถวมือถือ · ตารางคอม · หน้าการ์ด) โดยอ่านจาก
  `cardArtStyle()` ใน `src/lib/constants/card-art-style.ts`
- คีย์ Gemini ใหม่ (ข้อ 3)

## NEXT

1. เบสเปิด `/proto/manga-badge` → กดดู 6 แบบ + ตารางเทียบ แล้วเลื่อนลงดูกริดรูป 56 ใบว่าจัดถูกไหม
2. เปลี่ยนคีย์ Gemini แล้วลองฟีเจอร์ค้นหาด้วยรูปอีกครั้ง
3. หลังเคาะป้าย: ลงของจริง + อัปเดตสถานะใน `/proto`
