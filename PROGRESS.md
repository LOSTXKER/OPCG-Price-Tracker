# สถานะสด — meecard

> เขียนทับทุกครั้งที่จบงาน · อ่านไฟล์นี้ก่อนเริ่ม session ใหม่

**อัปเดต:** 2026-08-29 (คืนนี้)
**branch:** `master` (local) · push ไว้ที่ `proto/photo-desktop`

## ทำอะไรไปคืนนี้ (ขึ้นเว็บจริงหมดแล้ว ยกเว้นข้อ 6)

1. **แถบเมนูโฉมใหม่ (D2)** — สองแถว 104px (เดิม 132px) · แถว 1 = เลือกเกม›ชุด + สถิติ + ชีพจรตลาด + บัญชี · แถว 2 = โลโก้ + เมนู + ของฉัน + ช่องค้นหา · มือถือ แถวเลือกชุดไม่ตามลงมาเวลาเลื่อน
2. **หัวช่องค้นหา** — ปุ่ม "ยกเลิก" แบบ iOS แทนป้าย ESC · มือถือเปิดเต็มจอ · ตัดทางลัดไปหน้าต่างๆ ออกตอนยังไม่พิมพ์
3. **ค้นหาด้วยรูป** — แบนเนอร์ยาวเต็มความกว้าง + ป้าย AI · มาแรงยาวขึ้นเป็น 12 ใบ
4. **จอสแกนการ์ดบนมือถือ** — กล้องเต็มจอแบบแอปธนาคาร (อัปโหลดซ้ายล่าง · ชัตเตอร์กลาง · กรอบสัดส่วนการ์ด · ลายกริดเทคตอนสแกน)
5. **ค้นหาด้วยรูปบนคอม → เป็นการอัปโหลด ไม่ใช่กล้อง** (เบสเคาะแบบ ก) — จอกว้าง ≥768px เปิดเป็นกล่องกลางจอ กว้าง 576px: ลากรูปมาวางได้ · กด ⌘/Ctrl+V วางรูปที่ก๊อปมาได้ · กดเลือกไฟล์ได้ · ตอนอ่านรูปมีภาพที่วางโชว์พร้อมเส้นสแกน · เจอแล้วโชว์การ์ด+ราคาเหมือนเดิม · **มือถือไม่แตะ ยังเป็นกล้องเต็มจอ**
   - ผลข้างเคียงที่แก้ไปด้วย: error 500 เคยโชว์ `Internal server error (POST /api/cards/identify)` ให้ผู้ใช้เห็น → เปลี่ยนเป็น "วิเคราะห์ไม่สำเร็จ ลองอีกครั้งได้เลย" (4xx ยังโชว์ข้อความจริงเพราะเขียนมาให้คนอ่าน)
6. **ปุ่มค้นหาที่แถบล่าง** — เบสเคาะว่าเอาแบบปัจจุบัน (ปุ่มทองกลมเรียบ ไม่มีวงแหวน) เพราะทองซ้อนทองจมกันเอง

## ค้างตรงไหน

- **`/proto/ai-look` ยังรอเบสเคาะ** ว่าช่องค้นหาจะเอาภาษาภาพแบบไหน — A เรืองแสง / C ขอบหมุน / D ขอบไล่สีไหลวนสามชั้น · เคาะแล้วลงของจริงที่ `src/components/shared/command-search.tsx`
- `master` local นำหน้า remote — งานทั้งหมดอยู่บน branch `proto/photo-desktop` แล้ว ยังไม่ push เข้า master ตรงๆ ตามกฎ

## NEXT

1. เบสเปิด `/proto/ai-look` เคาะ A / C / D ให้จบ แล้วลงของจริง
2. อยากดูของข้อ 5: เปิดเว็บบนคอม → หน้าค้นหา → กดปุ่มกล้องขวาช่องค้นหา → ลากรูปการ์ดมาวาง หรือก๊อปรูปแล้วกด ⌘V
