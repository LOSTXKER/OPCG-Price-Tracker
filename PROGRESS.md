# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-28 (บ่าย ×2 รอบ) — **แถบ "ชุดการ์ดวันพีชล่าสุด" เป็น ticker แถวเดียววิ่งไหลเอง เหนือตารางราคา** (feedback เบสรอบสอง: สวยกว่านี้ได้ + ไม่กินความสูง + เลื่อนไหลเอง)

## รอบนี้ทำอะไร (session นี้ — 2 รอบต่อเนื่อง)

**รอบ 1** (commit `c68df1d`): เอาหัวข้อ "ตารางราคาการ์ด" ออกทั้งยวง (ลบ `home-market-intro.tsx` + `buildHomeMarketHeading`) แล้วย้ายแถบชุดการ์ดจากใต้ตารางขึ้นมาเป็นหัว section ตลาดแทน (กลับกติกา 08-26)

**รอบ 2** (เบสดูแล้วสั่งต่อ: "สวยกว่านี้ได้ ไม่อยากให้กินความสูง แถวเดียว เลื่อนไหลเอง"):
- **ทั้ง section เหลือแถวเดียวจริงๆ (~54px จากเดิม ~190px)** — หัวข้อ h2 "ชุดการ์ดวันพีชล่าสุด ›" + ประโยคขยาย ย่อเป็นบล็อกเล็กชิดซ้าย (ทั้งบล็อกเป็นลิงก์ไป /opcg/sets) · มือถือ stack เป็น 2 แถวกะทัดรัด
- **ticker วิ่งไหลเองแบบ CMC** — CSS marquee (`.set-marquee` ใน globals.css): track มี pill list 2 ชุด เลื่อน -50% loop 60 วิ (~38px/วิ) ไร้รอยต่อ · **ชี้เมาส์/โฟกัสคีย์บอร์ดแล้วหยุด**ให้กดได้ · fade ทั้งขอบซ้าย-ขวา
- **a11y/SEO ไม่เสีย**: ชุดสำเนาเป็น `aria-hidden` + `tabIndex -1` (crawler เห็นลิงก์ละชุดเดียว) · `prefers-reduced-motion` = หยุดวิ่ง เป็นรางเลื่อนมือธรรมดา + ซ่อนสำเนา · pill "ดูชุดทั้งหมด ›" (เส้นประ) วิ่งปิดท้าย loop

**Verify**: tsc 0 · ESLint 0 error · vitest 151 ไฟล์/906 ผ่าน · build 211 หน้า · เปิดดูจริง dev :3000 — desktop section สูง 54px, animation running 60s, ลิงก์ 12+12(clone aria-hidden) · มือถือ 375px วิ่งไหลปาดขอบจอ รูปกล่องครบ · console ไม่มี error

## ⚠️ งานคู่ขนานใน tree (ไม่ใช่ของ session นี้ — อย่าเหมารวม)

ตอน commit มีไฟล์แก้ค้างของอีก session (กวาดคำ "อัปเดตทุกวัน" ~35 จุด: blog/guide/most-expensive/layout/og-image + lib/seo/copy หลายไฟล์) — session นี้ **commit เฉพาะไฟล์ของงานตัวเอง** ของเขายังค้างใน working tree ให้เขาปิดเอง

## สถานะ git

- branch `chore/next-16.3` — งานรอบนี้ commit แยกก้อน push แล้ว (ต่อคิวจาก `30a8b19` ของรอบดึก)
- ไม่มี schema / migration / dependency / config change

## NEXT

1. เบสรีเฟรชหน้าแรกดู: แถบ "ชุดการ์ดวันพีชล่าสุด" แถวเดียวเหนือตาราง (desktop + มือถือ)
2. พอใจแล้วสั่งเปิด PR `chore/next-16.3` → master ได้ (แบบ #119/#120)
3. คิวงานค้างเดิม: กวาด "อัปเดตทุกวัน" ~35 จุดทั้งเว็บ (กำลังวิ่งในอีก session) · `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive (ย้ายมาใช้ pattern map 3 locale ของหน้าแรก)
