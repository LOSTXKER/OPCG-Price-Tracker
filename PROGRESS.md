# สถานะสด — meecard

> เขียนทับทุกครั้งที่จบงาน · อ่านไฟล์นี้ก่อนเริ่ม session ใหม่

**อัปเดต:** 2026-08-30 (ดึกมาก)
**branch:** `feat/pwa-install` (แยกจาก master · รอเบสดูแล้ว merge เอง)

## ทำอะไรไปรอบนี้ — เว็บติดตั้งลงหน้าจอโฮมได้แล้ว (PWA)

เบสสั่ง: "navbar มุมมือถือ อยากให้ข้างบนมีกดเพื่อเพิ่มไปยังหน้าโฮมด้วย"
ของเดิมยังไม่มีพื้นฐาน PWA เลย จึงทำทั้งก้อน: ตัวเว็บติดตั้งได้จริง + ปุ่มกด

### 1. พื้นฐานทำให้ติดตั้งได้จริง ✅ (ไม่มีทางเลือกให้เคาะ ทำเลย)

| ของ | ไฟล์ | ได้อะไร |
|---|---|---|
| บัตรประจำตัวแอป | `src/app/manifest.ts` | แอนดรอยด์รู้จักเว็บเป็นแอป · ชื่อ "Meecard" · เปิดเต็มจอไม่มีแถบที่อยู่เว็บ · ทางลัดกดค้างที่ไอคอน 3 อัน (รายการโปรด · พอร์ต · ค้นหา) |
| ไอคอนหมี | `public/icons/*.png` | 192 · 512 · maskable 512 · apple 180 — หมี Kuma บนพื้นสีแบรนด์ (ทำจาก `public/meecard.png` ด้วย `sips`) |
| ไอคอน iPhone | `src/app/apple-icon.tsx` | เดิมเป็นตัวอักษร "M" → เปลี่ยนเป็นหมี · อ่านไฟล์ตอน build แล้วส่งไบต์ตรงๆ (ไม่ผ่าน next/og เพราะรูป 567KB ทำ satori ล้ม) |
| สีแถบสถานะ | `src/app/layout.tsx` (`viewport`) | ขาว/ดำตามธีม ตรงกับ `--background` · `viewportFit: cover` เผื่อรอยบาก |
| หน้าจอตอนเน็ตหลุด | `public/offline.html` + `public/sw.js` | เห็นหมี + ปุ่ม "ลองใหม่" แทนไดโนเสาร์ของเบราว์เซอร์ |

**service worker ตั้งใจให้เกือบว่างเปล่า** — ไม่ cache หน้าเว็บ/ราคาเลย
(เว็บนี้ขายความสด · worker ที่ cache ผิดจะโชว์ราคาเก่าโดยไม่มีใครรู้ และปลดยาก)
มีไว้แค่ 2 อย่าง: ทำให้แอนดรอยด์ยอมเสนอติดตั้ง + หน้าจอตอนเน็ตหลุด ·
**ไม่ register บน localhost** (worker ค้างบน :3000 จะไปกวน project อื่น)

### 2. สมองกลางของฟีเจอร์ ✅

`src/hooks/use-install-prompt.ts` — ที่เดียวที่ตอบว่า "ตอนนี้ชวนติดตั้งได้ไหม"

- แอนดรอยด์ = กดปุ่มเดียวเด้งแผ่นติดตั้งจริง · iPhone = ไม่มี API เลย ต้องสอนวิธี
- เก็บ event ไว้ที่ระดับโมดูล + สคริปต์เล็กๆ ใน `<head>` — เพราะ Chrome ยิง
  `beforeinstallprompt` **ครั้งเดียว** และมักยิงก่อน React จะ hydrate เสร็จ
  (ถ้าดักใน component จะพลาด แล้วปุ่มจะไม่โผล่เลย — บั๊กที่หาไม่เจอทีหลัง)
- ปิดแล้วเงียบ 30 วัน · ติดตั้งแล้วไม่ชวนอีก · Chrome/Firefox บน iOS ถูกคัดออก
  (มันเพิ่มหน้าจอโฮมไม่ได้ สอนไปก็ทำตามไม่ได้)
- เทส 10 ข้อคุม UA จริง + วันหมดอายุ (`use-install-prompt.test.ts`)

### 3. ปุ่มที่เบสขอ ✅ ลงของจริงแล้ว

- **แถบบนมือถือ** (`InstallHeaderButton` ใน `header-mobile.tsx`) — ไอคอนมือถือ+
  ข้างซ้ายของหัวใจ · **โผล่เฉพาะคนที่ติดตั้งได้และยังไม่ติดตั้ง** คนอื่นแถวกว้างเท่าเดิม
  · วัดที่ 360px แล้ว: ปุ่มทุกตัวยัง 44px ครบ ชื่อหน้าเหลือ 111px (จาก 155px) ไม่ตัด
- **"ดูเพิ่มเติม"** (`InstallAppRow` ใน `/more`) — ทางกลับถาวร เผื่อกดปิดไปแล้วเปลี่ยนใจ
- **กล่องสอนวิธี iPhone** (`InstallGuideDialog`) — 3 ขั้น + ไอคอนแชร์ · ใช้ร่วมทั้งสองที่
- คำแปลครบ 3 ภาษา (`installApp*` ใน th/en/jp)

### 4. หน้าลองให้เบสเทียบ 🟡 รอเคาะ

`/proto/pwa-install` — 4 ทาง วางบนกรอบมือถือ 375px จริง พร้อมสลับ iPhone/Android:
**ปัจจุบัน · ก ปุ่มในแถวบน (ที่ลงจริงไปแล้ว) · ข แถบเชิญชวนเหนือแถบบน · ค แผ่นเด้งจากล่าง**
ทุกทางเขียนข้อแลกไว้ · กล่องสอนวิธี iPhone ในหน้านี้เป็นของจริง ไม่ใช่ของจำลอง

ตรวจครบ: lint (ไฟล์ใหม่สะอาด) · typecheck · เทส 934 ข้อ · `npm run build` ผ่าน ·
เปิดดูจริงที่ 360px และ 375px ทั้งสองธีม · กดปุ่มแล้ว flow ครบ (กดปิด → หาย → `/more` ยังมี)

## ค้างตรงไหน

- **รอเบสเคาะที่ `/proto/pwa-install`** ว่าจะเอาแค่ปุ่มในแถวบน (ของตอนนี้)
  หรือเพิ่มแถบ/แผ่นเชิญชวนด้วย — เปลี่ยนได้ใน 10 นาที ชิ้นส่วนพร้อมหมดแล้ว
- **ต้องลองบนมือถือจริงหลัง deploy** — บน localhost แอนดรอยด์ไม่ยิง event ติดตั้ง
  (worker ไม่ register บน localhost โดยตั้งใจ) จึงเห็นปุ่มจริงได้เฉพาะบน preview/prod
- **รอเบสตรวจรายชื่อ 56 ใบ** ที่ `/proto/manga-badge` (ค้างจากรอบก่อน)
- **⚠️ คีย์ Gemini ใน `.env` ใช้ไม่ได้** → "ค้นหาด้วยรูป" ยังพังอยู่ (ค้างจากรอบก่อน)

## NEXT

1. เบสเปิด `/proto/pwa-install` แล้วบอกว่าเอาแค่ปุ่ม หรือเพิ่มแถบ/แผ่นด้วย
2. deploy branch `feat/pwa-install` แล้วลองกด "เพิ่มไปหน้าจอโฮม" บนมือถือจริง
   (แอนดรอยด์ควรเด้งแผ่นติดตั้ง · iPhone เปิด Safari แล้วกดปุ่มแชร์)
3. เบสตรวจรายชื่อการ์ดมังงะที่ `/proto/manga-badge`
4. เปลี่ยนคีย์ Gemini แล้วลองค้นหาด้วยรูปอีกครั้ง
