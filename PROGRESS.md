# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบสี่ · เบสเคาะแบบ 2 แล้ว)

**⚠️ ดูที่ `http://localhost:3007/proto/navbar-ecom` ไม่ใช่ 3000** — dev server ที่ 3000 (ของอีก session, PID 94622) **ค้าง route manifest** เสิร์ฟหน้านี้เป็น 404 ทั้งที่ไฟล์อยู่ครบและ compile ผ่าน (touch/สร้างไฟล์ใหม่ก็ไม่ปลุก) เลยเปิดตัวใหม่ที่ 3007 ให้แทน · ถ้าอยากใช้ 3000 ต้องปิด-เปิดใหม่ (ไม่ได้ไปฆ่าให้ เพราะเป็นของ session อื่น)

### เบสเคาะแล้ว: เอา "แบบ 2 · แถบชุดการ์ด" แต่ติด 2 จุด
1. **ปุ่มเลือกเกมหาย** — รอบก่อนแตก "เกม › ชุด" ออกแล้วเก็บแค่ชุด ทั้งที่เว็บรองรับหลายเกม
2. **แถว 3 ตายตัวเกินไป** — อนาคตจะมีปุ่มเพิ่ม แต่เมนูเว็บกับชุดแย่งที่กันอยู่แถวเดียว แถมโชว์ได้แค่ 8 จาก 51 ชุด

### แก้ยังไง
- **ปุ่มเลือกเกมกลับมาทุกแบบ** และยืน**นำหน้าชุดเสมอ** เพราะเกมเป็นตัวกำหนดว่าชุดไหนโผล่ในแถบ (อ่านเป็นประโยคเดียว "OPCG › ชุดพวกนี้")
- **ชั้นวางชุดเลื่อนซ้าย-ขวาได้จริง** — `scrollLeft` + ปุ่มลูกศร + honor `prefers-reduced-motion` และ**ไม่เลื่อนเอง** ตาม pattern เดียวกับ `home-set-strip.tsx` ที่เบสเคาะไว้ · ใส่ 14 ชุดจริงในตัวอย่างให้เห็นว่ารับของเพิ่มได้ ปิดท้ายด้วยปุ่ม "ทุกชุด (51)"
- **หน้า proto เปลี่ยนโหมด**: จากเทียบ 4 แบบ → ล็อกโครงแบบ 2 ไว้ เหลือเลือกแค่ว่า**แถวที่ 3 จัดยังไง** (ธรรมเนียมเดียวกับตอนเคาะแบบ C แล้วเลือกตำแหน่งช่องค้นหา)

### 3 ตัวเลือกของแถวที่ 3 (ทุกแบบสูง 136px)
| แบบ | แถว 3 | ตอบโจทย์ "ปุ่มจะเพิ่มในอนาคต" ยังไง |
| --- | --- | --- |
| **A · เมนูนำ แคตตาล็อกตาม** ← ค่าเริ่มต้น | เมนู 4 ลิงก์ ‖ เกม▾ › ชุดเลื่อนได้ | ใกล้แบบ 2 เดิมสุด · เมนูเพิ่มได้ แคตตาล็อกหดให้แล้วเลื่อนเอา |
| B · แถวแคตตาล็อกล้วน | เกม▾ › ชุดเลื่อนได้ ‖ พอร์ต·โปรด·Honey (เมนูขึ้นแถวกลาง) | แยกขาด — เมนูเพิ่มกี่ปุ่มก็ไม่แตะพื้นที่ชุดเลย |
| C · แท็บเกม | [OPCG][Pokémon] ‖ ชุดเลื่อนได้ ‖ ของฉัน | เหมือน B แต่เห็นทุกเกมพร้อมกัน เตรียมเปิดเกมที่สอง |

**ความสูง 136px** (32+56+48) มากกว่าของจริง 4px เพราะแถว 3 ต้องมีที่พอให้ปุ่มขนาดกดได้ — ถ้าลงจริงต้องแก้ `--chrome-h` เป็น 8.5rem

**ตรวจรับแล้ว (วัดด้วยสคริปต์ในเบราว์เซอร์):** eslint 0 · tsc ผ่าน · ทุกปุ่ม ≥40px ทุกแบบ · ชั้นวางชุดเลื่อนได้จริงทั้ง 3 แบบ · ปุ่มเลือกเกมอยู่ครบ · ชื่อพอร์ต/รายการโปรด/Honey ครบ 3/3 · ไม่มีอะไรล้นที่ 1360px

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3007/proto/navbar-ecom`** (ไม่ใช่ 3000 — ดูหมายเหตุด้านบน) แล้วเคาะว่าเอา A, B หรือ C — ลองกดลูกศรเลื่อนชุดดูด้วย
2. เคาะแล้วลงของจริง: `header.tsx` + `header-market-ticker.tsx` in-place · `--chrome-h` เป็น 8.5rem · แถบชุดใช้ pattern เดียวกับ `home-set-strip.tsx` (อาจแยก component ใช้ร่วมกันได้)
3. ถ้าเลือก B/C ต้องคิดต่อว่าแถบชุดบน navbar กับแถบชุดหน้าแรกซ้ำกันไหม (เคยเป็นประเด็น ticker สองอันมาแล้ว)
4. มือถือยังไม่ได้ออกแบบใหม่ — ต้องทำต่อหลังเคาะ desktop
