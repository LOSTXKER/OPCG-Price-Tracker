# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## ✅ งานรอบนี้ — navbar D2 ลงของจริง + สลับแถวตามภาพที่เบสส่ง

**เบสสั่งรอบล่าสุด:** เอาแชท · แจ้งเตือน · โปรไฟล์ ไปต่อ Honey แถวล่าง · ช่องค้นหาขึ้นไปแถวบนหน้าอัปเกรด

### โครงที่ขึ้นแล้ว — 132px → **104px**
```
[48] เกม▾ › ชุด▾(รูปกล่อง) ‖ สถิติ · สายพาน ‖ ค้นหา ‖ อัปเกรด
[56] โลโก้ · เมนู 4 ลิงก์ ⟷ พอร์ต · รายการโปรด · Honey · แชท · แจ้งเตือน · โปรไฟล์
```
แถวบน = "กำลังดูอะไร + หาอะไร" · แถวล่าง = "ของฉันทั้งหมด"

### ⚠️ ปัญหาที่เจอตอนย้ายค้นหาขึ้นแถวบน (แก้แล้ว)
ค้นหา 288px ไปแย่งที่สายพาน — **วัดได้ 44px ที่จอ 1280 และ 40px ที่ 1024** (สายพานตายไปเลย)

**แก้ด้วย responsive 3 ชั้น** ให้ของหลบตามความกว้าง:
| จอ | สถิติที่เห็น | ปุ่มชุด | ค้นหา | **สายพานได้** |
| --- | --- | --- | --- | --- |
| 1024 | — | รหัสอย่างเดียว (w-40) | 256px | **260px** |
| 1280 | มูลค่ารวม | รหัส + ชื่อชุด (w-56) | 256px | **296px** |
| 1600 | ครบ 3 ตัว | รหัส + ชื่อชุด | 288px | **364px** |

- `การ์ดทั้งหมด` · `JPY/THB` → ซ่อนใต้ **2xl** (1536)
- `มูลค่ารวม` → ซ่อนใต้ **xl** (1280) · เป็นลิงก์เดียวไป market-overview จึงอยู่ได้นานสุด
- ชื่อชุดในปุ่ม → ซ่อนใต้ **xl** เหลือรหัส · ปุ่มแคบลง w-56 → w-40

### ไฟล์ที่แก้ (7 ไฟล์ + เทสต์ 2)
`header-market-ticker.tsx` (ยุบ 2 แถวเป็น 1 · รับ `searchSlot` แทน `children` · figures responsive) · `header.tsx` (โลโก้ลงแถวล่าง · account ลงแถวล่าง · ค้นหาขึ้นแถวบน) · `globals.css` (`--chrome-h` → 6.5rem) · `set-picker.tsx` (+`boxImageUrl`) · `header-catalog-control.tsx` (รูปกล่อง + responsive) · `header-mobile.tsx` (ซ่อนปุ่มชุดตอน scroll)

### ตรวจรับ
- `npx eslint src` **0 errors** · tsc ผ่าน · `npm run test` **918 ข้อผ่าน** · `npm run build` ผ่าน
- **วัดจริงบน production server ที่ 1024 / 1280 / 1600** — ไม่มีแถวไหนล้น · สายพานอยู่ได้ทุกความกว้าง · chrome 105px
- มือถือ 375: เลื่อนลง → ปุ่มเลือกเกม/ชุดหาย · เลื่อนขึ้น → กลับมา
- เทสต์ topology 3 ข้ออัปเดตให้ล็อกโครงใหม่ (สลับแถว search/account) **ไม่ได้ลบเทสต์**

### ⚠️ dev server cache
`npm run dev` เสิร์ฟ CSS เก่า (Turbopack cache ค้างที่ `.next/dev/`) — ถ้าความสูงยังเป็นแบบเดิม ให้ `rm -rf .next/dev` ก่อนเปิด

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิดดูของจริง** — production server อยู่ที่ `http://localhost:3014`
2. พอใจแล้วเปิด PR `feat/navbar-ecom-proto` → master
3. มือถือยังไม่ได้ออกแบบใหม่ (แตะแค่ให้ปุ่มชุดหายตอนเลื่อน)
4. ของค้างเดิม: `priceValidUntil` ใน `json-ld.ts` · `getHomeData().rarityRows` query ทิ้งเปล่า
