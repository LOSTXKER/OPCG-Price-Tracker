# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบสิบสี่ · แก้ช่องค้นหาของ D2)

**⚠️ ดูที่ `http://localhost:3008/proto/navbar-ecom`** (3000 ค้าง route · 3007 crash จากบั๊ก Turbopack)

**เบสบอก:** "ชอบแบบ D2 แต่ติดที่ช่องค้นหา" → **โครง D2 ล็อกแล้ว** เหลือแก้ช่องค้นหาอย่างเดียว

### ต้นเหตุที่เจอ
ปุ่ม "ค้นหา" สีเต็มกิน ~100px + kbd อีก 24px จากช่องกว้าง 384px → **เหลือที่พิมพ์จริงราว 250px ข้อความตัวอย่างเลยโดนตัด**

### 4 ทางแก้ (วัดจริงแล้ว ไม่มีแบบไหนข้อความโดนตัดอีก)
| แบบ | วิธี | ช่องกว้าง | ที่ข้อความได้ |
| --- | --- | --- | --- |
| **1 · ตัดปุ่มค้นหาออก** ← ค่าเริ่มต้น | เอาปุ่มสีทองออก เหลือไอคอนแว่นซ้าย (ทรงเดียวกับของจริง) | 384px | 315px |
| 2 · ขยายช่อง | เก็บปุ่มสีไว้ ขยาย 384 → 496px | 496px | 346px |
| 3 · ย่อข้อความ | เหลือ "ค้นหาการ์ด..." ช่องแคบลงได้ถึง 320px | 320px | 170px |
| 4 · ย้ายไปกลางแถว | ค้นหาไปกึ่งกลาง ของฉันไปขวาสุด (ไม่เบียดกัน) | 480px | 330px |

**แบบ 1 กับ 3 แก้ที่ต้นเหตุ · แบบ 2 กับ 4 แก้ที่ปลายเหตุ**

**ข้อแลกที่ต้องรู้:** แบบ 1 เสียความเด่นแบบ Shopee/Lazada (ปุ่มสีตัดกับช่องขาว) · แบบ 3 เสียคำใบ้ว่าค้นด้วยรหัสการ์ดได้ ซึ่งคนเล่นใช้บ่อย · แบบ 4 แยกของฉันออกจากช่องค้นหา

**ตรวจรับแล้ว:** eslint 0 · tsc ผ่าน · **วัดจริง: ไม่มีแบบไหนข้อความโดนตัด (textTruncated=false ทุกแบบ)** · ทุกปุ่มควบคุม ≥40px · สูง 104px เท่ากันหมด · ไม่ล้นที่ 1360px

### โครง D2 ที่ล็อกแล้ว
```
[48] OPCG▾ › OP15▾ ‖ สถิติ · สายพาน ‖ อัปเกรด · แชท · กระดิ่ง · โปรไฟล์
[56] Meecard · เมนู 4 ลิงก์ ⟷ พอร์ต · รายการโปรด · Honey · [ช่องค้นหา]
```

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3008/proto/navbar-ecom`** เคาะทางแก้ช่องค้นหา 1–4
2. เคาะแล้วลงของจริง (โครง D2):
   - `header.tsx` + `header-market-ticker.tsx` แก้ in-place
   - `--chrome-h` ใน `globals.css`: 8.25rem → **6.5rem** (104px)
   - `HeaderCatalogControl` ต้องรับ `boxImageUrl` (มีใน `/api/sets` แล้ว)
   - ซ่อน `StripFigure` 2 ตัว (จำนวนชุด · อัปเดตล่าสุด) · ย้าย `MyStuffCluster` ลงแถวล่าง
3. มือถือยังไม่ได้ออกแบบใหม่ — ทำต่อหลังเคาะ desktop
