# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนูแนว Shopee/Lazada

**เบสส่งภาพหัวเว็บ Shopee TH + Lazada TH มา สั่ง "ออกแบบ navbar ใหม่ ทำ proto ให้ดูก่อน · คงข้อมูลเดิมไว้ แต่จัดใหม่หมด"**

**ดูที่:** `http://localhost:3000/proto/navbar-ecom` (เปิด dev server แล้วเข้า URL นี้ · หน้านี้ไม่มี chrome ของเว็บและไม่ถูก Google เก็บ อัตโนมัติเพราะอยู่ใต้ `/proto`)

**ไฟล์เดียว ไม่แตะของจริงเลย:** `src/app/proto/navbar-ecom/page.tsx` (ไฟล์ใหม่ 1 ไฟล์ · ไม่มีไฟล์เดิมถูกแก้)

**สูตรที่ยกมาจาก Shopee/Lazada:** ช่องค้นหาใหญ่เป็นพระเอกกลางจอ (โตขึ้น ~3 เท่า) · ของประจำบัญชียกขึ้นแถบบางบนสุด · คำค้นยอดนิยมเป็นแถวลิงก์ใต้ช่องค้นหา · ตัวเลือกทาสีแบรนด์ทั้งแผงแบบ Shopee

**4 แบบให้สลับเทียบ (ปุ่มสลับด้านบน + ปุ่มพระจันทร์ดูโหมดมืด):**

| แบบ | ผัง | สูงจริงที่วัดได้ |
| --- | --- | --- |
| ปัจจุบัน | แบบ C ที่ขึ้นเว็บอยู่ (ตัวตั้งเทียบ) | 132px (32+44+56) |
| **S · แถบสีแบรนด์** ← เสนอหลัก | ทาสีแบรนด์ทั้งแผง · utility บาง (สถิติ+ภาษา+ข้อความ+กระดิ่ง+โปรไฟล์) · ค้นหาใหญ่กลางจอ · เมนู+คำค้นฮิตแถวล่าง | 124px (32+64+28) |
| L · พื้นเดิม | โครงเดียวกับ S บนพื้นสีเดิม · **แถบชีพจรเดิมครบทั้งสายพาน** ย้ายลงล่างสุด | 128px (32+64+32) |
| H · คงแถบชีพจร | แถบชีพจรเดิมอยู่บนสุดไม่แตะ · ยุบแถวโลโก้+แถวเมนูเหลือแถวเดียว | 124px (32+64+28) |

**ตาราง "ของเดิมแต่ละชิ้นไปอยู่ตรงไหน" ท้ายหน้า** ไล่ครบ 15 ชิ้นต่อแบบ (สถิติ · สายพาน · โลโก้ · เกม›ชุด · อัปเกรด · ข้อความ · แจ้งเตือน · โปรไฟล์ · ฝั่ง guest · เมนู · พอร์ต · โปรด · Honey · ค้นหา · ภาษา/สกุลเงิน/ธีม) — พิสูจน์ว่าไม่มีอะไรหายเงียบ

**สิ่งที่เบสต้องเคาะเมื่อดู:**
1. **แบบ S ต้องแลกสายพานการ์ดขยับแรงทิ้ง** (ไม่มีที่ลงในผังนี้) — ยอมแลกกับคำค้นยอดนิยมไหม? ถ้าไม่ยอม → แบบ L หรือ H เก็บสายพานไว้ครบ
2. **โหมดมืดของแบบ S กลายเป็นแถบสีทองสว่างทั้งแผง** (เพราะสีแบรนด์ในโหมดมืดคือทอง) — กดปุ่มพระจันทร์ดูก่อนตัดสิน
3. แบบ S ทาสีตลอด **ไม่มีสถานะโปร่งใสตอนอยู่บนสุดหน้า** แบบของจริงตอนนี้
4. คำค้นยอดนิยมตอนนี้เป็นคำตัวอย่างตายตัว — ของจริงยังไม่มีระบบเก็บคำค้น (ต้องต่อยอดจากการ์ดที่คนดูมากแทน)

**ตรวจรับแล้ว:** `npx eslint` ไฟล์ใหม่ 0 errors · `npx tsc --noEmit` ผ่าน · เปิดจริงบน browser ครบ 4 แบบ × 2 ธีม · สายพานวิ่งจริง (`ticker-scroll` 90s) · รูปการ์ดจาก R2 ขึ้นครบ 14/14 ไม่มีรูปเสีย · ไม่มีป้ายไทยโดนตัดที่ความกว้าง 1360px · ตาราง 15 แถวครบทุกแบบ

**ข้อมูลในหน้าเป็นค่าตายตัวทั้งหมด** (ตัวเลขชุดเดียวกับจอจริงของเบส 28 ส.ค. + การ์ดจริง 7 ใบพร้อมรูปจาก R2) — ไม่มี fetch/DB/สุ่ม ตามกติกา proto ของ repo · ปุ่มทุกปุ่มกดไม่ได้จริง เป็นหุ่นโชว์ผัง

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3000/proto/navbar-ecom` แล้วเคาะ** ว่าเอาแบบไหน (หรือผสม) — โดยเฉพาะ 4 ข้อที่ต้องตัดสินด้านบน
2. เคาะแล้วค่อยลงของจริง: แก้ `src/components/layout/header.tsx` + `header-market-ticker.tsx` in-place (ไม่มีเวอร์ชันแยก) · ต้องอัปเดต `--chrome-h` ใน `globals.css` ถ้าความสูงเปลี่ยนจาก 132px
3. ถ้าเอาแถวคำค้นยอดนิยมจริง ต้องคุยกันก่อนว่าเอาข้อมูลจากไหน (ตอนนี้ยังไม่มีระบบเก็บคำค้น)
4. รอบนี้ทำเฉพาะ desktop — มือถือยังไม่ได้ออกแบบใหม่ ถ้าเคาะแบบใหม่แล้วต้องทำต่อ
