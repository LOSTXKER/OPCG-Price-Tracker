# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบหก · ชุดกลับเป็น dropdown)

**⚠️ ดูที่ `http://localhost:3007/proto/navbar-ecom` ไม่ใช่ 3000** — dev server ที่ 3000 (ของอีก session, PID 94622) ค้าง route manifest เสิร์ฟหน้านี้เป็น 404 ทั้งที่ compile ผ่าน · ถ้าอยากใช้ 3000 ต้องปิด-เปิดใหม่

### เบสสั่งรอบนี้
> "ฉันว่าชุดเอาเป็น dropdown ดีกว่ามันรก จัดใหม่หน่อย แถว 3"

- **ชั้นวางชุดเรียงยาว 14 ชุด ตัดทิ้ง** → เหลือปุ่มชุดปุ่มเดียว
- **รูปกล่องไม่หาย** — ย้ายไปอยู่ในปุ่ม (กล่องของชุดที่เลือกอยู่ + รหัส + ชื่อชุด) และในรายการที่กางออกมา
- พอชุดไม่กินที่แล้ว แถว 3 ว่างเยอะ → เสนอ 3 วิธีใช้ที่ว่างนั้น

### โครงที่ล็อกแล้ว (จากแบบ B ที่เบสเคาะ)
1. แถบชีพจร — สถิติ + สายพานการ์ด
2. แถวกลาง 56px — โลโก้ · เมนูเว็บ · ช่องค้นหา 44px · อัปเกรด · ข้อความ · แจ้งเตือน · โปรไฟล์
3. แถว 3 — เกม▾ › ชุด▾ ‖ พอร์ต · รายการโปรด · Honey

### 3 วิธีจัดแถว 3
| แบบ | ทำอะไร | สูงรวม | ช่องค้นหากว้าง |
| --- | --- | --- | --- |
| A · โล่งสองฝั่ง | ซ้าย = เกม›ชุด · ขวา = ของฉัน · กลางปล่อยว่างตั้งใจ (เหลือที่เพิ่มปุ่มมากสุด) | 136px | 576px |
| **B · ย้ายสถิติลงมา** ← ค่าเริ่มต้น | ยกตัวเลขตลาดลงมาเติมกลางแถว 3 → **แถบชีพจรบนเหลือสายพานล้วนเต็มความกว้าง** เห็นการ์ดต่อรอบเยอะขึ้นเกือบเท่าตัว | 136px | 576px |
| C · สองแถวจบ | เมนูเว็บขึ้นไปคู่สายพานแถวบน แล้วรวบทุกอย่างลงแถวเดียว | **100px** | **351px** ⚠️ |

**⚠️ ข้อควรรู้ของ C:** เตี้ยลง 32px จริง แต่ช่องค้นหาเหลือ 351px — เกือบเท่าของจริงตอนนี้ (320px) เท่ากับแลกจุดขายของทั้งงาน (ค้นหาเป็นพระเอก) ทิ้งไปเพื่อความเตี้ย · แถมต้องตัดตัวหนังสือ "Meecard" ข้างโลโก้ออกและย่อปุ่มชุดแล้วถึงพอ

**ตรวจรับแล้ว (วัดในเบราว์เซอร์):** eslint 0 · tsc ผ่าน · ทุกปุ่มควบคุม ≥40px ทุกแบบ · รูปกล่องโหลดได้ · ชื่อพอร์ต/รายการโปรด/Honey ครบ · ไม่ล้นที่ 1360px · ไม่มี markdown หลุดในคำอธิบาย

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3007/proto/navbar-ecom`** เลือก A / B / C — ถ้าไม่อยากเสียความกว้างช่องค้นหา ให้เลี่ยง C
2. เคาะแล้วลงของจริง:
   - `header.tsx` + `header-market-ticker.tsx` แก้ in-place
   - `--chrome-h` ใน `globals.css`: 8.25rem → **8.5rem** (A/B) หรือ **6.25rem** (C)
   - ปุ่มชุดที่มีรูปกล่อง: `HeaderCatalogControl` ต้องรับ `boxImageUrl` เพิ่ม (มีใน `/api/sets` อยู่แล้ว)
   - ถ้าเลือก B ต้องแยก `StripFigure` ออกจาก `HeaderMarketTicker` ไปแถว 3
3. มือถือยังไม่ได้ออกแบบใหม่ — ต้องทำต่อหลังเคาะ desktop
