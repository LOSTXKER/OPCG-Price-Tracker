# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-28 (ดึก ปิดงาน) — ✅ **งานทั้งคืนขึ้น master แล้ว (`88b8811`) · เบสสั่งเอง · Vercel deploy อัตโนมัติ · ปิด dev server ทุกพอร์ตแล้ว**

## ✅ สถานะปิดงานคืนนี้

- **master = `88b8811`** (merge commit รวม 12 commits จาก `feat/navbar-direction-r2`) — เบสสั่ง "commit push main ทั้งหมดเลย" จึง push เข้า master ตรงตามที่อนุมัติ (ปกติกฎห้าม push master ของ repo ที่ต่อ Vercel)
- **ตรวจก่อนขึ้นครบ:** ESLint บน `src/` **0 errors** · tsc ผ่าน · test **918 ข้อผ่าน** · build ผ่าน
  - หมายเหตุ: `npm run lint` เต็มรายงาน 9 errors แต่**ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/`** = ไฟล์ build ของ worktree ที่ session งาน E668 สร้าง (ถูก exclude จาก git อยู่แล้ว ไม่ได้ commit ไม่กระทบ production) — ถ้าอยากให้เงียบ ต้องเพิ่ม ignore ใน eslint config
- **ปิด dev server หมดแล้ว** (3000/3001/3002) ไม่มี process ค้าง — รวมถึงตัวที่ 3000 ที่ค้างไม่ยอม rebuild CSS ทั้งคืน เปิดใหม่จะได้ของสดแล้ว
- branch `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ) ถ้าอยากลบสั่งได้

## งานทั้งหมดที่ขึ้นเว็บรอบนี้ (3 สายรวมกัน)

1. **แถบเมนูบน แบบ C** — แถบชีพจร 32px บนสุด · แถวโลโก้ 44px · แถวเมนู 56px (ช่องค้นหาขวาสุดหลัง Honey) · รวม 132px
2. **แถบชีพจรตลาด** — การ์ดขยับแรง 24 ชม. วิ่งพร้อมรูปจิ๋ว+สีเขียวแดง+ลูกศร · ~39px/วินาที · หยุดเมื่อ hover/focus
3. **หน้าการ์ด → navbar บอกชุด** ของการ์ดใบนั้นอัตโนมัติ
4. **หน้าแรก** — แถบชุดการ์ดขึ้นแทนหัวตาราง + อยู่นิ่ง · กวาดคำ "อัปเดตทุกวัน" ครบทั้งเว็บ
5. **popup ค้นหา** — ยังไม่พิมพ์ก็มีของให้ดู (สาย 3)

**เบสเจอปัญหา "ทำแบบใหม่ push แล้ว Vercel มี แต่ local กลับเป็นแบบเดิม"** → สาเหตุ: งานเขาอยู่บน `master` (PR #122) ส่วน local เปิด branch `feat/navbar-direction-r2` ที่แตกไป**ก่อน**นั้น → **แก้แล้วด้วยการ merge `origin/master` เข้า branch นี้** (conflict มีแค่ PROGRESS.md ไฟล์เดียว · i18n รวมกันเองได้) ตอนนี้ local มีงานครบทั้งสองสาย

## งานสายนี้ — Navbar แบบ C + แถบชีพจรมีชีวิต

**โครงแถบบน 3 ชั้น (desktop ≥md · รวม 132px · `--chrome-h` md: 8.25rem):**
1. **แถบชีพจร h-8 (32px)** — ซ้าย: ตัวเลขคงที่ (การ์ดทั้งหมด · ชุดการ์ด · มูลค่ารวม · JPY/THB · อัปเดตล่าสุด) เป็นตัวหนังสือเปล่าสีกลาง ไม่ใช่ชิป · ขวา: **สายพานการ์ดที่ราคาขยับแรง 24 ชม. วิ่งต่อเนื่อง**
2. **แถวโลโก้ h-11** — โลโก้ · เกม›ชุด · อัปเกรด · แชท/กระดิ่ง/แคปซูลโปรไฟล์ (ไม่มีเส้นแบ่งแนวตั้งแล้ว)
3. **แถวเมนู h-14** — เมนู 4 ลิงก์ทรงแคปซูล · พอร์ต/รายการโปรด/Honey · **ช่องค้นหาขวาสุดท้ายแถว**

**สายพาน (เบสสั่ง "เพิ่มสี เพิ่มข้อมูล ให้มันเลื่อน"):**
- การ์ด 12 ใบ (ขึ้น 6 + ลง 6 สลับฟันปลา) · **รูปการ์ดจิ๋ว 14×20px** (สัดส่วน 63:88, `alt=""` เพราะชื่ออยู่ข้างๆ, `loading="eager"` เพราะรางเลื่อนด้วย transform ซึ่งไม่ปลุก lazy observer) + ชื่อ + รหัสชุด + ราคา + %
- **เขียว/แดงถูกกติกา** เพราะเป็น delta ของการ์ดจริง (ไม่ใช่ chrome) + **ลูกศร ▲▼ คู่เสมอ** ตาม VISION §4 กฎ 3 · ตัวเลขรวมของเว็บยังสีกลาง
- วิ่งด้วย `.animate-ticker` (CSS ที่มีในระบบอยู่แล้ว) **~39px/วินาที** (เบสสั่ง "ขอช้ากว่านี้หน่อย" → `ticker-scroll` 60s → **90s** ใน globals.css · วัดจริง 59.2 → 39.5 px/s ช้าลง 1.5 เท่า) ขวา→ซ้าย · **หยุดเมื่อ hover และเมื่อ focus ด้วยคีย์บอร์ด** · `prefers-reduced-motion` → กลายเป็นแถบเลื่อนมือ (ไม่ซ่อนข้อมูล) · ขอบจาง mask สองข้าง
- **เกณฑ์ราคาขั้นต่ำ 500 JPY** — ก่อนใส่ สายพานโชว์การ์ด 120 JPY ที่ −20.0% ติดกัน 3 ใบ (ขยับจริง 7 บาท ดูเหมือนข้อมูลเสีย) ตามหลัก VISION §5.3 movers ต้องวัดที่ขนาดการขยับ ไม่ใช่ %
- **API `/api/cards/ticker`** รวมตัวเลข+วันที่+movers ในคำขอเดียว **แทน `/api/cards?limit=1`** เดิม ⇒ จำนวน request เท่าเดิม payload เบาลง · ลิงก์ 24 ตัว `prefetch={false}`

## งานสาย 3 — popup ค้นหาโฉม CoinMarketCap (เบสส่งภาพ CMC มาเป็นแนวทาง)

**ก่อนพิมพ์ popup ไม่ใช่กล่องเปล่าแล้ว — เป็นหน้าชวนดูของ:** ค้นหาล่าสุด → แถว **"ยอดนิยม"** การ์ดดูบ่อยสุดเป็นชิปเลื่อนข้าง (รูป+ชื่อ+ราคา แบบ Top Boosted) → ลิสต์ **"มาแรง"** การ์ดขยับแรง 24 ชม. พร้อมราคา + %เขียว/แดงคู่ลูกศร (กติกา VISION: delta จริงถึงได้สี) + ลิงก์ "ดูทั้งหมด" ไปหน้ามาแรง → ทางลัด 12 หน้า **ยุบเป็นเม็ดยา** แถวล่าง · พิมพ์ปุ๊บของแนะนำหลบให้ผลค้นหาแบบเดิมทันที · popup กว้างขึ้น (max-w-lg → 2xl บน desktop) · ปุ่มลูกศรคีย์บอร์ดไล่ครบทุกรายการ (เปลี่ยน index เลขฐานเป็น map ตามลำดับที่เห็นจริง) · กด X ล้างคำแล้ว focus กลับช่องพิมพ์

**ข้อมูลมาจาก `/api/cards/spotlight` ใหม่** (ดูบ่อยสุด 6 + ขยับแรง 6, ใช้เกณฑ์ราคาขั้นต่ำ 500 JPY เดียวกับสายพาน) — โหลด**ครั้งแรกที่เปิด popup เท่านั้น** แล้ว cache ทั้งอายุหน้า (hook `use-search-spotlight.ts`) ไม่เพิ่มภาระตอนโหลดหน้า

**เบสสั่งเพิ่มกลางคัน "เวลาค้นหา แสดงรหัสของการ์ดให้ครบ"** → `search-result-row.tsx` (แถวผลค้นหากลางของทุกช่องค้นหา: popup + hero + dropdown) เปลี่ยนจากโชว์รหัสชุด (op13) เป็น**รหัสการ์ดเต็ม (OP13-118)** — ใช้ `baseCardCode` ตามกติกา ไม่มีหางระบบ `_p1`

**ตรวจรับสาย 3:** tsc ผ่าน · `npx eslint src` **0 errors** · test **153 ไฟล์/917 ข้อผ่าน** (เพิ่มเทสต์ spotlight route + เทสต์ล็อกหน้า discovery) · build ผ่าน · เปิดจริง: มืด+สว่าง ✓ มือถือ 375 ✓ (ชิปเลื่อนข้าง ไม่ล้นจอ) พิมพ์/ล้าง/ลูกศร ✓ รหัสเต็มขึ้นทุกแถว ✓
- ⚠️ `npm run lint` ทั้ง repo ตอนนี้เด้ง 528 errors — **ทั้งหมดมาจาก `.claude/worktrees/exciting-saha-a8f2a1/.next/**`** (ขยะ build ของ worktree อีก session ที่ eslint ไม่ได้ ignore) ไม่ใช่โค้ดจริง · ถ้า worktree นั้นถูกเก็บแล้วยังเหลือ ค่อยเพิ่ม ignore ใน eslint config (ต้องถามเบสก่อนเพราะเป็น config)

## งานจาก master ที่เพิ่ง merge เข้ามา (PR #122 — ของเบส/อีก session)

- `c68df1d` `2fabbab` `35916e2` — **แถบชุดการ์ดหน้าแรกขึ้นไปแทนหัวตารางราคา** เหลือแถวเดียว เลื่อนได้ทั้งปัดนิ้ว/หมุนเมาส์/ปุ่มลูกศร (กลไกอยู่ที่ `home-set-strip.tsx` + `set-ticker-motion.ts` + test)
  - **แก้ต่อ 2026-08-28 (เบสสั่ง "ขอแบบไม่ต้องเลื่อนเอง นิ่งๆ"):** ถอดการไหลอัตโนมัติออก (rAF loop + hold/resume handlers) · **ถอดการ์ดชุดที่สองออกด้วย** เพราะโคลนมีไว้ให้วนไม่มีรอยต่อ พอไม่วนแล้วมันคือรายการซ้ำที่ทำให้ผู้ใช้สับสน — ตอนนี้เหลือชุดเดียวเริ่มที่ชุดใหม่สุด จบที่ปุ่ม "ดูทั้งหมด" · ปุ่มลูกศรเปลี่ยนไปใช้การ clamp ของเบราว์เซอร์แทนการ wrap และ honor `prefers-reduced-motion` (smooth → auto)
  - `set-ticker-motion.ts` ตอนนี้เหลือใช้แค่ `NUDGE` — ฟังก์ชัน drift ทั้งหมด (+เทสต์ 79 บรรทัด) **จอดไว้โดยตั้งใจ ไม่ใช่ลืม** เพราะ auto-scroll เปิด-ปิดมาแล้ว 2 รอบในคืนเดียว เก็บไว้ต่อกลับได้ทันที มีคอมเมนต์กำกับที่หัวไฟล์ + แยก task ไว้ลบเมื่อแบบนิ่งอยู่ตัวแล้ว
- `81c075b` — **กวาดคำ "อัปเดตทุกวัน/daily/毎日" ที่เหลือทั้งเว็บ** (layout · og-image · guide/versions · blog · most-expensive · seo/copy ทุกไฟล์ · i18n 3 ภาษา) เหลือ "อัปเดตล่าสุด <วันที่จริง>" — **คิวงานนี้ปิดแล้ว**
- ⚠️ ของที่สายนั้นฝากไว้: `src/lib/seo/json-ld.ts` มี `priceValidUntil = scrapedAt + 24 ชม.` ซึ่งตอนนี้คำนวณออกมาเป็นวันหมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — **รอเบสตัดสิน** ว่าจะเอาออก/ขยายเป็น 7 วัน/อย่างอื่น

## หลักฐานตรวจรับ (สายนี้ ก่อน merge)

- ESLint 0 errors · tsc ผ่าน · test **152 ไฟล์ / 910 ข้อผ่าน** · build ผ่าน · ตัวตรวจดีไซน์ 0 findings
- Browser จริง: พิสูจน์สายพานด้วยการวัด — วิ่ง 0 → −3378px = ครึ่ง track เป๊ะ ⇒ **วนไร้รอยต่อจริง** · รูป 24/24 โหลดครบผ่าน next/image · chrome 133px · sticky เกาะ 132px ทุกหน้า · ไม่ล้นที่ 768/1024/1512 · มืด+สว่างอ่านออก · มือถือ 375 ไม่กระทบ
- ⚠️ **`Router action dispatched before initialization` (E668)** ขึ้นทุกหน้า **รวมหน้าที่ไม่มี navbar** (`/login`, `/proto/navbar`) = ปัญหาเดิมระดับแอป ไม่ได้เกิดจากงานนี้ — เบสเปิด session แยกไล่แล้ว

## ⛔ ยังไม่ได้ verify หลัง merge

merge เพิ่งเสร็จ **ยังไม่ได้รัน lint/test/build และยังไม่ได้เปิดดูจริง** — ที่ต้องดูเป็นพิเศษ: หน้าแรกตอนนี้มี **ticker สองอันพร้อมกัน** (แถบชีพจรบน navbar + แถบชุดการ์ดของเบส) ต้องดูว่ารกไปไหม

## หน้าการ์ด → navbar เลือกชุดให้อัตโนมัติ (เบสสั่ง 2026-08-28)

เข้าหน้าการ์ดใดก็ตาม ตัวเลือกชุดบน navbar จะโชว์ชุดของการ์ดนั้น (เดิมโชว์ "เลือกชุดการ์ด")

- **ทำไมไม่เดาจากรหัสการ์ด:** วัดข้อมูลจริง 100 ใบ → **ผิด 16 ใบ** เพราะ reprint/promo เก็บรหัสเดิมไว้แต่ไปอยู่ชุดใหม่ (`OP09-006_r1` อยู่ st23 · `OP05-115_p1` อยู่ prb01 · `P-029_r1` อยู่ st16) ⇒ ถ้าเดาจะโชว์ชุดผิด 1 ใน 6 ใบ แย่กว่าไม่โชว์
- **วิธีที่ใช้:** หน้าการ์ดรู้ชุดจริงจาก DB อยู่แล้ว → `ActiveSetPublisher` (component เปล่าแบบเดียวกับ `CardViewTracker`) เขียนลง store `activeSetCode` ตอน mount และล้างเป็น null ตอน unmount · `HeaderCatalogControl` อ่านเป็น fallback: `getHeaderSetCode(pathname) ?? publishedSetCode` (URL มาก่อนเสมอ)
- ไม่ persist ค่านี้ (เป็นบริบทของหน้า ไม่ใช่ค่าตั้งค่า) · **ไม่มี request เพิ่ม**
- วัดจริง: `OP13-118_p3` → "op13 · Carrying on His Will" · **`OP09-006_r1` → "st23 · Red Shanks"** (เคสที่การเดาจะพัง) · กลับหน้าแรก → กลับเป็น "เลือกชุดการ์ด" · มือถือ 375 ได้ด้วยไม่ล้นจอ

## ⚠️ dev server ที่ port 3000 ค้าง — อ่านก่อนถ้ารีเฟรชแล้วไม่เห็นของใหม่

dev server (Turbopack) ที่เปิดค้างไว้ที่ **port 3000 หยุด rebuild CSS แล้ว** — แก้ `globals.css` เป็น 90s แต่หน้าเว็บยังเสิร์ฟ 60s (stylesheet ชื่อ `[root-of-the-server]__20zuhjp._.css` ไม่เปลี่ยน hash) แม้ hard reload และ `npm run build` แล้ว
**วิธีดูของใหม่:** ปิด-เปิด dev server ที่ 3000 ใหม่ **หรือ** ดูที่ **`http://localhost:3002`** (production server ที่เปิดไว้ให้แล้ว ยืนยันว่าเป็น 90s จริง) · ไม่ได้ไปฆ่า process ที่ 3000 ให้ เพราะเป็นของที่เบส/อีก session เปิดไว้

## สถานะ git

- branch `feat/navbar-direction-r2` · merge `origin/master` (78f659c) เข้ามาแล้ว · ไม่มี schema/migration/dependency/config change
- `header-search-preferences.test.tsx` (รวมเทสต์รูปการ์ด 4 บรรทัดของสายชีพจร) **สาย 3 commit ให้แล้ว** · ไฟล์ spotlight ที่ติดไปใน `06c9aa5` ใช้ได้จริง ไม่ต้องแก้ย้อน
- `home-set-strip.tsx` ยังแก้ค้างอยู่ = ของสายแถบชุดการ์ด **สาย 3 ไม่แตะ**

## NEXT

1. **เบสรีเฟรช `localhost:3000` กดช่องค้นหา** — ดู popup โฉมใหม่: ยอดนิยม/มาแรง/เม็ดยาทางลัด + รหัสการ์ดเต็มในผลค้นหา (สาย 3 verify ผ่านแล้ว ทั้ง test/build/เปิดจริง)
2. **verify หลัง merge**: เปิดหน้าแรกดูจริงว่า ticker สองอันอยู่ด้วยกันได้ไหม (test+build ล่าสุดผ่านแล้วโดยสาย 3 — เหลือดูด้วยตา)
3. เบสดูแล้วตัดสิน: แถบชุดการ์ด (ของเบส) กับ แถบชีพจร (navbar) ควรอยู่ด้วยกันทั้งคู่ หรือปรับอันใดอันหนึ่ง
4. ตัดสิน `priceValidUntil` ใน `src/lib/seo/json-ld.ts`
5. พอใจแล้วเปิด PR `feat/navbar-direction-r2` → master
6. คิวเดิมค้าง: `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
