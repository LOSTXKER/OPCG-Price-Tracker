# สถานะสด — meecard

> เขียนทับทุกครั้งที่จบงาน · อ่านไฟล์นี้ก่อนเริ่ม session ใหม่

**อัปเดต:** 2026-08-29 (รอบดึก)
**branch:** `master` (local) · งานรอบนี้ push ที่ `proto/row-favorite`

## ทำอะไรไปรอบนี้

**หน้าลองใหม่: ปุ่มรายการโปรดในแถวรายการการ์ดบนมือถือ** — `/proto/row-favorite` (สถานะ: **รอเบสเคาะ**)

เบสสั่งว่าอยากมีปุ่มกดรายการโปรดในตารางหน้าแรกมุมมือถือ · ที่ยังไม่ชัดคือ "ปุ่มไปอยู่ตรงไหนของแถว"
เพราะแถวบนมือถือกว้าง 375px และตอนนี้ชื่อการ์ดได้ที่แค่ 152px → ใส่ปุ่มตรงไหนก็มีของต้องแลก
จึงทำหน้าลองเทียบ 4 ทางก่อน (ยังไม่แตะโค้ดของจริง):

- **ปัจจุบัน** — ในแถวไม่มีปุ่มเลย (ต้องเข้าหน้าการ์ด หรือสลับไปมุมมองกริดที่มีหัวใจอยู่แล้ว)
- **A · ขวาสุดของแถว** — นิ้วโป้งถึงง่ายสุด ไล่กดรัวได้ · แลกกับชื่อการ์ดหายไปราว 40px
- **B · ซ้ายสุด ก่อนเลขอันดับ** — เรียงเหมือนตารางบนคอมเป๊ะ · แลกกับมือเอื้อมไกลกว่า + มุมซ้ายแน่น
- **C · ซ้อนมุมขวาบนของรูปการ์ด** — ไม่กินความกว้างชื่อเลย · แลกกับปุ่มเล็กกว่ามาตรฐาน (28px ไม่ใช่ 44px)

ในหน้ามีปุ่มสลับสถานะขอบ: "ชื่อยาวทุกแถว" (ดูว่าชื่อโดนตัดแค่ไหน) · "กดโปรดไว้ทุกใบ" (ดูว่าหัวใจแดงเต็มแถวรกไหม)
· สลับโหมดมืด/สว่างในหน้าได้ · ข้อมูล ชื่อ ราคา % และรูปการ์ดก๊อปมาจากหน้าแรกจริง (ตายตัว ไม่ต่อฐานข้อมูล)
· ตัด "ปัดแถวแล้วปุ่มโผล่" ออกจากตัวเลือกเพราะไม่มีอะไรบอกว่าปัดได้ (เขียนบอกไว้บนหน้าแล้ว)

เปิดดูเองครบแล้ว: ทั้ง 4 ทาง × มือถือ/คอม × โหมดมืด/สว่าง · lint + typecheck ผ่าน

## ค้างตรงไหน

- **รอเบสเคาะว่าเอาแบบไหน** (ปัจจุบัน / A / B / C) — เคาะแล้วค่อยลงของจริงที่
  `src/components/home/mobile-card-item.tsx` (แถวเดียวนี้ใช้ทั้งหน้าแรกและหน้าค้นหา)

## NEXT

1. เบสเปิด `/proto/row-favorite` บนมือถือ → กดสลับ 4 ปุ่มบนสุด แล้วบอกกลับมาว่าเอาตัวไหน (ตอบเป็นตัวอักษรได้เลย)
2. เคาะแล้ว: ลงของจริงในแถวมือถือ + อัปเดตสถานะในทะเบียน `/proto` เป็น "เคาะแล้ว"
