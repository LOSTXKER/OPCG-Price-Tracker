# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## ✅ งานรอบนี้ — navbar D2 **ลงของจริงแล้ว** (เบสเคาะ 2026-08-29)

**เบสเคาะ:** โครง D2 + ช่องค้นหาแบบ 3 (ย่อข้อความ) + มือถือเลื่อนลงแล้วปุ่มเลือกเกม/ชุดไม่ต้องตามมา → **"ทำเลย"**

### โครงใหม่ที่ขึ้นแล้ว (desktop) — 132px → **104px**
```
[48] เกม▾ › ชุด▾(รูปกล่อง) ‖ การ์ดทั้งหมด · มูลค่ารวม · JPY/THB · สายพาน ‖ อัปเกรด · แชท · กระดิ่ง · โปรไฟล์
[56] โลโก้ Meecard · เมนู 4 ลิงก์ ⟷ พอร์ต · รายการโปรด · Honey · ช่องค้นหา 320px
```

### ไฟล์ที่แก้
| ไฟล์ | ทำอะไร |
| --- | --- |
| `header-market-ticker.tsx` | ยุบแถบชีพจร + แถวโลโก้เหลือแถวเดียว h-12 · catalog control นำแถว · **ซ่อนสถิติ 2 ตัว** (จำนวนชุด · อัปเดตล่าสุด) ให้สายพานมีที่ · โลโก้ย้ายออก |
| `header.tsx` | โลโก้ย้ายมานำแถวเมนู · ช่องค้นหา `w-52 lg:w-80` → `w-80` คงที่ |
| `globals.css` | `--chrome-h` md: 8.25rem → **6.5rem** |
| `set-picker.tsx` | `SetPickerItem` เพิ่ม `boxImageUrl` (API ส่งมาอยู่แล้ว ไม่ต้องแก้ API) |
| `header-catalog-control.tsx` | ปุ่มชุด desktop โชว์**รูปกล่องจริง** + รหัส + ชื่อชุด 2 บรรทัด (fallback เป็นไอคอนเดิมถ้าไม่มีรูป) |
| `header-mobile.tsx` | **ซ่อนปุ่มเลือกเกม/ชุดเมื่อ scrolled** · ค้นหา/แจ้งเตือน/โลโก้ยังอยู่ |
| เทสต์ 2 ไฟล์ | อัปเดต topology test ให้ล็อกโครงใหม่ (ไม่ได้ลบเทสต์) |

**หมายเหตุช่องค้นหา:** แบบ 3 ที่เบสเลือก (ย่อข้อความ) **ตรงกับของจริงอยู่แล้ว** — `searchCardsDots` = "ค้นหาการ์ด..." + ทรงไอคอนแว่นไม่มีปุ่มสี จึงไม่ต้องแก้ placeholder เลย เปลี่ยนแค่ความกว้างให้คงที่ 320px

### ตรวจรับ (ทำครบแล้ว)
- `npx eslint src` **0 errors** (26 warnings เดิมที่มีอยู่ก่อน)
- `npx tsc --noEmit` ผ่าน · `npm run test` **918 ข้อผ่าน / 153 ไฟล์** · `npm run build` ผ่าน
- **เปิดดูจริงบน production server (port 3010)** — วัดได้: `--chrome-h: 6.5rem` · แถวบน 48px + แถวล่าง 56px = 104px · โลโก้อยู่แถวล่าง · catalog อยู่แถวบน · ค้นหา 320px · สายพานได้ที่ 422px
- **หน้าชุด op15**: ปุ่มชุดโชว์รูปกล่องจริง + "op15 / Adventure on KAMI's Island" (รูปโหลดสำเร็จ)
- **มือถือ 375px**: เลื่อนลง → ปุ่มเลือกเกม/ชุด**หายจริง** ค้นหา+แจ้งเตือน+โลโก้ยังอยู่ · เลื่อนกลับขึ้นบน → กลับมา

### ⚠️ dev server cache
`npm run dev` เสิร์ฟ CSS เก่า (`--chrome-h: 8.25rem`) เพราะ Turbopack cache ที่ `.next/dev/` ค้าง — **production build ถูกต้องแล้ว** ยืนยันจาก `.next/static/chunks/*.css` = 6.5rem · ถ้าเปิด dev แล้วความสูงยังเป็นแบบเก่า ให้ลบ `.next/dev` แล้วเปิดใหม่

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิดดูของจริง** — ตอนนี้อยู่ที่ `http://localhost:3010` (production server) · ถ้าจะใช้ dev ให้ลบ `.next/dev` ก่อน ไม่งั้นได้ CSS เก่า
2. พอใจแล้วเปิด PR `feat/navbar-ecom-proto` → master (มีทั้ง proto และของจริง)
3. **มือถือยังไม่ได้ออกแบบใหม่** — รอบนี้แตะแค่ให้ปุ่มชุดหายตอนเลื่อน ยังไม่ได้รื้อผัง
4. ของค้างเดิม: `priceValidUntil` ใน `json-ld.ts` · `getHomeData().rarityRows` query ทิ้งเปล่า
