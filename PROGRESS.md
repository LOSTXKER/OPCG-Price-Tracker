# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนูแนวใหม่ (รอบสาม)

**ดูที่:** `http://localhost:3000/proto/navbar-ecom` · **ไฟล์เดียว ไม่แตะของจริงเลย:** `src/app/proto/navbar-ecom/page.tsx`

### เบสสั่งอะไรมาบ้าง (สะสม 3 รอบ)
1. รอบแรก: ไม่ชอบแถวคำค้นยอดนิยม · แบบทาสีแบรนด์ไม่เอา · **ที่พอได้คือ L (พื้นเดิม) กับ H (คงแถบชีพจร)**
2. รอบสอง: **"พวกนี้มันเล็กไป คำนึงถึงการใช้งานจริง และหน้าที่ของแต่ละอย่างด้วย"** (ชี้ที่กลุ่ม อัปเกรด/ภาษา/ข้อความ/กระดิ่ง/โปรไฟล์)
3. รอบสอง (ต่อ): **พอร์ต · รายการโปรด · Honey ห้ามซ่อนชื่อ**

### แก้อะไรไปในรอบนี้
- **ปุ่มทุกตัวสูง ≥40px** (วัดจริงแล้ว minTap = 40 ทุกแบบ) — รอบก่อนย่อของบัญชีเหลือ 28px ซึ่งผิดกฎ hit target ของ repo เอง
- **พอร์ต/รายการโปรด/Honey มีป้ายชื่อครบทุกแบบ** — ลบ component ย่อไอคอน (`MyStuffIcons`) ทิ้ง
- **ปุ่มภาษาถอดออกจากแถบ กลับเข้าเมนูโปรไฟล์** — เป็นของตั้งครั้งเดียวจบ ไม่ควรกินที่ตลอดเวลา (ตรงกับของจริง)
- **ขนาดไล่ตามหน้าที่:** ค้นหา 44px ใหญ่สุด → อัปเกรดเป็นปุ่มมีกรอบ (ทางไปจ่ายเงิน) → เมนู/ของฉัน 40px → แจ้งเตือนเป็นไอคอนแต่จุดแดงเห็นชัด
- **แบบ "สองแถวจบ" ถูกตัดทิ้ง** — พอห้ามซ่อนชื่อ คำนวณแล้วของล้นเกิน 1360px ทำไม่ได้จริง → แทนด้วยแบบ 3 ใหม่

### 3 ทิศทางปัจจุบัน
| แบบ | ไอเดีย | สูงจริง |
| --- | --- | --- |
| ปัจจุบัน | แบบ C ที่ขึ้นเว็บ (ตัวตั้งเทียบ) | 132px |
| **1 · ค้นหาครองแถว** ← ค่าเริ่มต้น | ตัวเลือกเกม›ชุดกลายเป็นหัวช่องค้นหา (ท่า Lazada/Amazon) ช่องค้นหายืดเต็มแถว · กลาง = ตัวตน+ค้นหา+บัญชี · ล่าง = นำทาง+ของฉัน | 132px (32+56+44) |
| 2 · แถบชุดการ์ด | ล่าง = แถวนำทางเต็มรูป (เมนู ‖ ทุกชุด + ชุดล่าสุด 8 ชุด) · กลาง = ค้นหา+ของฉัน+บัญชี | 132px (32+56+44) |
| 3 · ค้นหาลงล่าง | พลิกลำดับ: กลาง = โลโก้+เมนู+บัญชี · ล่าง = ช่องค้นหายักษ์เต็มแถวติดเนื้อหา + ของฉัน | 144px (32+56+56) |

**ตรวจรับแล้ว:** eslint 0 · tsc ผ่าน · เปิดจริง 4 แบบ × 2 ธีม ไม่มี console error · **วัดด้วยสคริปต์: ทุกปุ่ม ≥40px · ไม่มีอะไรล้น/โดนตัดที่ 1360px · ชื่อพอร์ต/รายการโปรด/Honey อยู่ครบทุกแบบ · ความสูงตรงกับที่เขียนในคำอธิบาย**

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสรีเฟรช `http://localhost:3000/proto/navbar-ecom` แล้วเคาะ** ว่าเอาทิศทางไหน (ผสมได้ เช่น หัวช่องค้นหาของแบบ 1 + แถบชุดของแบบ 2)
2. ถ้ายังไม่โดน บอกได้ว่าไม่โดนตรงไหน — ตอนนี้แก้ครบแล้วทั้งขนาดปุ่ม ชื่อของฉัน และลำดับความสำคัญตามหน้าที่
3. เคาะแล้วค่อยลงของจริง: แก้ `src/components/layout/header.tsx` + `header-market-ticker.tsx` in-place · ถ้าเลือกแบบ 3 ต้องแก้ `--chrome-h` ใน `globals.css` จาก 8.25rem → 9rem
4. รอบนี้ทำเฉพาะ desktop — มือถือยังไม่ได้ออกแบบใหม่
