# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบเก้า · เหลือคำถามเดียว)

**⚠️ ดูที่ `http://localhost:3007/proto/navbar-ecom` ไม่ใช่ 3000** — dev server ที่ 3000 (ของอีก session, PID 94622) ค้าง route manifest เสิร์ฟหน้านี้เป็น 404

**เบสบอก "ชอบ 1 2"** → ทั้งสองแบบมีแถว 1 (ชีพจร) และแถว 3 (เมนู ⟷ ของฉัน) **เหมือนกันเป๊ะ** ต่างกันจุดเดียว: **ปุ่มชุดอยู่นอกหรืออยู่ในช่องค้นหา**

### โครงที่ล็อกแล้ว (ทุกแบบเหมือนกัน)
1. **แถว 1** — แถบชีพจรตลาด (สถิติ + สายพาน) ตรงตามที่ขึ้นเว็บ ไม่แตะ
2. **แถว 2** — โลโก้ · [จุดที่ต่างกัน] · อัปเกรด · ข้อความ · แจ้งเตือน · โปรไฟล์
3. **แถว 3** — เมนูเว็บซ้าย ⟷ พอร์ต · รายการโปรด · Honey ขวา

### ตัวเลือกที่เหลือ
| แบบ | ปุ่มชุด | อ่านได้ว่า | ช่องค้นหา |
| --- | --- | --- | --- |
| **เทียบ 1 ↔ 2** ← เปิดมาที่นี่ | (ซ้อนกันให้เห็นชัด) | — | 575 / 958 |
| 1 · ชุดอยู่นอก | ลอยข้างโลโก้ | "ตอนนี้อยู่ที่ OPCG › OP15" (บริบท) | 575px |
| 2 · ชุดอยู่ใน | หัวช่องค้นหา | "ค้นใน OPCG › OP15" (ขอบเขต) | **958px** |
| **1+ · ลูกผสม** | ลอยข้างโลโก้ + ช่องค้นหาบอกขอบเขตเอง | ได้ทั้งบริบทและขอบเขต | 685px |

**ลูกผสม 1+ ทำอะไร:** ปุ่มชุดยังลอยอยู่นอก (กดเปลี่ยนชุดได้โดยไม่ต้องแตะช่องค้นหา = ข้อดีของ 1) แต่ช่องค้นหาเขียนเองว่า "ค้นในชุด OP15 — พิมพ์ชื่อการ์ดหรือรหัส..." (= ความหมายของ 2) แล้ว**ย้ายปุ่มอัปเกรดเข้าเมนูโปรไฟล์**เพื่อคืนความกว้าง (+110px)
→ ข้อแลก: ปุ่มอัปเกรดหายจากหน้าแถบ เหลือทางเข้าเดียวในเมนูโปรไฟล์ — **ถ้าอยากดันยอดสมัคร อาจไม่คุ้ม ต้องเบสตัดสิน**

**ตรวจรับแล้ว:** eslint 0 · tsc ผ่าน · แถว 1 เป็นชีพจรล้วนทุกแบบ · ทุกปุ่มควบคุม ≥40px · ชื่อพอร์ต/รายการโปรด/Honey ครบ · ไม่ล้นที่ 1360px · โหมดเทียบเรนเดอร์ทั้งสองแบบซ้อนกันได้ถูกต้อง

### ประวัติที่เบสเคาะมาแล้ว (อย่าเสนอซ้ำ)
- ❌ ทาสีแบรนด์ทั้งแผง · ❌ แถวคำค้นยอดนิยม · ❌ ชั้นวางชุดเรียงยาว · ❌ แตะแถว 1 · ❌ ซ่อนชื่อพอร์ต/โปรด/Honey · ❌ ปุ่มเล็กกว่า 40px
- ✅ พื้นสีเดิม · ✅ ค้นหาใหญ่ · ✅ ชุดเป็น dropdown มีรูปกล่อง · ✅ แถว 1 = ชีพจรเท่านั้น · ✅ แถว 3 = เมนู ⟷ ของฉัน

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3007/proto/navbar-ecom`** — เปิดมาที่โหมดเทียบเลย ดูสองแบบซ้อนกัน แล้วเคาะ 1 / 2 / 1+
2. เคาะแล้วลงของจริง: `header.tsx` + `header-market-ticker.tsx` in-place · `--chrome-h` 8.25rem → **8.5rem** · `HeaderCatalogControl` ต้องรับ `boxImageUrl` (มีใน `/api/sets` แล้ว) · ถ้าเลือก 1+ ต้องเอาปุ่มอัปเกรดออกจาก header ด้วย
3. มือถือยังไม่ได้ออกแบบใหม่ — ทำต่อหลังเคาะ desktop
