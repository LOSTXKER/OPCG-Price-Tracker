# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบสิบ · ของฉันขึ้นแถว 2)

**⚠️ ดูที่ `http://localhost:3007/proto/navbar-ecom` ไม่ใช่ 3000** — dev server ที่ 3000 (ของอีก session, PID 94622) ค้าง route manifest เสิร์ฟหน้านี้เป็น 404

**เบสสั่ง:** ส่งภาพ navbar จริงมา + "ขอลองแบบนี้แต่ พอร์ต/รายการโปรด/Honey ไปแถว 2 แล้วจัดหน่อย"

### ผลที่ตามมา (สำคัญ)
พอของฉันย้ายขึ้นแถว 2 แล้ว **แถว 3 เหลือแค่เมนูกับช่องค้นหา** → ช่องค้นหายืดได้ **981px** จากเดิม 320px (โตขึ้น 3 เท่า) โดยไม่ต้องเบียดใคร

### โครง (ทุกแบบเหมือนกัน)
1. **แถว 1** — แถบชีพจรตลาด ล็อกตรงตามที่ขึ้นเว็บ ไม่แตะ
2. **แถว 2** — โลโก้ · เกม▾ › ชุด▾ (รูปกล่อง) · [ของฉัน] · อัปเกรด · ข้อความ · แจ้งเตือน · โปรไฟล์
3. **แถว 3** — เมนูเว็บ 4 ลิงก์ · ช่องค้นหา

### 3 วิธีจัด
| แบบ | ของฉันอยู่ตรงไหนในแถว 2 | ช่องค้นหา |
| --- | --- | --- |
| **A · ของฉันชิดขวา** ← ค่าเริ่มต้น | ชิดขวา ติดกลุ่มบัญชี มีเส้นคั่น — "มุมขวาคือเรื่องของฉันทั้งหมด" | 981px ยืดเต็ม |
| B · ของฉันชิดซ้าย | ต่อจากปุ่มชุดทันที — ซ้ายรวมเครื่องมือทำงาน ขวาเหลือแค่บัญชี ไม่แน่น | 981px ยืดเต็ม |
| C · ค้นหากลางจอ | เหมือน A | 768px กึ่งกลางจอ (ทรง Shopee/Lazada) |

**ข้อแลกที่ต้องดู:** A มุมขวาแถว 2 มีของ 7 ชิ้นเรียงกัน แน่นสุด · B พอร์ตอยู่ห่างจากโปรไฟล์ทั้งที่เป็นของส่วนตัวเหมือนกัน · C เสียที่ว่างฝั่งขวาเพื่อความสมมาตร

**สูงรวม 144px** ทุกแบบใหม่ (ของจริง 132px) เพราะสองแถวล่างต้องสูง 56px ให้ช่องค้นหาลงได้

**ตรวจรับแล้ว:** eslint 0 · tsc ผ่าน · ของฉันอยู่แถว 2 ครบทุกแบบ (ตรวจด้วยสคริปต์) · ทุกปุ่มควบคุม ≥40px · ไม่ล้นที่ 1360px

### ประวัติที่เบสเคาะมาแล้ว (อย่าเสนอซ้ำ)
- ❌ ทาสีแบรนด์ทั้งแผง · ❌ แถวคำค้นยอดนิยม · ❌ ชั้นวางชุดเรียงยาว · ❌ แตะแถว 1 · ❌ ซ่อนชื่อของฉัน · ❌ ปุ่มเล็กกว่า 40px
- ✅ พื้นสีเดิม · ✅ ค้นหาใหญ่ · ✅ ชุดเป็น dropdown มีรูปกล่อง · ✅ แถว 1 = ชีพจรเท่านั้น · ✅ **ของฉันอยู่แถว 2**

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3007/proto/navbar-ecom`** เคาะ A / B / C
2. เคาะแล้วลงของจริง: `header.tsx` + `header-market-ticker.tsx` in-place · `--chrome-h` 8.25rem → **9rem** (144px) · `HeaderCatalogControl` ต้องรับ `boxImageUrl` (มีใน `/api/sets` แล้ว) · ย้าย `MyStuffCluster` จากแถวเมนูขึ้นแถวโลโก้
3. มือถือยังไม่ได้ออกแบบใหม่ — ทำต่อหลังเคาะ desktop
