# สถานะสด — meecard

> เขียนทับทุกครั้งที่จบงาน · อ่านไฟล์นี้ก่อนเริ่ม session ใหม่

**อัปเดต:** 2026-08-30 (ดึก)
**branch:** `master` (เบสสั่ง merge + push ขึ้น master เอง 2026-08-30)

## ทำอะไรไปรอบนี้ — ลงเว็บจริงหมดแล้ว

### 1. ปุ่มรายการโปรดในแถวรายการการ์ดบนมือถือ ✅

เบสเคาะแบบ **A (ขวาสุดของแถว)** จากหน้าลอง `/proto/row-favorite`

- ปุ่มหัวใจอยู่นอกลิงก์ กดแล้วไม่เปิดหน้าการ์ด · กล่องกด 32×44px ห่างขอบจอ 12px
- บีบปุ่ม 36→32px และดึงชิดขอบ เพื่อคืนที่ให้บรรทัด "รหัส + ความหายาก" ไม่ให้ตัดสองบรรทัด
- ได้ทั้งหน้าแรกและหน้าค้นหาพร้อมกัน เพราะใช้แถวเดียวกัน

### 2. ป้ายลายศิลป์ มังงะ · มังงะแดง · ใบประกาศจับ ✅

เบสเคาะแบบ **ค (ป้ายคำต่อท้ายชื่อการ์ด)** จากหน้าลอง `/proto/manga-badge` (6 ทาง)

- component กลางตัวใหม่: `ArtStyleBadge` (`shared/art-style-badge.tsx`) — เป็นป้าย **ที่สอง**
  ข้างป้ายความหายาก ไม่ได้แทนที่ · คืน `null` เองเมื่อการ์ดเป็นลายปกติ
- ขึ้นแล้ว 4 ที่: แถวรายการมือถือ (รุ่น `compact`) · ตารางบนคอม · การ์ดในมุมมองกริด · หน้าการ์ด
- **ข้อแลกที่เห็นจากของจริง:** บนมือถือชื่อการ์ดมีที่ 116px พอมีป้าย "มังงะแดง" (46px)
  ชื่อเหลือ 64px → "Monkey…" เฉพาะการ์ดลายพิเศษ 56 ใบเท่านั้น (จาก 3,838 ใบ)
  ถ้าเบสว่ายังสั้นไป ปรับได้ที่ `compact` ใน `art-style-badge.tsx`
- ทะเบียนรายชื่ออยู่ที่ `src/lib/constants/card-art-style.ts` (มังงะ 35 · มังงะแดง 5 · ใบประกาศจับ 16)

### 3. ตัวกรอง "ลายศิลป์" + ย้าย "เวอร์ชัน" ขึ้นบนสุด ✅

- ลำดับในกล่องตัวกรองใหม่ทั้งหน้าแรกและหน้าค้นหา:
  **ความหายาก → เวอร์ชัน → ลายพิเศษ → ประเภท → สี → ราคา** (เบสเคาะ: ความหายากบนสุด)
- ชื่อหัวข้อตัวกรองใช้คำว่า **"ลายพิเศษ"** (เดิมร่างไว้ว่า "ลายศิลป์" — เบสเปลี่ยน)
- ฝั่งเซิร์ฟเวอร์: `/api/cards?artStyle=manga,mangaRed,wanted` → แปลงเป็นรายการรหัสการ์ดจากทะเบียน
  (ค่าที่ไม่รู้จัก = 400) · ทดสอบแล้ว: มังงะแดง 5 ใบ · มังงะ+ประกาศจับ 51 ใบ · ใช้ร่วมกับตัวกรองสีได้
- แก้บั๊กที่ซ่อนอยู่ระหว่างทาง: `where.AND` เคยถูก assign ทับ ทำให้ถ้ามีตัวกรองสองตัวที่ใช้ AND
  ตัวแรกจะหายเงียบๆ — เปลี่ยนเป็นสะสมใน list เดียวแล้วค่อยใส่

ตรวจครบ: lint · typecheck · เทส 924 ข้อ · `npm run build` ผ่าน · เปิดดูจริงทั้งมือถือ/คอม

## ค้างตรงไหน

- **รอเบสตรวจรายชื่อ 56 ใบ** ที่ `/proto/manga-badge` (เลื่อนลงล่างมีกริดรูปทุกใบ) —
  เจอใบที่จัดผิดหรือใบตกหล่น บอกรหัสมา แก้ที่ `src/lib/constants/card-art-style.ts` ไฟล์เดียว
- **⚠️ คีย์ Gemini ใน `.env` ใช้ไม่ได้** (`API key not valid`) → ฟีเจอร์ "ค้นหาด้วยรูป" ที่เพิ่งขึ้นเว็บ
  น่าจะพังอยู่ ต้องเอาคีย์ใหม่จาก Google AI Studio ใส่ทั้ง `.env` และ Vercel
  (สคริปต์ `scripts/classify-manga-art.ts` ใช้ได้ทันทีเมื่อมีคีย์ใหม่)

## NEXT

1. เบสตรวจรายชื่อการ์ดมังงะที่ `/proto/manga-badge` แล้วบอกใบที่ผิด
2. เปลี่ยนคีย์ Gemini แล้วลองค้นหาด้วยรูปอีกครั้ง
3. ดูของจริงบนเว็บหลัง Vercel deploy master เสร็จ
