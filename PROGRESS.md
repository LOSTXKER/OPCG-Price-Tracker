# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · hook โหลดไฟล์นี้ทุก session · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-09 — **/opcg/trending รื้อ UI** (เส้นตาราง + กล่องมูฟเวอร์จืด/ไม่มีสี — ดู section 🎨) commit+push master แล้ว · ก่อนหน้า (2026-08-08): รูปกล่อง/ซองชุดการ์ดของจริงทั้งเว็บ (50/51 ชุด · ดู section 📦) + สลับสะกด "วันพีช" นำทั้งเว็บ (🔤🔁) · ติดต่อเราเหลือเฟซบุ๊ก (📮) · รูปชุดในแถบหน้าแรก 2 บรรทัด (🖼️) — **ทั้งหมด commit+push เข้า master แล้วตามคำสั่งเบส** · ก่อนหน้า: SEO ครบ 3 รอบ push master แล้ว (R1+R2 = `130af6e`) · รายงาน audit: artifact "ตรวจ SEO ทั้งเว็บ Meecard" https://claude.ai/code/artifact/ca79b001-d4d4-4b2d-9294-36c210a3813b · ติ๊กราย task: PLAN.md §SEO Round 1–3

## 🎨 /opcg/trending — เส้นตาราง + กล่องมูฟเวอร์จืด (2026-08-09 · commit+push master แล้ว)
เบสชี้จากหน้าเว็บจริง (`?tab=gainers`) 2 จุด: ตารางมีเส้นใต้ทุกแถวไม่เหมือนหน้าอื่น + ถามว่ากล่อง SEO ใต้ตารางซ้ำซ้อนไหม → ตอบว่าไม่ซ้ำซ้อน (คนละช่วงเวลา ตั้งใจไว้ให้ครอว์เลอร์เห็นข้อมูลนอกแท็บ default) แต่เบสตามด้วย "ทำ UI ให้ดีกว่านี้ได้มั้ย มันไม่สวย" เลยรื้อรอบเดียว

- **เส้นตาราง**: `TrendingRow` (trending-tabs.tsx) มี `border-b border-hair` ทุกแถว ต่างจาก `MarketTable`/ตารางพอร์ตที่ขีดเส้นแค่ใต้หัวตาราง (parity confirm จาก market-table-row.tsx ไม่มีเส้นแถวเลย) — ลบเส้นออกจากแถวข้อมูล ให้ตรงกับ convention เว็บ
- **กล่องมูฟเวอร์ 3 กล่อง** (ราคาลง 24h/ขึ้นแรง 7 วัน/ขึ้นแรง 30 วัน) เดิมเป็นตัวหนังสือล้วน ไม่มีรูป ไม่มีสี หน้าตาเหมือนกันเป๊ะทั้ง 3 → ย้ายไปใช้ `ListRow` (canonical) ใส่รูปการ์ดจริงต่อแถว + ป้ายสีเขียว/แดงข้างหัวข้อแยกกล่อง + **% เปลี่ยนใช้ `PriceTag` แทนตัวหนังสือสีเทาที่เขียนเอง** (พลาดรอบแรก เบสจับได้จากภาพจริงว่า "% ไม่ใช้แดงเขียวหรอ" แก้ตามแล้ว)
- **การ์ด "น่าเก็บ/น่าลงทุน" 3 ใบ** เดิมไม่มีไอคอน (ผิดกติกา 1 concept = 1 ไอคอน) → เพิ่มไอคอน Zap/TrendingUp/AlertTriangle สไตล์เดียวกับ `RelatedPageCard` ท้ายหน้า ให้จังหวะหน้าไม่สะดุดตอนเจอกล่อง "เพิ่มเติม" มีสีต่อท้าย

**Verify**: tsc 0 · lint 0 · test เดิม `trending-movers-sections.test.tsx` 8/8 ผ่าน · เปิดดูจริงทั้ง prod (opcg-price-tracker.vercel.app ยืนยันบั๊กเส้นตาราง) และ dev :3001 (ยืนยันรูป+สีหลังแก้) · โครงสร้าง server HTML/SEO เดิมทุกจุด (h2/ลิงก์/ราคา/% เหมือนเดิม) — แก้แค่การนำเสนอ

**ยังไม่แตะ (บอกไว้กับเบสแล้ว)**: ตารางหลัก 50 แถวยังยาวเท่าเดิม (ตั้งใจไว้เพื่อความลึก SEO ranking) — ถ้าอยากให้สั้นลงหรือเปลี่ยนกล่องมูฟเวอร์เป็นการ์ดรูปใหญ่แนวนอนแทนลิสต์ตัวหนังสือ เป็นงานถัดไปที่ใหญ่กว่านี้

## 🔨 รอบก่อนหน้า — Round 3 (commit+push master แล้ว 2026-08-08)

ไฮไลต์: **/guide จัดใหม่** — การ์ดบทขึ้นจอแรก (เดิม prose 1.5 จอ) บทนำย่อ+ย้ายลงในคอลัมน์อ่าน แก้ breadcrumb "Guide" อังกฤษ · **ลิงก์ในเนื้อความ** คู่มือทุกหน้า (เดิมกักท้ายหน้า) · **/guide/versions ได้ section "การ์ดวันพีซมีภาษาไทยไหม"** (KIDZ & KITZ — ข้อเท็จจริง verify แล้ว: การ์ดพิมพ์ไทยยังไม่มี) + Article schema + วันที่ · **trending ได้ section "น่าเก็บ/น่าลงทุน"** · **rarities เลิก hardcode ราคา** (ดึง DB จริง + ตารางเทียบ parallel ใช้การ์ดจริง OP05-119) · **Honey จอแรกเลิกเป็นกล่องเทา** · **รูปชุดแก้ราก** (fallback เคยหยิบการ์ดข้ามชุด → เลือกรหัสชุดตัวเองก่อน + onError) · WebSite schema +alternateName −SearchAction · Breadcrumb JSON-LD ไทยตรง UI (การ์ด/ค้นหา/ภาพรวมตลาด/guide ทั้งหมด)

**Verify**: tsc 0 · lint 0 · vitest 140 ไฟล์/776 · build 209 หน้า · เปิดดูจริง :3001 มือถือ+desktop

**Known state**: dev DB ราคา scrape ล่าสุด 5 เม.ย. 2569 (วันที่บนหน้า/ราคา rarities สะท้อนข้อมูลเก่าตามจริง — prod จะสดเอง) · marketplace flag ใน dev เปิดอยู่ · พอร์ต 3000 = Bill Tracker · meecard ทดสอบที่ :3001 (`meecard-start-3001`) · `STATIC_CONTENT_UPDATED` ใน sitemap.ts อัปเดตมือเมื่อแก้เนื้อหานิ่ง

## 🔗 รอบเพิ่มเติมหลัง R3 (commit+push master แล้ว 2026-08-08): CTA สองชั้นทั้งเว็บ
เบสชี้จาก screenshot ว่าลิงก์ CTA แบบ text จมเกิน → ตั้งนโยบาย 2 ชั้น: ในเนื้อความ=plain (ถูกแล้ว) · ก้าวถัดไป=canonical ใหม่ 2 ตัว **`ArrowLink`** + **`RelatedPageCard`** (ลง AGENTS.md แล้ว) กวาด 7 จุด: set detail ×2 · deck-calc (ตัดบล็อกซ้ำ) · drop-calc (list→RelatedPages) · trending · search · home strip — verify ครบ (tsc/lint/test 776/build/เปิดดูจริง)

**+ หน้าชุด intro** (เบสชี้อีกจุด): ย่อหน้า data dump (ทวน rarity/กล่อง/ใบแพงสุดที่ตาเห็นบนหน้าอยู่แล้ว) → ประโยคเดียว 129 ตัวอักษร + SectionHead พร้อม "ดูชุดทั้งหมด" เป็น action ขวา **เต็มความกว้าง content** (จำกัดคอลัมน์แล้วลิงก์ลอยกลางหน้า — เบสชี้) — กติกาใหม่ที่ test ล็อกไว้: **intro ห้ามทวนของที่หน้าโชว์**

**+ กวาด intro ทุกหน้า** (เบสสั่งตรวจทั้งเว็บ): แก้เพิ่ม 3 จุด — หน้าแรก (1 ประโยค ตัด methodology/how-to) · market-overview (เลิกเล่าซ้ำแผง KPI เหลือประโยคเดียว+ชุดมูลค่าสูงสุด) · compare (<li>→<p> + breadcrumb ไทย) — หน้าอื่นสำรวจแล้วผ่านกติกา · verify: tsc/lint/test 776/build/curl HTML จริงทั้ง 3 หน้า

## ⬆️ อัปเกรด Next.js 16.2.1 → 16.3.0 + rename middleware → proxy
**dependency**: `package.json` (`next` + `eslint-config-next` = `16.3.0` pin เป๊ะตามเดิม ไม่ใส่ `^`) · `package-lock.json` · `.claude/launch.json` (+config `meecard-start-auto` เปิดพอร์ตว่างเอง เพราะ :3001 มี session อื่นจองอยู่) — React คงที่ 19.2.4

**rename ตาม Next 16**: `src/middleware.ts` → **`src/proxy.ts`** + `export async function middleware` → `proxy` (test คู่กันย้ายเป็น `src/proxy.test.ts` ด้วย) · codemod ทางการรันไม่ได้เพราะ tree สกปรก จึงทำมือตาม doc เป๊ะ · **ไม่ต้องแตะ**: `src/lib/supabase/middleware.ts` (คนละไฟล์ helper) · header `x-middleware-next` / `x-middleware-request-x-game` (ชื่อภายในของ Next ยังเหมือนเดิม) · `export const config.matcher` (ไม่เปลี่ยน) · ไม่มี `skipMiddlewareUrlNormalize` ให้แก้ · **ผลที่ได้**: build ไม่เตือน deprecated แล้ว

**Verify (รันซ้ำหลัง rename)**: tsc 0 · lint 0 error (35 warning เดิม — `<img>`/unused var/`window.location.href`) · vitest 140 ไฟล์/776 · build 209 หน้า Turbopack · เปิดดูจริง :3002 มือถือ+desktop ไม่มี console/server error · curl 23 route = 200 หมด · proxy ทำงานครบ: `/sets`→308 `/opcg/sets` · `/opcg/portfolio`→308 `/portfolio` · `/@handle`+`/opcg`+`/all/search` rewrite ถูก

## 🏢 + บริษัทบน /about · /guide intro เต็มหน้า (commit+push master แล้ว 2026-08-08)
- **/about เพิ่ม section "ผู้ดำเนินการเว็บไซต์"** (เบสให้ลิงก์ทะเบียน): บริษัท มี-ไลค์ โซเชียล จำกัด (Mee-Like Social Company Limited) เชียงใหม่ — 3 ภาษา + Organization schema ได้ legalName/alternateName/address · ใส่เฉพาะที่ยืนยันได้จากแหล่งที่เบสให้ (เลขทะเบียน/ที่อยู่เต็ม ไม่มีในหน้านั้น — เบสส่งมาเพิ่มได้) + ประโยคกันเข้าใจผิด (เป็นแหล่งข้อมูลราคา ไม่ซื้อขายเอง ไม่เกี่ยวกับ Bandai)
- **/guide intro เลิกครึ่งหน้าว่าง** (เบสชี้): สองคอลัมน์บน lg — ย่อหน้า "เกมคืออะไร" ซ้าย + ย่อหน้า "Meecard = ราคาอ้างอิง" เป็น Surface การ์ดขวา พร้อม ArrowLink → /about#methodology (ได้ทั้งความเต็มหน้าและ internal link) · มือถือซ้อนตามปกติ
- verify: tsc 0 · lint 0 · test 140 ไฟล์/778 (รวม test กับดัก ApiError +2) · build ผ่าน · เปิดดูจริง (guide สองคอลัมน์ + about มี section และ schema ครบ)

## 📐 กวาด "ครึ่งหน้าว่าง" ทั้งเว็บ (commit+push master แล้ว 2026-08-08)
เบสถามว่าหน้าอื่นยังเป็นแบบ /guide ไหม → สำรวจครบ: **แก้ 2 จุด** — trending section "น่าเก็บ" + compare section "ใช้ยังไงให้คุ้ม" (ทั้งคู่เป็นเคล็ดลับ 3 ข้อโดยธรรมชาติ → แปลงเป็น lead 1 บรรทัด + การ์ด Surface 3 ใบเรียงเต็มแถว — copy restructure เป็น {title, body} ใน tools.ts) · **ตั้งใจไม่แตะ**: ก้อนอธิบายราคาหน้าแรก (มีบันทึกตัดสินใจเดิมในโค้ด: ห้ามใส่กล่อง เพราะเคยลองแล้วดูเป็นแบนเนอร์โฆษณา — คอลัมน์แคบคือความตั้งใจ) · หน้า guide ลูกทั้งหมด = คอลัมน์กลางสมมาตร (ตั้งใจแบบหน้าบทความ ไม่ใช่ครึ่งหน้าว่าง) · drop-calc/honey/market-overview prose วิ่งเต็มความกว้างอยู่แล้ว · verify: tsc 0 · lint 0 · test 778 · build ผ่าน · วัด DOM จริง (compare การ์ด 3×397px เต็มแถว 1216px · trending tips 3/3 ใน HTML)

## 📖 ความกว้างหน้า guide + ตัด "มัธยฐาน" (commit+push master แล้ว 2026-08-08)
- **/guide/rarities เลิกใช้คำว่า "มัธยฐาน"** (เบส: ไม่จำเป็น) — เหลือช่วง "17–209,580 ฿" + "จากข้อมูลจริง 115 ใบ" · ตัดการคำนวณ median ใน query + ลบคีย์ 3 ภาษา + test ล็อกว่าห้ามกลับมา
- **หน้า guide ลูกกว้างขึ้น** (เบส: รู้สึกแคบ ไม่เท่าหน้าอื่น) — วัดแล้วพบว่า**ตัวหนังสือกว้างพอดีอยู่แล้ว** (672px ≈ 82–89 ตัวอักษร/บรรทัด = เพดานของความสบายตา) แต่**ตาราง/แถบการ์ด/กริดอึดอัด** เพราะติดคอลัมน์อ่าน 672px → เพิ่ม width token `article` (max-w-5xl) ใน page-container + ผูก `/guide/.+` ใน ROUTE_WIDTH + คลาส `.guide-article` ใน globals.css ล็อกเฉพาะ prose (`section > p/ul/ol`, `section > div > p`) ไว้ที่ 42rem เท่าเดิม · ผล: คอนเทนต์ 672→**960px** ตาราง/การ์ดได้ที่เพิ่ม 43% ส่วนตัวหนังสือกว้างเท่าเดิมเป๊ะ · มือถือไม่กระทบ (ไม่มี overflow)
- verify: tsc 0 · lint 0 · test 143 ไฟล์/837 · build ผ่าน · วัด DOM จริง 1440px (prose 672 · table 960) + มือถือ 375px

## 🐛 ปริศนา ApiError 500 (เกิด 2 ครั้ง — ยังไม่รู้ route ต้นเหตุ แต่วางกับดักแล้ว)
popup "Internal server error" เด้งใน dev 2 ครั้ง (บน 16.2.1 และ 16.3.0 — ไม่ใช่ผลอัปเกรด) · สืบแล้ว: กวาดยิง GET ทั้ง 49 route บน dev :3000 = **เขียวหมด** → ต้นเหตุน่าจะเป็น route ฝั่งเขียนข้อมูล หรืออาการวูบชั่วคราว (dev compile/Supabase สะดุด) · **แก้ที่เครื่องมือ**: `src/lib/api/client.ts` — error 5xx พกชื่อ route ในข้อความแล้ว เช่น "Internal server error (POST /api/xyz)" (4xx ไม่แตะเพราะโชว์ผู้ใช้) + test ล็อก · **ครั้งหน้าที่เด้ง popup จะบอกชื่อ route เอง — ก๊อปมาวางได้เลย**

## 🔤 เอา `_p1` / `_p2` (รหัสภายใน) ออกจากทุกที่ที่คนอ่านเห็น (commit+push master แล้ว 2026-08-08)
เบสชี้จาก screenshot หน้า /guide/sets: ป้าย `OP09-001_p1` — "การ์ดจริงมันไม่มี" → กวาดทั้งเว็บ

**หลักที่ยึด**: `_pN` (พาราเลล 1,082 ใบ) / `_rN` (reprint 362 ใบ) = คีย์ของ scraper เราเอง Bandai ไม่ได้พิมพ์บนการ์ด → **ห้ามโผล่ในข้อความที่คนอ่าน** แต่ **ยังอยู่ใน URL / `sku` / ชื่อไฟล์รูป / คิวรี DB** เพราะนั่นคือ "ที่อยู่จริง" ของการ์ด (ยืนยัน DB แล้ว: `baseCode` มีครบ 3,838 แถว และเท่ากับ cardCode ที่ตัด suffix เสมอ)

**ของกลางใหม่**: `src/lib/cards/card-code.ts` — `baseCardCode()` · `cardVariant()` · `printingLabel()` · `formatCardCodeLabel()` · **ไม่ import อะไรเลย** (ของเดิมอยู่ใน `seo/copy/card.ts` ซึ่งลาก dictionary ภาษาไทยทั้งก้อนเข้า client bundle) — path เดิม re-export ต่อ ไม่พัง

**แก้ไป ~40 ไฟล์** กลุ่มใหญ่: JSON-LD (`Product.name`/`description` + ItemList ของหน้าแรก/ค้นหา/ภาพรวมตลาด/หน้าชุด) · `og:image:alt` + รูป OG · meta/`<title>` ของ marketplace · การ์ด/ตาราง/ค้นหา/พอร์ต/watchlist/alert/notification bell/messages/seller/orders/deck-calc/compare · **ข้อความที่ส่งออกนอกเว็บ**: LINE, อีเมลแจ้งเตือน, cron alert · CSV export 4 ไฟล์

**จุดที่ต้องตัดสินใจ ไม่ใช่แค่ห่อ helper**:
- **/guide/sets** — แถว `OP09-001_p1` ในตาราง "รูปแบบ Card Code" สอนรหัสที่ไม่มีจริง → เปลี่ยนเป็น `P-014` (โปรโม = ตระกูลรหัสจริงตัวที่ 4) + คีย์ i18n `guideSetCodeParallelDesc` → `guideSetCodePromoDesc` ครบ 3 ภาษา
- **FAQ หน้าค้นหา** มีข้อ "ต่อท้ายด้วย _p1 หรือ _p2 คืออะไร" (เข้า FAQPage JSON-LD ด้วย) → เขียนใหม่เป็น "ทำไมรหัสเดียวกันมีหลายราคา" อธิบายพาราเลลโดยไม่อ้าง syntax ภายใน + ลิงก์ /guide/rarities · tip หน้า compare ก็เอา `_p1` ออก
- **แถบ "เวอร์ชันอื่น"** (หน้าการ์ด) เคยต่อ `_p1` ท้ายโค้ดชุด → ตัดทิ้ง ไม่ใส่แทน (ทุกใบในแถบคือเลขเดียวกันอยู่แล้ว · ป้าย rarity + ภาพวาดคือสิ่งที่แยกให้เห็น)
- **การ์ดแชร์พอร์ต** เคยเติม "· P1" → ตัด (รูปนี้ลง LINE/X ยิ่งไม่ควรมีเลขภายใน)
- **CSV 4 ไฟล์** — สเปรดชีตไม่มีภาพวาดให้แยก จึงไม่ตัดทิ้งเฉยๆ แต่**เพิ่มคอลัมน์ `Printing`** ("Parallel 2" / "Reprint 1" / ว่าง) ต่อจาก `Card Code` → ไม่เสียข้อมูล ไม่โชว์ syntax ภายใน

**ที่จงใจเก็บไว้**: `<title>` ของหน้าการ์ดยังเป็น "OP09-001 (Parallel 1)" — ไม่มี `_p` แล้ว แต่ต้องมีคำนี้ไว้กันชื่อซ้ำ เพราะ OP09-001 มีพาราเลล 2 ใบที่ rarity เท่ากัน (P-L ทั้งคู่) ถ้าตัดทิ้ง title จะซ้ำเป๊ะกัน — **ถ้าเบสอยากให้ตัดด้วย บอกได้ แก้จุดเดียว**

**Verify**: tsc 0 · lint 0 error (35 warning เดิม) · vitest 143 ไฟล์/837 (เพิ่ม guard 4 ชุด: helper · SEO copy การ์ด · JSON-LD · แถบเวอร์ชันอื่น · CSV) · build 209 หน้า · **สแกน HTML จริง 30 route** (ข้อความที่เห็น + JSON-LD ฟิลด์ที่คนอ่าน + meta ที่เป็นข้อความ + `<title>`) = **0 leak** · สคริปต์สแกนอยู่ที่ scratchpad `audit.py` รันซ้ำได้

**หมายเหตุ**: ระหว่างทำ มี session อื่นแก้ worktree เดียวกันอยู่ (compare/trending + `src/lib/guide/price-format.ts` ที่ยัง type error เพราะคีย์ i18n ยังไม่ครบ) — ไม่ได้แตะของเขา · build ที่ผ่าน 209 หน้า คือตอนย้ายไฟล์ที่เขายังทำค้างออกชั่วคราวแล้วคืนที่เดิม

## 📖 /guide เลิกเป็น text ล้วน — ระลอก 0 + หน้าแรก (commit+push master แล้ว 2026-08-08)
เบสชี้ว่า "guide บางหน้าโอเคแล้ว มีตัวอย่างเห็นภาพ แต่บางหน้ามีแต่ Text" — audit ทั้ง 9 หน้าด้วย workflow (11 agent) เจอ **สาเหตุร่วมข้อเดียว: หน้าไหนแตะฐานข้อมูล หน้านั้นมีของให้ดู** · 6 หน้าที่ query prisma (rarities/card-types/colors/sets/getting-started/hub) มีรูปการ์ด+ราคาจริง ส่วน **authenticity · buying · versions ไม่มี prisma แม้บรรทัดเดียว** เลยเหลือแต่ `<p>` เรียงกัน
> ตรวจแล้วไม่ใช่ปัญหา: `max-w-3xl` ที่กระจายในหน้าลูก **เป็น no-op** (chrome ให้ `reading`=max-w-2xl อยู่แล้วผ่าน ROUTE_WIDTH) — ไม่มีบั๊กจัดกลาง

**ระลอก 0 — guide kit ใหม่ 4 ตัว** (ลง AGENTS.md แล้ว): `GuideFigure` (กรอบหลักฐาน eyebrow+ของ+caption+snapshot) · `GuideCompareTable` (ยกจากตาราง JP/EN ของ versions — table ตั้งแต่ sm, ใต้นั้น stacked) · `GuidePointList` (แปลง `<p>` เรียงกันเป็นแถวมีไอคอน/เลข) · `GuidePriceFacts` (ราคาจริง 2–3 ช่อง) · + `lib/guide/card-examples.ts` (query กลาง cache 1 ชม. + try/catch ทุกตัว) · `lib/guide/price-format.ts` (กัน ฿ หลุดเข้า .tsx — ต่อยอด formatThb เดิม ไม่เขียนซ้ำ)

**หน้าแรกที่ทำ = /guide/authenticity** (หนักสุด 100:0 text:visual): intro 3 ย่อหน้ารวด → ย่อหน้า+**callout**+ย่อหน้า · จุดสังเกต 5 ข้อ + สัญญาณอันตราย 5 ข้อ → `GuidePointList` มีไอคอนคนละใบ (คง anchor `check-*` เดิม) · **แผนผัง 2 อัน** (เทียบใบต่อใบ / ส่องไฟ) เป็น schematic วาดเอง **ไม่ใช้รูปการ์ดจริง** เพราะติดป้าย "ใบที่สงสัย" บนการ์ดจริง = กล่าวหาของจริง · **รูปการ์ดแพงสุด 6 ใบ + ราคาบาทจริง** ใต้หัวข้อเกรด (ใช้ argument "ของปลอมวิ่งตามมูลค่า" ให้มีของดู)

**Verify**: tsc 0 · lint 0 error · **test 143 ไฟล์/837 ผ่านหมด** · build ผ่าน · เปิดดูจริง :3010 (ต้องเพิ่ม config `meecard-dev-auto` ใน launch.json เพราะ :3000 เป็นแอปอื่น และ :3002 เป็น production build ของ session ก่อน)
- ⚠️ ต้องเพิ่ม `guide/authenticity/page.tsx` เข้า allowlist ของ `display-currency-boundary.test.ts` (เหมือน rarities) เพราะหน้า guide โชว์บาทตายตัวตามกติกาเว็บ — หน้า guide หน้าอื่นที่จะโชว์ราคาก็ต้องเพิ่มทีละหน้าเหมือนกัน

### 🔴🔴 บั๊กใหญ่: การ์ด parallel ~60% โชว์รูปของการ์ดต้นฉบับ (เบสจับได้จากกรอบเทียบ)
เบสทักว่ารูป 2 ใบในกรอบ "การ์ดใบเดียวกัน คนละเวอร์ชัน" **หน้าตาเหมือนกัน** → ไล่ดูแล้วพบว่า `OP15-118.png` กับ `OP15-118_p2.png` เป็นไฟล์**เดียวกันเป๊ะ** (md5 872f79... เท่ากัน 250,117 ไบต์) ทั้งที่ URL คนละอัน
- **สุ่มตรวจ 18 คู่ที่ parallelIndex=1 → ซ้ำ 11 คู่ · ตรวจ 5 คู่แพงสุดในย่านราคา → ซ้ำ 5/5** ⇒ ประมาณ **60% ของ parallel ใช้รูปของ base** (ตอน scrape ไม่ได้ดึงลายพิเศษมา)
- **กระทบทั้งเว็บ ไม่ใช่แค่หน้า guide** — หน้าการ์ด/ค้นหา/พอร์ต ที่โชว์ `OP15-118_p2` (26,880 ฿) ก็ขึ้นลายของใบธรรมดา
- **แก้ที่ต้นทาง = งาน scraper** (ยังไม่แตะ) · ที่ทำไปคือกันไม่ให้ guide โชว์รูปซ้ำ: `getGuideParallelPair` ยิง **HEAD เช็ค ETag** (R2 คืน ETag = md5 พอดี → เช็คได้โดยไม่โหลดไฟล์) แล้วข้ามคู่ที่ลายเหมือนกัน — อยู่ใน cache 1 ชม. และ**หายเองเมื่อ scraper ดึงลายจริงมา**
- ผลตอนนี้: ข้าม Enel/Mihawk/Bonney/Nami/Robin (ลายซ้ำ) → ได้ **Luffy OP05-119 = 164 ฿ vs OP05-119_p1 = 6,258 ฿ (38 เท่า)** ลายต่างกันจริง (ตรวจด้วยตาแล้ว: ฟ้า-ขาว vs ม่วง-เขียว GEAR 5)

### 🔴 เจอข้อมูลบนหน้าที่ขัดกับ DB เอง (ยังไม่แก้ — ต้องให้เบสเคาะ)
1. **rarities**: ตัวเลข "SR 3-4 · R 7-8 · Leader ~4 ใบต่อกล่อง" (th.ts:2571-2573) พิมพ์มือ ขัดกับ `SetDropRate` จริง
2. **sets**: หัวข้อ/intro เขียนว่า "ชุดที่คนดูมากที่สุด / คนเช็คราคาบ่อยที่สุด" แต่ query เรียงตาม `releaseDate` — ระบบยังไม่มีตารางนับ view
3. **card-types**: ป้าย "Life 4-5" แต่ค่าจริงมี 2/3/6 ด้วย
4. **TR กับ DON มี 0 ใบใน DB** → บล็อกที่อิง 2 ระดับนี้ควรซ่อน ไม่ใช่โชว์กล่องเปล่า
5. คู่ปกติ-vs-parallel ที่ห่างกันสุดในคลังคือ **500–4,150 เท่า** (ฐาน ¥100-800 เทียบ manga art หลักแสน) — จริงตามข้อมูลแต่เอาไปโชว์แล้วดูเหมือนพิมพ์ผิด จึงตั้งกฎเลือกคู่ที่ "ฐานมีราคาจริง + ห่าง 5–100 เท่า" ได้ **Enel OP15-118 = 1,046 ฿ → 26,880 ฿ (25.7 เท่า)** เป็นตัวอย่างมาตรฐาน (มัธยฐานทั้งคลัง ~11 เท่า)

### ✅ ระลอก 1 ครบ 3 หน้าแล้ว (buying + versions ทำต่อจากรอบแรก)
- **buying**: §4 ได้ **GuideFigure คู่ Enel จริง** (OP15-118 = 1,046 ฿ vs OP15-118_p2 = 26,880 ฿ พร้อมรูป 2 ใบ) — section นี้สั่งให้ผู้อ่าน "ดูว่าได้เวอร์ชันไหน" มาตลอดแต่ไม่เคยมีรูปให้ดู · §6 ได้ `GuidePriceFacts` ชุด op15 จริง (152 ใบ · 24 × 6 = 144 ต่อกล่อง) + ปุ่มเขียนมือ → `RelatedPageCard` · §3 ย่อหน้าภาษีนำเข้า → callout warning (ตัดกำแพง 4 ย่อหน้า) · §7 ข้อควรระวัง 5 แถวเปล่า → `GuidePointList` มีไอคอน
  - **กับดักที่เจอ**: ใบแพงสุดของชุดล่าสุด = ใบ parallel เดียวกับที่ §4 โชว์อยู่แล้ว (ทั้งคู่เลือก "parallel แพงสุด") → การ์ดใบเดิมโผล่ 2 ที่ในหน้าเดียว ดูเหมือนบั๊ก ใส่ guard ตัดช่องซ้ำออกแล้ว
- **versions**: ตารางเขียนมือ → `GuideCompareTable` (ทั้งหมวดใช้ตัวเดียวกันแล้ว) · กฎวันที่ 28 พ.ย. 2025 → callout info · **ไม่ใส่ตัวเลขราคา JP/EN ตามที่ตั้งใจ — DB มีแต่ราคาฝั่ง JP หน้านี้จึงได้แค่โครง ไม่ได้รูปการ์ด (ตรงไปตรงมา ดีกว่าเดา)**
- Verify รอบนี้: tsc 0 · lint 0 error · **test 837 ผ่าน** · build ผ่าน · ตรวจ HTML จริงที่ :3010 ทั้ง 2 หน้า (รูป OP15 2 ใบอยู่ใน DOM จริง, ราคา 1,046/26,880 ฿ ขึ้นจริง)
  - ⚠️ preview pane ของ Claude เพี้ยนทั้ง session (ไม่ยอม repaint / viewport = 0) — screenshot เชื่อไม่ได้ ต้องเปิดดูเองที่ :3010

### 🚫 กติกา `_p1` — เบสทักว่า "P1 ไม่ต้องแสดง พึ่งแก้ระบบไปไม่ใช่หรอ" (ถูกต้อง)
มีระบบอยู่แล้วที่ `src/lib/cards/card-code.ts` (กติกาเจ้าของ 2026-08-08 "เอาออกไปเลย ทั้งเว็บ") แต่ฉันดันเอา `OP05-119_p1` / `OP15-118_p2` ไปโชว์เป็น label เอง → แก้แล้ว:
- กรอบเทียบเลิกโชว์รหัสรายใบ → โชว์เลขการ์ด **ครั้งเดียวบนหัวกรอบ** (`— OP05-119` ซึ่งทั้งคู่ใช้ร่วมกัน อธิบายแนวคิด "ใบเดียวกัน" ได้ดีกว่าเดิมด้วย) แล้วป้ายใต้รูปเป็นคำ: **ใบธรรมดา / ลายพิเศษ (พาราเลล)**
- `sub` ของช่องใบแพงสุดในชุด → `baseCardCode()`
- ตรวจ HTML ที่เรนเดอร์จริง (ตัด `<script>` ออกก่อน) = **ไม่มี `_p#` ในข้อความที่คนอ่านเห็นเลย** · ส่วนที่ยังมี `_p#` คือ URL/ชื่อไฟล์รูป/React key ซึ่งกติกาอนุญาตชัดเจน

### ✅ ระลอก 2 — hub (/guide) เสร็จ
- **หน้าที่ไม่มีรูปเลยสักใบ ตอนนี้มีรูป 8 ใบแล้ว**: ชิปชุดการ์ดที่เป็นตัวหนังสือ → กริดไทล์มีรูปปกชุด (2 คอลัมน์มือถือ / 4 บน sm)
  - ⚠️ **`boxImageUrl` เป็น NULL ทั้ง 51 ชุด** — แผน audit ที่บอกให้ใช้รูปกล่องทำไม่ได้จริง เลยใช้ "การ์ดแพงสุดในชุดนั้น" เป็นรูปปกแทน (วิธีเดียวกับที่ /guide/sets ใช้อยู่)
- การ์ดลิงก์ 2 ใบที่เขียนมือ → `RelatedPageCard` (ต่างจากของเดิมแค่ไม่มี chevron = ผิด Component Kit)
- ไอคอน `Calculator` ซ้ำ 2 ที่ → drop-calculator ใช้ `Dices` (เรื่องความน่าจะเป็น) · deck-calculator คง `Calculator` (เรื่องคำนวณราคา) — 1 ไอคอน = 1 ความหมาย
- ปุ่ม "ดูชุดทั้งหมด" เขียน class เอง → `ArrowLink`
- Verify: tsc 0 · lint 0 error · test 837 ผ่าน · build ผ่าน · curl เช็คแล้วมี `<img>` ปกชุด 8 ใบจริง

### เหลือ 5 หน้า
- **ระลอก 2 ที่เหลือ**: hub (ชิปชุด 8 ชิ้นเป็นตัวหนังสือ ทั้งที่มี `boxImageUrl`; การ์ดลิงก์ 2 ใบเขียนมือ → `RelatedPageCard`; ไอคอน Calculator ซ้ำ 2 ที่) · sets · colors
- **ระลอก 3–4**: rarities · getting-started · card-types (คลังคีย์เวิร์ด 12 กล่อง × 3 ย่อหน้า = 36 ย่อหน้าติดกัน — กำแพงสูงสุดในเว็บ)
- รายงาน audit เต็มรายหน้า: `/private/tmp/claude-501/-Users-lostxker-dev-Git-meecard/c2cb4aca-8622-4b7d-aeeb-b33d682d35e9/tasks/w3pbkjtv5.output`

## 🔤🔁 สลับสะกด "วันพีช" ขึ้นเป็นตัวนำทั้งเว็บ (2026-08-08 · commit+push master แล้ว)
เบสถาม "H1 หน้าแรกคำดีรึยัง" → ตรวจกับโลกจริง (Google suggest + SERP + คู่แข่ง) ได้คำตอบ: โครง H1 ถูกแล้ว (verdict 7/10 — "เช็คราคา..." คือ verb ที่คนค้นจริง อยู่ระดับเดียวกับเช็คราคาทอง) **แต่สะกดผิดข้าง**: คนไทยพิมพ์ "วันพีช" 9/10 และ Google autocorrect ซ→ช · เดิม convention คือ H1/title = วันพีซ, description แทรก วันพีช → เบสเคาะทางเลือก (ข) **flip ทั้งเว็บ**

**Convention ใหม่ (กลับบทบาทเป๊ะ)**: ข้อความที่คนอ่านเห็นทั้งหมด (H1/title/H2/UI/body/FAQ/JSON-LD) = **วันพีช** · ตัวรอง **วันพีซ** แทรกหน้าละอย่างน้อย 1 จุด (ปกติ = meta description) เพื่อคงการเก็บคำค้นสองสะกดต่อหน้า · keyword หลักของหน้าแรกกลายเป็น "เช็คราคาการ์ดวันพีช"

**แก้ ~19 ไฟล์ ~200 จุด**: copy ทุกโมดูล (home/site/sets/guide×4/tools/card/most-expensive) · `i18n/th.ts` (12 จุด UI) · `layout.tsx` (title/desc/keywords — keywords มีทั้งสองสะกด) · `json-ld.ts` · comment ที่บันทึกกติกาเดิมเขียนใหม่หมด · guard test 5 ไฟล์ flip เป็น invariant กระจก (ไม่มี test ถูกลบ/อ่อนลง — บางตัวแข็งขึ้น เช่น home intro ห้ามมี วันพีซ) · `doc/seo-content-plan.md` §1 อัปเดตกติกา

**จุดตัดสินใจที่ควรรู้**:
- **หน้าการ์ด**: slot ตัวรองย้ายจาก description → ประโยค intro ("ในเกมการ์ดวันพีซ...") เพราะ description ต้องขึ้นต้น "เช็คราคาการ์ดวันพีช" (คำหลักใหม่) — ทั้งหน้าเก็บสองสะกดครบ แต่ snippet บน Google จะไม่ bold คำ ซ แล้ว
- **/guide/sets**: ตัวรองอยู่ในประโยคนำ (มรดกจากโครงเดิม) ไม่ใช่ description — หน้าเดียวที่ต่างจากแบบแผน
- doc line 107: คีย์เวิร์ด quote ในงานวิจัยเก่าถูก flip ตาม (ผล research เดิมอาจรันด้วยสะกด ซ)

**Verify**: tsc 0 · lint 0 error (35 warning เดิม) · vitest 143 ไฟล์/**837 ผ่านหมด** · build ผ่าน (exit 0) · เปิดดูจริง :3000 — หน้าแรก H1 "เช็คราคาการ์ดวันพีช ทุกใบ ทุกเกรด", ข้อความมองเห็น วันพีซ = 0 จุด, description ยังพก วันพีซ · /guide H1+intro เป็น วันพีช ครบ · ตรวจปิดท้ายด้วย verify agent: ทุกหน้ายังเก็บสองสะกด ไม่มีหน้าไหนหลุด

**หมายเหตุ worktree**: งานนี้ปนอยู่กับงานอื่นที่ยังไม่ commit ในต้นไม้เดียวกัน (baseCardCode/guide layout/th.ts) — ถ้าจะ commit แยกต้อง stage ราย hunk

## 📮 ติดต่อเรา = เฟซบุ๊กช่องทางเดียว (2026-08-08 · commit+push master แล้ว)
เบสแจ้ง "เรามีแต่ช่องทาง facebook นะ" + ให้ลิงก์เพจ → อีเมล `support@meecard.app` **ไม่ใช่ช่องทางจริง** (ไม่มีคนอ่าน) จึงเอาออกทั้งหมด ไม่ใช่แค่เพิ่มเฟซบุ๊ก — โฆษณาอีเมลที่ไม่มีคนตอบแย่กว่าไม่มีเลย

- `SUPPORT_EMAIL` ลบทิ้ง → `CONTACT_FACEBOOK_URL` (https://www.facebook.com/profile.php?id=61575382823788 · เป็น numeric id ห้ามแต่ง @handle ปลอม)
- /contact: การ์ดช่องทาง Email → Facebook (ไอคอน MessageCircle · ปุ่มเปิดแท็บใหม่ + `rel="noopener noreferrer"`) · ปุ่ม "รายงานปัญหา" เลิก mailto · FAQ 3 ข้อ × 3 ภาษาเขียนใหม่หมด (ยังคงกติกา: **ห้ามสัญญาเวลาตอบ** — ของเดิม EN/JP แอบสัญญา "1–2 business days" อยู่ ตัดออกแล้ว)
- /about: บรรทัดแจ้งแก้ราคา → ลิงก์เพจ · Organization schema เลิกอ้าง email → `sameAs` + `ContactPoint.url` เป็นเพจ
- i18n 3 ภาษา: `contactEmailDesc/Button` → `contactFacebookDesc/Button` (rename ครบทั้ง 3 dict + call site — `TranslationKey` มาจาก th.ts ถ้า rename ไม่ครบ tsc จะฟ้อง) · ลบคีย์ตาย `contactSocialSoon*`
- **Verify**: tsc 0 · lint 0 error · test 837 ผ่าน · build ผ่าน · เปิดดูจริง /contact + /about (ลิงก์ถูกต้อง 3 จุด · ไม่มีคำว่าอีเมลเหลือ · schema ไม่มี email แล้ว)
- ⚠️ **ค้างให้เบสเคาะ**: schema ยังบอกว่ารองรับ th+en — ถ้าเพจตอบไทยอย่างเดียวควรตัด en ออก · `sitemap.ts` `STATIC_CONTENT_UPDATED` ยังเป็น 5 ส.ค. (แก้แล้วจะ re-date หน้าอื่นที่ไม่ได้แก้ด้วย) · คำว่า "ทักแชท" มี loanword "แชท" — ถ้าอยากไทยล้วนเปลี่ยนเป็น "ทักหาทีมงาน" ได้

## 🖼️ รูปชุดในแถบ "ชุดการ์ดวันพีชล่าสุด" (2026-08-08 · commit+push master แล้ว)
เบสถาม "ให้มันแสดงรูปของชุดด้วยดีมั้ย" → ทำให้ดูแล้วที่ :3000 (revert ง่าย แก้ 2 ไฟล์)
- **ข้อเท็จจริง**: `boxImageUrl` = null ทั้ง 51 ชุด → ไม่มีรูปกล่องจริงในระบบ ใช้ **การ์ดแพงสุดของชุดนั้น** เป็นหน้าปกแทน (วิธีเดียวกับ /guide และ /guide/sets)
- **ทำเป็นรูปเล็กในชิปเดิม ไม่ใช่การ์ดใหญ่** — เพราะแถบนี้อยู่เหนือตารางราคา: มือถือสูง 68→78px (ยังแถวเดียวเลื่อนได้ ไม่ล้นจอ) · จอ 1280 สูง ~168→198px (ยัง 3 แถวเท่าเดิม) ถ้าทำเป็นไทล์ใหญ่จะกินเพิ่ม ~200px ดันตารางหลุดจอแรก (เหตุผลเดียวกับที่แถบนี้เป็น rail แต่แรก)
- `lib/data/home.ts`: `withSetCovers()` ยิงคิวรีเดียวเฉพาะ 12 ชุดบนจอ · มี try/catch — ถ้าคิวรีล่ม เสียแค่รูป ลิงก์ (หน้าที่หลักของแถบ = SEO) ยังอยู่
- `HomeSetStripItem.coverUrl` เป็น optional → guard test เดิมที่ส่งแค่ {code,name} ไม่พัง
- **เบสสั่งเพิ่ม "ขอแค่ 2 บรรทัด"** → `twoRowVisibility(index)` ในคอมโพเนนต์: ความกว้างชิปขึ้นกับชื่อชุด จำนวนคงที่จึงตกบรรทัดไม่เท่ากันทุกจอ — วัดจากชื่อจริง 12 ชุดแล้วล็อกเป็น **sm 4 · md 6 · lg 8 · xl 10 ชิป = 2 บรรทัดเป๊ะทุกจอ** (คอลัมน์เนื้อหาตันที่ 1216px ดังนั้น 10 คือเพดานจริงบนทุกมอนิเตอร์ ใบที่ 11-12 ยังไงก็เป็นบรรทัดสาม)
  - ชิปที่ซ่อน **ยังอยู่ใน HTML** (`hidden` ไม่ใช่ลบ) — ลิงก์ยังส่งน้ำหนักไปคลัสเตอร์ชุดตาม SEO plan §3.1 · มือถือยังเลื่อนเห็นครบ 12 · และมี "ดูชุดทั้งหมด" อยู่แล้ว
- Verify: tsc 0 · lint 0 error · test 837 · build ผ่าน · **วัด DOM จริง 6 ความกว้าง**: 375 (12 ใบ แถวเดียว เลื่อนได้ ไม่ล้นจอ) · 660 = 4/2 แถว · 820 = 6/2 · 1100 = 8/2 · 1280 = 10/2 · 1700 = 10/2
- ⚠️ บั๊กเดิมยังอยู่: การ์ดลายพิเศษ ~60% ใช้รูปใบธรรมดา → ปกบางชุดจะไม่ใช่ลายพิเศษ (หายเองเมื่อ scraper ดึงลายจริง)

## 📦 รูปกล่อง/ซองชุดการ์ดของจริง — ทั้งเว็บ (2026-08-08 · commit+push master แล้ว)
เบสสั่ง "อยากได้รูปชุดการ์ดจริงๆ ไปหามาหน่อย และทุกหน้าที่แสดงชุดการ์ดก็ดึงใช้ด้วย บางส่วนไม่มีรูปเลย พวก guide"

**แหล่ง (verify แล้วทุกรหัส ไม่ได้สุ่ม)**: เว็บทางการ Bandai เก็บรูปแพ็กเกจไว้ที่ path ที่เดาจากรหัสชุดได้ตรงๆ — `/renewal/images/products/boosters/{code}/img_item01.webp` (op/eb/prb) และ `.../decks/{code}/...` (st) · ยิงครบ 51 รหัส → **200 image/webp 50 ชุด · 404 แค่ `don`** (DON!! Card Collection ไม่ใช่สินค้าขายเป็นกล่อง จึงไม่มีแพ็กเกจให้หา — คงรูปการ์ดแทนตามเดิม) · ไฟล์ 670×670 WebP **มี alpha channel จริง (พื้นหลังใส)** ไม่ซ้ำกันสักไฟล์

**สคริปต์ใหม่ `scripts/upload-set-images.ts`** (โคลนคอนเวนชันจาก `upload-images.ts`): โหลด → ขึ้น R2 ที่ `sets/{code}.webp` (prefix `sets/` จำเป็น เพราะรูปการ์ดอยู่ที่ `{setCode}/{cardCode}.png` ถ้าไม่มี prefix จะชนโฟลเดอร์ชื่อเดียวกัน) → เขียน `boxImageUrl` · มี `--dry-run` (ไม่มีในสคริปต์การ์ด — เพิ่มเพราะ DATABASE_URL ชี้ Supabase ตัวจริง) · `--sets=` · `--force` · ข้าม `don` พร้อมเหตุผล
**รันไปแล้ว**: dry-run 50/50 ผ่าน → ลองจริง 2 ชุด → เปิดดูรูปยืนยันเป็นซองจริง → รันครบ **50/51 อยู่ใน DB แล้ว** (เขียน production เป็นการเติมช่องว่าง ไม่ทับของเดิม)

**หน้าที่ติดเองทันทีหลังเติมข้อมูล (ไม่ต้องแก้โค้ด)**: หน้าชุด hero + ไทล์ · /opcg/sets · /guide/sets · ภาพรวมตลาด · ช่องเลือกชุดหน้าแรก/ค้นหา/drop-calc · admin
**หน้าที่ต้องต่อสายเพิ่ม (แก้แล้ว)**: `/guide` hub (เพิ่ม select + ช่องเป็น `aspect-square`) · `/guide/getting-started` (สตาร์ทเตอร์เด็คเคยเป็นชิปตัวอักษรล้วน ตอนนี้มีรูปกล่อง) · `/guide/buying` (ข้อมูลมีอยู่แล้วแต่ไม่มีใครเรนเดอร์ → ใส่ `GuideFigure`) · `/search` SSR · drop-calculator (หน้าคำนวณโอกาสจากกล่อง แต่ไม่เคยมีรูปกล่อง) · admin sets · แถบชุดหน้าแรก
**บั๊กที่เจอระหว่างทาง**: `api/sets/route.ts` คืน `boxImageUrl` แต่ฝั่งรับอ่าน `imageUrl` → ช่องเลือกชุดในพอร์ต/card-picker **ว่างตลอดกาล** ต่อให้เติมข้อมูลแล้ว — map ที่ route จุดเดียว แก้ครบทุก caller

**เรื่องสัดส่วนรูป (วัดจริงทั้ง 50 ไฟล์)**: canvas เป็นสี่เหลี่ยมจัตุรัส แต่ตัวสินค้าข้างในเป็นแนวตั้ง (ซอง 0.495 · กล่องเด็ค 0.566 · การ์ด 0.716) มีขอบใสซ้ายขวา → `object-cover` ในกรอบทรงการ์ดตัด**เฉพาะขอบใส** ทำให้ซองดูใหญ่ขึ้น ใช้ได้ 47/50 · ยกเว้น `st10/st13/st21` (กล่อง EX ทรงกว้าง) ที่โดนตัดจริง → แยกเป็น set ค่าคงที่ให้ใช้ `object-contain` เฉพาะ 3 ตัวนี้ **ไม่ได้กวาดเปลี่ยนทั้งเว็บ**

**Verify**: tsc 0 · lint 0 error · test 837 · build ผ่าน · **เปิด production server จริงตรวจ 10 หน้า** นับรูปกล่องในหน้า: / =62 · /opcg/sets =100 · /guide/sets =24 · /guide =16 · /guide/getting-started =12 · /guide/buying =2 · หน้าชุด op15 =28 · market-overview =12 (client) · search =22 (client) · drop-calc ขึ้นหลังเลือกชุด · **หน้า `don` ที่ไม่มีรูปกล่อง = 200 ไม่มี img พัง**
⚠️ ค้าง: แพ็กเกจเป็นภาษาญี่ปุ่นทั้งหมด (ไม่มีเวอร์ชัน EN/TH บนเว็บทางการ) · admin แก้ค่าเป็นค่าว่างไม่ได้ (null ถูก filter ทิ้ง) · `src/lib/constants/sets.ts` เก่า ขาด op16/op17/eb05/st30+

## NEXT
0. ~~/opcg/trending รื้อ UI~~ — **เสร็จแล้ว 2026-08-09**: เส้นตาราง + กล่องมูฟเวอร์ + ไอคอนการ์ด "น่าเก็บ" (ดู section 🎨) commit+push master แล้ว
1. ~~commit+push~~ — **เสร็จแล้ว 2026-08-08**: งานค้างทั้งหมด (สะกดวันพีช · _p1 sweep · guide visual · about บริษัท · guide width · ApiError trap) commit บน `chore/next-16.3` แล้ว push เข้า master ตามคำสั่งเบส → Vercel จะ deploy production เอง
2. **SEO-OPS บน Vercel (ค้างมานาน — ตอนนี้สำคัญสุด)**: ตั้ง `NEXT_PUBLIC_APP_URL` เป็นโดเมนจริง (canonical/og:url ทั้งเว็บชี้ตามค่านี้) · Google Search Console + ใส่ `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` · submit /sitemap.xml
3. เช็คบน production หลัง deploy: ป้าย "อัปเดตล่าสุด" ต้องเป็นวันปัจจุบัน (ถ้าเก่า = cron scrape มีปัญหา) · ลองแชร์ลิงก์ผ่าน LINE/FB ดู preview ใหม่
4. งาน SEO เชิงเนื้อหาระยะถัดไป (นอกแผน 3 รอบ — ทำเมื่อพร้อม): เขียนบทความ blog 2-3 ชิ้นแรกแล้วปลด noindex · backfill `releaseDate` ของชุด (FAQ "ชุดล่าสุด" จะได้ตอบแม่น) · พิจารณา character hub (/cards/character/luffy) เก็บ query ระดับตัวละคร · แยก URL /en /jp + hreflang ถ้าจะ rank ภาษาอื่น
