# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## ✅ งานรอบนี้ — **navbar D2 ขึ้นเว็บจริงแล้ว** (เบสเคาะ 2026-08-29)

**เบสสรุป:** "เอา D2 นั้นแหละ ทำเลย" → ลงของจริงเรียบร้อย

### โครงที่ขึ้นแล้ว — 132px → **104px**
```
[48] เกม▾ › ชุด▾(รูปกล่อง) ‖ การ์ดทั้งหมด · มูลค่ารวม · JPY/THB · สายพาน ‖ อัปเกรด · แชท · แจ้งเตือน · โปรไฟล์
[56] โลโก้ Meecard · เมนู 4 ลิงก์ ⟷ พอร์ต · รายการโปรด · Honey · ช่องค้นหา 320px
```
**คืนพื้นที่ให้เนื้อหา 28px ทุกหน้า**

### ไฟล์ที่แก้ (6 + เทสต์ 2)
| ไฟล์ | ทำอะไร |
| --- | --- |
| `header-market-ticker.tsx` | ยุบแถบชีพจร + แถวโลโก้เหลือแถวเดียว h-12 · catalog control นำแถว · **สถิติหลบตามความกว้างจอ** |
| `header.tsx` | โลโก้ย้ายมานำแถวเมนู · ค้นหา `w-80` คงที่ 320px |
| `globals.css` | `--chrome-h` md: 8.25rem → **6.5rem** |
| `set-picker.tsx` | `SetPickerItem` เพิ่ม `boxImageUrl` (API ส่งมาอยู่แล้ว) |
| `header-catalog-control.tsx` | ปุ่มชุด desktop โชว์**รูปกล่องจริง** + รหัส + ชื่อชุด · แคบลง/ซ่อนชื่อที่จอเล็ก |
| `header-mobile.tsx` | **ซ่อนปุ่มเลือกเกม/ชุดเมื่อ scrolled** |
| เทสต์ 2 ไฟล์ | อัปเดต topology test ล็อกโครงใหม่ (ไม่ได้ลบ) |

### ⚠️ ปัญหาที่เจอตอนลงจริง (แก้แล้ว)
พอยุบเหลือแถวเดียว **ที่ 1024px แถบล้นและสายพานหายสนิท** — แก้ด้วย responsive 3 ชั้น:
| จอ | สถิติที่เห็น | ปุ่มชุด | **สายพานได้** |
| --- | --- | --- | --- |
| 1024 | มูลค่ารวม | รหัสอย่างเดียว (w-40) | **230px** |
| 1280 | มูลค่ารวม + การ์ดทั้งหมด | รหัส + ชื่อชุด (w-56) | **304px** |
| 1600 | ครบ 3 ตัว | รหัส + ชื่อชุด | **422px** |

`มูลค่ารวม` อยู่ได้นานสุดเพราะเป็นตัวเลขเดียวที่เป็นลิงก์ไป market-overview

### ตรวจรับครบ
- `npx eslint src` **0 errors** · `npm run test` **918 ข้อผ่าน** · `npm run build` ผ่าน
- **วัดจริงบน production server ที่ 1024 / 1280 / 1600** — ไม่มีแถวไหนล้น · chrome 105px ทุกจอ
- **หน้าชุด op15**: ปุ่มชุดโชว์รูปกล่องจริง + ชื่อชุด
- **มือถือ 375**: เลื่อนลง → ปุ่มเลือกเกม/ชุดหาย · ค้นหา+โลโก้ยังอยู่ · เลื่อนขึ้น → กลับมา

### หมายเหตุ dev server
`npm run dev` อาจเสิร์ฟ CSS เก่า (Turbopack cache ที่ `.next/dev/`) ถ้าความสูงยังเป็น 132px ให้ลบโฟลเดอร์นั้นแล้วเปิดใหม่

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิดดูของจริง** — production server อยู่ที่ `http://localhost:3016`
2. พอใจแล้วเปิด PR `feat/navbar-ecom-proto` → master (มีทั้ง proto และของจริง)
3. มือถือยังไม่ได้ออกแบบใหม่ (แตะแค่ให้ปุ่มชุดหายตอนเลื่อน)
4. ของค้างเดิม: `priceValidUntil` ใน `json-ld.ts` · `getHomeData().rarityRows` query ทิ้งเปล่า
