# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · hook โหลดไฟล์นี้ทุก session · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-06 — **จัดหน้าแรกใหม่หลังใส่เนื้อหา SEO (2 รอบ)** บนสาขา `feat/seo-pillar-content` (verify ครบ · ยังไม่ merge) · รอบก่อนหน้า = สเกลชั้นซ้อน (z-index) ทั้งเว็บ (ยังไม่ merge เหมือนกัน)

## 🔨 รอบล่าสุด — หน้าแรกหลังใส่ SEO

**โจทย์จากเบส**: "หน้าแรกปรับให้ดูดีกว่านี้ได้มั้ย หลังจากที่เราเพิ่มข้อมูล SEO มา"

**วิธีดู**: build production แล้วยิง Chrome จริงเข้าไปวัด (390px + 1440px) ไม่ได้เดาจากโค้ด

**สิ่งที่เจอและแก้ (วัดผลได้ทุกข้อ)**
1. **แถบชุดการ์ดกินจอแรกทั้งจอ** — 12 ปุ่มตัดบรรทัดเป็น **7 แถว = 328px** สูงกว่า hero ทั้งก้อน ดันตารางราคาหลุดจอแรกไปเลย → เปลี่ยนเป็น**แถวเดียวเลื่อนซ้าย–ขวา**บนมือถือ (`no-sb` + `overflow-x-auto` แบบเดียวกับแถบเกรด) ส่วน `sm` ขึ้นไปยังตัดบรรทัดเหมือนเดิม · **328px → 68px** · ลิงก์ทั้ง 12 ยังอยู่ครบใน HTML
2. **"ชุดการ์ดวันพีซล่าสุด" โชว์แต่ starter deck** — st29…st18 ล้วน ไม่มี booster สักชุด. **ต้นเหตุ: `releaseDate` เป็น NULL ทั้ง 51 ชุด** การเรียงตามวันที่เลยไม่ทำงาน ตกไปที่ tiebreak "โค้ดย้อนอักษร" ซึ่ง st* ชนะ op* เสมอ → เพิ่มลำดับตามชนิดชุด (BOOSTER ก่อน) คั่นก่อนโค้ด · ตอนนี้ขึ้น **op15 → op04** แล้ว (ยืนยันด้วยการรันจริงกับ DB)
3. **ชื่อการ์ดในลิสต์มือถือถูกตัด 16 จาก 20 แถว** — ช่องชื่อเหลือ **75px** จากจอ 390px เพราะมีกราฟจิ๋ว 48px มาแทรกกลาง สี่แถวติดกันอ่านว่า "Monkey.D…" เหมือนกันหมด → เอากราฟจิ๋วออกจากแถวมือถือ (ตารางเดสก์ท็อปยังมีคอลัมน์ "กราฟ 30 วัน" ของตัวเอง) · ช่องชื่อ **75px → 135px** · ตัด **16/20 → 6/20** (ที่เหลือคือชื่อยาวจริงแบบ "(Parallel)")
4. **การ์ด 3 ใบใต้ตารางเขียนมือ ซ้ำภาษากับ `RelatedPages` ที่อยู่ถัดลงไป 64px** — ใช้ `surface-1 hairline rounded-xl p-6` ซึ่งคือ `Surface variant="hero"` (สงวนไว้ 1 ใบต่อหน้า) → เปลี่ยนมาใช้ `Surface variant="outline"` ไอคอนซ้าย + chevron ทรงเดียวกับ "สำรวจเพิ่มเติม"
5. **กล่องอธิบายราคาเป็นการ์ดทึบใบใหญ่** ทั้งที่ข้างในเป็นเนื้อความล้วน กดไม่ได้ (การ์ด 3 ใบเหนือมันกดได้) → เอากล่องออกเหลือ prose + จำกัดความกว้าง `max-w-3xl` (เดิมยาวเต็ม container บนเดสก์ท็อป)
6. **ย่อหน้าเหนือตารางยาว 6 บรรทัดบนมือถือ** และครึ่งหลังเป็นวิธีใช้ UI ที่พิมพ์ไว้*เหนือ*ปุ่มที่มันอธิบาย → ย่อ copy ในที่เดิมทั้ง TH/EN/JP (คีย์เวิร์ด · "วันพีช" · Yuyu-tei · ตัวเลขนับ ยังอยู่ครบ — มีเทสต์ยาม)
7. ระยะห่างท้ายหน้า `space-y-16` → `space-y-10 sm:space-y-14`

**รอบสาม (2026-08-06) — ยุบบล็อกลิงก์กำพร้าใต้ FAQ หน้าแรก**
10. **"อ่านต่อจากคำถามด้านบน" หายไปทั้งก้อน** — เกิดจากข้อจำกัดเดิมที่ `FaqSection` รับคำตอบเป็นข้อความเปล่า ฝังลิงก์ไม่ได้ ลิงก์ 4 ตัวเลยกองเป็นบล็อกแยก · แก้ที่ต้นเหตุ: เพิ่ม `link` (optional) ใน `FaqItem` — ลิงก์ "อ่านต่อ" render ท้ายคำตอบในกล่อง ไม่เข้า JSON-LD (`faqJsonLd` อ่านแค่ question/answer) ไม่กระทบ 17 หน้าที่ใช้ FaqSection อยู่ · ลิงก์ 4 ตัวย้ายเข้าคำตอบข้อที่เกี่ยว ตัดวลี "ในลิงก์ด้านล่าง" ออกจากคำตอบทั้ง 3 ภาษา · ลบ `buildHomeFaqLinks` ทิ้ง · เทสต์ยาม: วลีเก่าห้ามโผล่ + ลิงก์ต้องอยู่ในคำตอบ · ยืนยันหน้าจริง: ลิงก์ครบ 4 ใน `<details>`, บล็อกกำพร้า = 0

**รอบสี่ (2026-08-06) — จัดหน้า /opcg/most-expensive เข้าภาษาเดียวกับเว็บ + ปรับคำ SEO**
11. เบสถามว่าหน้านี้จำเป็นมั้ย → สรุป: เก็บ (เป็น landing ดักคำค้น "การ์ดวันพีซที่แพงที่สุด" — คนละ intent กับหน้าแรก แบบเดียวกับที่ CMC แยก Top Gainers ทั้งที่หน้าแรกก็เรียงมูลค่า) แต่ UI ค้างยุคเก่า → จัดใหม่:
   - **โค้ดการ์ดเลิกโชว์ไส้ใน `_p3`** — เพิ่ม `baseCode` เข้า data + ใช้ทุกจุด (ลิสต์/ตาราง/intro/meta/FAQ/JSON-LD name) · URL ยังใช้โค้ดเต็ม (ที่อยู่จริงของ variant)
   - **ลบ `ChangeCell` เขียนมือ (ลูกศร ▲▼ ที่ทั้งเว็บเลิกใช้) → `PriceTag` canonical** แบบเดียวกับตารางหน้าแรก (plain, no arrow)
   - **แถวมือถือใช้ grammar เดียวกับ `mobile-card-item`**: rank · รูปไม่ครอป (aspect 63/88 object-contain — เดิม object-cover หัวการ์ดขาด) · ซ้าย identity / ขวา money stack · ไม่มีกล่องครอบลิสต์ · ราคา `font-price`
   - **intro เลิกใส่กล่อง** → prose เปล่า `max-w-3xl` แบบ price-explainer หน้าแรก · การ์ด topByRarity เพิ่มรูป + ทรง identity เดียวกัน
   - **คำ SEO**: subtitle เลิกพูดแง่ลบ ("ไม่ใช่บทความเก่า…" → "จัดใหม่อัตโนมัติทุกวัน") · **intro ยุบ 3 ย่อหน้า → 1 ย่อหน้า** (เบสชี้ว่า "ยาว เยอะไป" — เก็บเฉพาะคำตอบ query + คีย์เวิร์ด + ขนาด + ความสด; ที่ตัด (แหล่งราคา/คำเตือนราคาแกว่ง) มีใน FAQ ใต้ตารางอยู่แล้ว) · title/description เดิมดีอยู่ ไม่แตะ
   - verify ครบ (tsc/test 741/lint/build) + เปิด Chrome จริงทั้ง 390/1440 + grep HTML ยืนยันไม่มี `_p*` ในข้อความ

**รอบสอง (2026-08-06) — เบสสั่งเพิ่ม 2 เรื่อง**
8. **แถบ "ชุดการ์ดวันพีซล่าสุด" — ลองย้ายลงท้ายหน้าแล้วเบสให้เอากลับ** ⚠️ decision จบแล้ว: **อยู่ข้างบนใต้ hero เหมือนเดิม** (เบสดู preview แล้วสั่งกลับ 2026-08-06) โดยคงร่างมือถือแบบแถวเดียวเลื่อนได้ (68px) ไว้ — อย่าย้ายลงล่างอีก
9. **กวาดชื่อแหล่งราคา (Yuyu-tei / SNKRDUNK) ออกจาก "เนื้อความ" ทั้งเว็บ = ศูนย์** (owner decision 2026-08-06 สั่งเป็นขั้น: หน้าแรก → footer → "เอาออกให้หมด") — จุดที่แก้ทั้ง 3 ภาษา: meta หน้าแรก · `SITE_DESCRIPTION`+`keywords` ใน root layout · ย่อหน้าเหนือตาราง · การ์ดฟีเจอร์ · กล่องอธิบายราคา (`seoPriceP2a/b`+`<strong>`→`seoPriceP2`) · FAQ หน้าแรกทุกข้อ **รวม `seoFaq2A` ที่เคยตกลงคง — คำสั่ง "เอาออกให้หมด" ครอบ** · footer (`footerSources` → "ข้อมูลราคาจากตลาดญี่ปุ่น · ภาพการ์ด © BANDAI") · หน้า about (`aboutSourcesBody`) · guide ทุกหน้า (getting-started `P2a/b`→`P2`, rarities ลบ Yuyu-tei ออกจากรายการแหล่งอ้างอิง, buying ลบออกจาก `guideBuySources`+ตารางร้าน, tips, FAQ) · seo/copy per-card + per-set + site + tools · help tooltips (`marketPriceHelp` ฯลฯ) · ปุ่ม external ใน card detail เปลี่ยน label เป็นกลาง ("ดูที่ร้านต้นทาง" / "เช็คราคาตลาดเกรด") แต่ลิงก์ยังทำงาน · ลบ key ตาย `guideBuyShopYuyutei*`+`guideRaritySourceYuyuteiDesc` · แทนทุกจุดด้วย "ตลาดญี่ปุ่น/ตลาดการ์ดเกรด" ไม่ใช่ลบสาระ · เทสต์ยาม 2 จุด (home-seo + per-card FAQ: แบรนด์ = 0) · ยืนยันด้วย curl 8 หน้า = 0 mentions
   **ตั้งใจไม่แตะ**: (ก) ตารางประวัติซื้อขาย/แหล่งราคาใน card detail — โลโก้+ชื่อ+ลิงก์แหล่งต่อแถวเป็น data attribution (หลายแหล่ง: Yuyutei/SNKRDUNK/Mercari/eBay) เอาออก = ตารางบอกไม่ได้ว่าราคาแถวไหนมาจากไหน ถ้าเบสอยากเอาออกด้วยต้องออกแบบใหม่ (ข) หน้า admin ภายใน (เครื่องมือจับคู่ข้อมูลต้องระบุแหล่ง) (ค) โค้ดภายใน/scraper/enum

**ผลรวมที่วัดได้ (มือถือ 390px)**: หน้าเต็ม **6,768px → 6,118px** · ตารางราคาเริ่มที่ y=705 → **y=445** · ท้ายหน้า SEO 2,704px → 2,363px

**Verify**: tsc 0 · lint 0 error (29 warning เดิม นอก scope) · test **134 files / 740 tests** ผ่าน (เพิ่มเทสต์ใหม่ 1 ตัวยามไม่ให้มีคอลัมน์ที่ 3 กลับมาแทรกในแถวมือถือ) · build 209 หน้า · ยิง Chrome จริงบน production build ทั้ง 390px/1440px + เช็ก `/search` ที่ใช้ตารางเดียวกัน (0 error)

**⚠️ ที่ยังไม่ได้แก้ (ต้องให้เบสตัดสิน)**
- `releaseDate` ว่างทั้ง 51 ชุด — ที่แก้ไปคือ**กลบอาการ**ด้วยการเรียงตามชนิดชุด. ทางแก้จริงคือ backfill วันวางขาย (เขียน DB → **ต้องขออนุญาต**) แล้วการเรียงตามวันที่จะทำงานเองทันที
- ยุบการ์ด 3 ใบเข้า `RelatedPages` เป็น component เดียวจริงๆ (ตอนนี้แค่ทำหน้าตาให้ตรงกัน) — ต้องเพิ่ม prop `description` ในของกลาง กระทบหน้าอื่นที่ใช้อยู่

## 🔨 รอบก่อนหน้า — ชั้นซ้อน (z-index) ทั้งเว็บ

**อาการที่เบสเจอ**: กดอะไรที่เด้ง popup แล้วแถบเมนูทะลุขึ้นมาทับ — reproduce ได้จริงด้วย Chrome จริง (playwright-core ยิงเข้า Chrome ของเครื่อง) หน้าแรกจอ 390px: พิมพ์ในช่องค้นหา → **แถบโฆษณาลอย + แถบแท็บล่างทับรายการผลลัพธ์** และกดโดนแท็บแทนการ์ด

**ต้นเหตุ**: ทั้งเว็บไม่มีสเกลชั้นซ้อน — header (desktop+mobile), แถบแท็บล่าง, dialog, sheet, popover, menu, tooltip **อยู่ที่ `z-50` เท่ากันหมด** เลยตัดสินกันด้วยลำดับใน DOM ซึ่งแถบแท็บล่าง render ท้ายสุดใน `layout.tsx` → ชนะทุกครั้ง. ซ้ำร้าย `position: sticky/fixed` ที่มี z-index สร้าง stacking context → ตัวเลขที่เขียนข้างในถูก re-base ยกไม่ขึ้นไม่ว่าใส่เท่าไหร่

**สิ่งที่ทำ**
- **สเกลกลางใน `src/app/globals.css` (`@theme`)**: `z-sticky 30 · z-ad 35 · z-floating 40 · z-chrome 50 · z-dropdown 55 · z-modal 70 · z-popup 80 · z-toast 90 · z-skip 100` — เขียนกติกาไว้ใน `AGENTS.md` §Stacking layers แล้ว
- **ย้ายทุก fixed/sticky มาใช้ token** (~20 ไฟล์): header ×2 · bottom-nav · admin/seller shell · floating ad · compare bar · scroll-to-top · sticky buy · profile CTA · drop-calc tray · admin bulk/save bar · sticky sub-nav 4 จุด
- **kit กลาง**: dialog/sheet → `z-modal` (พื้นหลังกับตัว modal **ชั้นเดียวกันตั้งใจ**) · popover/menu/tooltip/select → `z-popup` · select เลิกใช้ `z-[110]` และลบคอมเมนต์เก่าที่บอกผิดว่า modal เป็น `z-[100]`
- **บั๊กจริง 4 ตัวที่ผู้ใช้เจอ**
  1. ช่องค้นหาหน้าแรก — `<section relative z-30>` เป็นเพดานขังอยู่ → เอา z-30 ออก แล้วให้ตัว wrapper ยกเป็น `z-dropdown` เฉพาะตอนเปิด
  2. `SetPicker` `z-30` → `z-dropdown` (แก้จุดเดียว ครอบคลุมหน้าแรก/ค้นหา/drop-calculator/watchlist/portfolio)
  3. toast ของ honey — เดิม `z-50` และอยู่ใน "แถบเดียวกับ" แถบแท็บพอดี → **มองไม่เห็นเลย 100%** ตอนนี้ `z-toast` + ยกให้พ้นแถบแท็บ/โฆษณา
  4. กระดิ่งแจ้งเตือน — แผงเดิมเป็น div `fixed z-50` ที่เกิดในหัวเว็บ sticky (ขังถาวร ไม่มีเลขไหนช่วยได้) → เปลี่ยนเป็น `Popover` canonical (portal ออก body, ได้ Escape/โฟกัสฟรี)

**บั๊กที่ทำพังเองระหว่างแก้แล้วแก้กลับ** (สแกนเจอ ไม่ได้หลุดออกไป): ยกช่องค้นหาไป `z-popup` (80) ทำให้มันทะลุ Cmd+K palette (70) → เลยเพิ่มชั้น `z-dropdown` 55 ไว้เหนือ chrome แต่ใต้ modal · และการแยก backdrop ไปชั้น 60 ทำให้ dialog ซ้อน dialog เสียฉากหลัง (`blurBackdrop` ของ FilterModal) → รวมกลับเป็น `z-modal` ชั้นเดียว

**Verify** — tsc 0 · lint 0 error (29 warning เดิม นอก scope) · test 134 files / 739 tests · build 209 หน้า · **ยิง Chrome จริงบน production build** ทั้ง 390px และ 1440px: ผลลัพธ์ค้นหา/set picker/แผงแจ้งเตือน อยู่เหนือแถบแท็บ · palette ทับช่องค้นหาถูกต้อง · dialog ซ้อน dialog ฉากหลังคลุมถูก · select ใน dialog อยู่บนสุด

## ⚠️ เรื่องคอมมิต — ต้องดู

คอมมิต **`d186d1f`** และ **`13121de`** (ข้อความว่า card-detail) **กวาดงาน z-index ไปด้วย** เพราะคอมมิตตอนที่ยังแก้ไม่เสร็จ — โค้ดครบ ไม่หาย แต่ประวัติปนกัน ถ้าอยากได้คอมมิตแยกต้อง `git rebase -i` แยกเอง

## ⏭ NEXT

1. **เบสเปิดดูด้วยตา** (รายละเอียดข้อ "จุดที่ต้องดู" ข้างล่าง) แล้วค่อย merge — ตอนนี้มี 2 รอบรวมกันในสาขาเดียว (z-index + หน้าแรก)
2. **ตัดสินใจเรื่อง `releaseDate` ว่างทั้ง 51 ชุด** — ถ้า backfill ได้ การเรียง "ชุดล่าสุด" จะถูกต้องจริง ไม่ต้องพึ่งลำดับตามชนิดชุดที่ใส่ไว้ชั่วคราว (⚠️ เขียน DB = ต้องขออนุญาต)
2. **ยังไม่ได้แก้ — SetPicker ไม่ได้ portal** เลยยังโดนขังใน 3 ที่: (ก) ในกล่อง `FilterModal` โดน `overflow-y-auto` ตัด (desktop) (ข) `drop-calculator/card-picker.tsx:254` เป็น `div.sticky.top-32` — **sticky สร้าง stacking context แม้ไม่มี z-index** เลยยกไม่ขึ้น (ค) `admin/cards/cards-browser.tsx:299` `sticky z-20` แบบเดียวกัน → ทางแก้จริงคือเปลี่ยน SetPicker เป็น `Popover` แต่ต้องรักษาทรง "ปุ่ม+ลิสต์ติดกัน กว้างเท่ากัน" ที่เบสสั่งไว้ เลยยังไม่แตะ
3. ค้างเดิม: **SEO** สาขานี้ยังไม่ merge · บน Vercel ต้องตั้ง `NEXT_PUBLIC_APP_URL` เป็นโดเมนจริง + สมัคร Google Search Console → ใส่ `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` → submit sitemap
4. ค้างเดิม: เครื่องหมายเกม 3 แบบทั่วเว็บ (จุดสี header · `Layers3` MINE · `Gamepad2` picker) — ต้องเลือกอันเดียว
5. ค้างเดิม: ตัวเลือกเกมในพอร์ตโชว์ทั้งที่มีเกมเดียว
6. ค้างเดิม: จะใช้บล็อกจริงต้องสร้างตาราง BlogPost ก่อน (⚠️ migration = ต้องขออนุญาตเบส)

## 👀 จุดที่อยากให้เบสดูด้วยตาบน preview

**รอบหน้าแรก (ใหม่)**
- **แถบชุดการ์ดกลับมาอยู่ใต้ hero แล้ว** (ตามที่เบสสั่งหลังลองแบบท้ายหน้า) — มือถือเป็นแถวเดียวเลื่อนซ้าย-ขวา เห็นทีละ ~2 ชุด ต้องปัดนิ้วดูที่เหลือ ถ้าไม่ชอบการปัด ทางเลือกคือซ่อนชื่อชุดเหลือแต่โค้ด (op15, op14, …) แล้วตัดบรรทัด จบใน 2 แถว
- **สำนวนใหม่ที่แทนชื่อแบรนด์ด้วย "ตลาดญี่ปุ่น/ตลาดการ์ดเกรด" ทั่วเว็บ** — จุดสำคัญที่ควรอ่านทวน: FAQ หน้าแรก · หน้า about · guide/getting-started ย่อหน้าราคา · guide/buying (Yuyu-tei หายจากตารางร้าน+แหล่งอ้างอิงแล้ว) · ปุ่มใน card detail ("ดูที่ร้านต้นทาง" / "เช็คราคาตลาดเกรด")
- **ตารางแหล่งราคาใน card detail ยังโชว์โลโก้/ชื่อแหล่งต่อแถว** — ตั้งใจคงไว้เพราะเป็นป้ายบอกที่มาของข้อมูลแต่ละแถว (เหมือน CMC โชว์ชื่อ exchange) ถ้าอยากเอาออกด้วยต้องคุยเรื่องออกแบบใหม่
- **กราฟจิ๋วหายไปจากลิสต์มือถือ** (เดสก์ท็อปยังมี) — แลกมาเพื่อให้ชื่อการ์ดไม่ถูกตัด ถ้าเบสอยากได้กราฟคืน ต้องยอมให้ชื่อสั้นลงอีกครั้ง
- **"ชุดการ์ดวันพีซล่าสุด" ตอนนี้ขึ้น op15 → op04** ไม่มี starter deck แล้ว — เช็กว่าตรงกับที่ตั้งใจ

**รอบ z-index (เดิม)**
- **หน้าแรก มือถือ** — พิมพ์ค้นหาแล้วรายการยาวจะ**คลุมทับแถบแท็บล่าง**บางส่วน (เมื่อก่อนแท็บทับรายการ) ถ้าอยากให้รายการหยุดเหนือแถบแท็บ ต้องปรับ `max-h-[60vh]` ในช่องค้นหา
- **หน้าแรก desktop** — ถ้าเปิดรายการค้นหาค้างไว้แล้วเลื่อนหน้า ช่องค้นหา+รายการจะ**ลอยทับหัวเว็บ** (เมื่อก่อนมุดใต้หัวเว็บแต่โดนตัด) ถ้าไม่ชอบ ทางแก้คือปิดรายการตอนเลื่อน ไม่ใช่ลดเลขชั้น
- **กระดิ่งแจ้งเตือน** — เปลี่ยนกลไกทั้งก้อนเป็น Popover: ขอบซ้ายบนมือถือ 16px เท่าเดิม แต่ขอบขวาเหลือ ~22px (เดิม 16px) · ขอบกล่องเป็น ring แทน border · ลองกด Esc / แตะนอกกล่อง / หมุนจอแนวนอน
- **toast honey** — ลอยสูงขึ้นจากเดิมมาก (พ้นแถบแท็บ+โฆษณา) เพราะเมื่อก่อนมองไม่เห็นเลย
- **admin — ตัวเลือกเดือนในหน้า raffle**: ฉากรับคลิกปิดตอนนี้คลุมหัว admin ด้วย (คลิกหัวเว็บ = ปิดตัวเลือกก่อน 1 ครั้ง)

## 🔒 การตัดสินใจที่ต้องรักษา (z-index)

- **`z-dropdown` (55) อยู่เหนือ `z-chrome` (50) โดยตั้งใจ** — dropdown ที่ไม่ได้ portal ต้องชนะแถบแท็บให้ได้ แต่ต้องอยู่ **ใต้** modal เสมอ
- **`z-modal` คลุมทั้งฉากหลังและตัว modal** — ถ้าแยกฉากหลังลงไปชั้นล่าง dialog ที่เปิดซ้อน dialog จะเสียฉากหลังทันที (ลำดับที่ถูกต้องมาจากลำดับ DOM ของ portal ไม่ใช่จากตัวเลข)
- **`z-popup` (80) อยู่เหนือ modal** เพราะ select/menu เปิดจากในกล่อง modal ได้
- **ห้ามแก้ dropdown ที่ถูกขังด้วยการเพิ่มตัวเลข** — ถ้ามันอยู่ใน `sticky` / `transform` / `overflow-hidden` ต้อง portal (ใช้ `Popover`) เท่านั้น
- sonner (toast กลาง) ใช้ชั้นของตัวเองสูงกว่าทุกอย่าง — ปล่อยไว้ ตั้งใจ
- `/proto/**` ไม่แตะ (มี shell ของตัวเอง ไม่ปนกับ chrome หลัก)

## 🔒 การตัดสินใจเดิมที่ยังมีผล (SEO / UI)

- SEO copy อยู่ใน `src/lib/seo/copy/*.ts` ไม่ใช่ dictionary i18n · ห้ามตัดชื่อการ์ดใน title ด้วย "…" · ไม่ทำ `/guide/shops` · ห้ามเคลมว่า Bandai มี "ฟีเจอร์กันปลอม" · ห้ามใส่คำสัญญาเวลาตอบกลับ
- **`Card.nameTh` ไม่ใช่ไทยสักใบ** (3,838 ใบเป็นสำเนาอังกฤษ) แต่ `effectTh` เป็นไทยจริง 3,478 ใบ · `CardSet.nameTh` ว่างทั้ง 51 ชุด · `latestPriceThb` ว่างทั้งหมด (ต้องแปลงจาก JPY) · ตาราง BlogPost ไม่มีใน DB
- ราคาที่ผู้ใช้เห็นต้องตามสกุลเงินที่เลือก (มีเทสต์ยาม `display-currency-boundary.test.ts`)
- card picker ไม่มีเลขขั้น · ลิสต์มือถือ ซ้าย=identity ขวา=money stack · แถบแท็บ = แท็บ + ปุ่มของหน้าเท่านั้น · set เป็น control เด่นนอก FilterModal
- เครื่อง dev ตั้ง `NEXT_PUBLIC_BYPASS_AUTH=true` → ทดสอบหน้า auth-gated ต้อง build ใหม่ด้วย `false`
