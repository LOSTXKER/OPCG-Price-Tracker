# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-28 (รอบดึก) — **Navbar จัดใหม่แบบ CoinGecko: โลโก้ขึ้นแถวบน · ค้นหาเด่นกลางแถวล่าง · ตั้งค่าเข้าเมนูโปรไฟล์**

## ผลลัพธ์ — ยังไม่ commit

เบสส่งภาพ CoinGecko + CoinMarketCap มาเป็นแบบอ้างอิง แล้วสั่ง 4 อย่าง ทำครบแล้วทั้งหมด:

**1) ภาษา / สกุลเงิน / ธีม ย้ายเข้าเมนูโปรไฟล์**
- ถอดออกจากแถบบนหมด (เดิมกิน 3 ปุ่ม ~233px)
- อยู่ในเมนูโปรไฟล์แบบ CMC: `ภาษา  ไทย ›` · `สกุลเงิน  ฿ THB ›` · `ธีม [☀ 🌙 🖥]`
- **ธีมเป็นปุ่มสลับที่เห็นค่าปัจจุบันเลย** ไม่ต้องเปิดเมนูซ้อน · กดแล้วเปลี่ยนทันทีและเมนูไม่ปิด (ทดสอบจริงแล้ว)
- ใช้ `SegmentedControl` ที่เป็น component กลางตามกฎ AGENTS.md
- **คนที่ยังไม่ล็อกอินยังตั้งค่าได้** — เพิ่มปุ่มเฟือง ⚙ ในแถบบน (แบบ CoinGecko) เปิดเมนูตั้งค่าตัวเดียวกัน

**2) ทำให้รู้ว่ากดตรงไหนเพื่อตั้งค่า**
- ปุ่มโปรไฟล์เปลี่ยนเป็นทรงแคปซูลมีขอบ = **ไอคอนเมนู ☰ + รูปโปรไฟล์** (แบบ CMC) — เดิมเป็นรูปโปรไฟล์ลอยๆ ซึ่งอ่านว่า "บัญชีฉัน" ไม่ได้อ่านว่า "เมนูตั้งค่า"

**3) ช่องค้นหาเด่นขึ้น**
- ย้ายมาอยู่ **กลางแถวล่าง กว้าง 448px** เป็นกล่องมีขอบ พื้นสี card มีข้อความ "ค้นหาการ์ด..." และป้าย `/` — อ่านออกว่าเป็นช่องค้นหาตั้งแต่แรกเห็น
- เดิมเป็นปุ่มเล็กมุมขวา 176px
- สอดคล้องกับที่ hero ไม่มีช่องค้นหาแล้ว (ถูกถอดไปตั้งแต่รอบก่อน) — ตอนนี้ navbar เป็นทางเข้าค้นหาทางเดียว

**4) โลโก้ขึ้นไปแถวบน**
- Meecard อยู่หัวแถวบนแล้ว → แถวล่างว่างพอให้ช่องค้นหากว้าง

**เมนูโปรไฟล์ดีไซน์ใหม่:** หัวเมนูเป็นรูป + ชื่อ + อีเมล (แบบ CMC) → ป้าย Free/บรอนซ์ + แถบ EXP + Honey → ตั้งค่า 3 อย่าง → เมนูเดิมทั้งหมด

## หลักฐานตรวจรับ

- ESLint **0 errors** (26 warnings เดิม) · full test **151 files / 906 tests ผ่าน** · build **211 หน้า**
- **ความสูงเท่ากันหมด** (วัดด้วยสคริปต์): จอ ≥1024 แถวบน = 32px ทุกชิ้น · แถวล่าง = 36px ทุกชิ้น · จอ 768 = 44px ทุกชิ้น (คงขนาดนิ้วกด)
- ความสูงแถบรวมยัง **101px** ทุกขนาด → `--chrome-h` ไม่ต้องแก้
- ไม่มี horizontal overflow ที่ 768 / 1024 / 1440 / 1920 · ตัวเลขตลาดครบ 3 ตัวที่ 1440 และ 1920
- ทดสอบกดจริง: เปิดเมนูโปรไฟล์ · กดปุ่มธีม → เปลี่ยนเป็นสว่างทันที เมนูไม่ปิด → กดกลับเป็นมืดได้
- แท็บใหม่ console error 0 (error ระหว่างแก้เป็นของค้างจาก hot-reload)
- **ยังไม่ได้ทดสอบสถานะยังไม่ล็อกอินด้วยตา** — ปุ่มเฟืองยืนยันด้วย test ที่ตรวจว่าอยู่ใน guest branch จริง

## ⚠️ มีอีก session แก้ไฟล์พร้อมกันอยู่

ระหว่างที่ฉันทำ navbar มีการแก้ไฟล์ชุดอื่นเข้ามาโดยไม่ใช่ฉัน — งาน **copy หน้าแรก** (`home-search-hero`, `home-market-intro`, `lib/seo/copy/home.ts`, `page.tsx`) และ **ระบบโฆษณา** (`inventory.ts`, `google-ad-mockup`, `direct-sponsor-creative`)

- ฉันไม่ได้แตะไฟล์เหล่านั้นเลย และไม่ได้ย้อนทิ้ง
- **ตอนนี้ TypeScript ยังพัง 1 ไฟล์:** `src/components/home/home-seo.test.tsx` ส่ง props แบบเก่าให้ `HomeSearchHero`/`HomeMarketIntro` ที่เพิ่งเปลี่ยน signature — เป็นงานที่ session นั้นทำค้าง ไม่ใช่ของฉัน (vitest ผ่านเพราะไม่ type-check, build ผ่านเพราะไม่ build ไฟล์ test)
- ต้องให้ session นั้นปิดงานให้จบ หรือเบสสั่งให้ฉันแก้

## สถานะ git

- branch `chore/next-16.3` · commit ล่าสุด `539220d` push แล้ว
- งาน navbar รอบนี้ + งานของอีก session **ยังไม่ commit**

## NEXT

1. เบสรีเฟรช `http://localhost:3001` ดู navbar ใหม่ + กดปุ่ม ☰ มุมขวาบนดูเมนูโปรไฟล์ใหม่
2. บอกว่าจะให้ฉันแก้ `home-seo.test.tsx` ให้ไหม (จะได้ TypeScript เขียวทั้งโปรเจค) หรือปล่อยให้ session ที่ทำ copy หน้าแรกจัดการ
3. เคาะแล้ว commit + push
4. ยังไม่ได้เปิด PR ไป master
