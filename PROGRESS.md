# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนู (รอบสิบห้า · D3 ตามภาพที่เบสส่ง)

**⚠️ ดูที่ `http://localhost:3001/proto/navbar-ecom`**

### ⛔ ความผิดพลาดที่แก้แล้ว — เคยเผลอไปแก้ของจริง
รอบก่อนตีความคำว่า "ทำเลย" ว่าให้ลงเว็บจริง แล้วแก้ `header.tsx` · `header-market-ticker.tsx` · `globals.css` · `header-mobile.tsx` · `header-catalog-control.tsx` · `set-picker.tsx` + เทสต์ 2 ไฟล์ → **เบสท้วงว่าไม่ได้สั่ง ให้ทำ proto** → **ย้อนทุกไฟล์กลับสภาพเดิมแล้ว** (`git checkout ecf549d -- <ไฟล์>`) · ตรวจแล้ว test 918 ผ่าน · eslint 0 errors · `--chrome-h` กลับเป็น 8.25rem
**กติกาต่อจากนี้: แตะ production เมื่อเบสสั่งชัดว่า "ลงเว็บจริง/ลง production" เท่านั้น · "ทำเลย" = ทำใน proto**

### D3 — ผังตามภาพที่เบสส่ง
```
[48] เกม▾ › ชุด▾ ‖ สถิติ · สายพาน ‖ ค้นหา ‖ อัปเกรด
[56] โลโก้ · เมนู ⟷ พอร์ต · รายการโปรด · Honey · แชท · แจ้งเตือน · โปรไฟล์
```
แถวบน = "กำลังดูอะไร + หาอะไร" · แถวล่าง = ของฉันทั้งหมดต่อกันเป็นแถวเดียว

### ⚠️ สิ่งที่วัดได้ (วัดที่ความกว้างเดียวกันทั้งหมด)
| แบบ | ที่วิ่งสายพาน |
| --- | --- |
| **D3 (ตามภาพ ไม่หลบอะไร)** | **0px — หายสนิท** |
| D3 หลบของ (สถิติเหลือมูลค่ารวม · ชุดเหลือรหัส · ค้นหาแคบลง) | 475px |
| D2 (ก่อนสลับ) | 105px |
| ปัจจุบันบนเว็บ | 781px |

**แถวบนของ D3 มีของ 6 ก้อน** (เกม · ชุด · สถิติ 4 ตัว · สายพาน · ค้นหา · อัปเกรด) — สายพานเป็นตัวเดียวที่ยืดหดได้ เลยโดนบีบจนหมด · หน้า proto มีปุ่ม **"D3 บนจอ 1280"** ให้กดดูว่าถ้ายอมให้สถิติกับชื่อชุดหลบ สายพานจะกลับมาได้แค่ไหน

**ตรวจรับ:** eslint 0 · tsc ผ่าน · วัดจริงในเบราว์เซอร์ · ทุกปุ่ม ≥40px · ไม่มีแถวไหนล้น · สูง 104px ทุกแบบใหม่

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3001/proto/navbar-ecom`** — ดู D3 แล้วกด "D3 บนจอ 1280" เทียบ · ตัดสินว่ายอมให้สถิติ/ชื่อชุดหลบเพื่อแลกกับผังนี้ไหม
2. ถ้าเคาะแล้ว **รอเบสสั่งชัดว่าให้ลงเว็บจริง** ถึงจะแตะ `src/components/layout/*`
3. ของค้างเดิม: `priceValidUntil` ใน `json-ld.ts` · `getHomeData().rarityRows` query ทิ้งเปล่า
