# 📍 PROGRESS — สถานะสด
> **เขียนทับทุกครั้ง ไม่สะสม log** · อ่านอันนี้ก่อน แล้วทำต่อจาก NEXT

อัปเดตล่าสุด: 2026-08-29 — 🎨 **ทำ proto แถบเมนูแนวใหม่ (แรงบันดาลใจ Shopee/Lazada) รอเบสดูแล้วเคาะ** · ของจริงบนเว็บยังเป็นแบบ C ไม่ถูกแตะเลย

## 🆕 งานรอบนี้ — proto แถบเมนูแนวใหม่ (รอบสอง)

**รอบแรกเบสตัดสินว่า "ไม่โดน"** → สั่ง: เอาแถวคำค้นยอดนิยมออก · ที่พอได้คือแบบ L (พื้นเดิม) กับ H (คงแถบชีพจร) → **ทำใหม่ทับหน้าเดิม URL เดิม**

**ดูที่:** `http://localhost:3000/proto/navbar-ecom` · **ไฟล์เดียว ไม่แตะของจริงเลย:** `src/app/proto/navbar-ecom/page.tsx`

**ตัดออกจากรอบแรก:** แบบ S (ทาสีแบรนด์ทั้งแผง) · แถวคำค้นยอดนิยมทุกแบบ · โค้ด tone="band" ทั้งชุด

**สิ่งที่ทุกแบบใหม่เหมือนกัน:** พื้นสีเดิมของเว็บ (ไม่ทาสีทับ) · **แถบชีพจร + สายพานการ์ดขยับแรงอยู่ครบเหมือนเดิมทุกแบบ** · ช่องค้นหาโตขึ้น 2–3 เท่าและย้ายมากลางแถว

**3 ทิศทางใหม่ (คำถามหลักคือ "แถวล่างของแถบเมนูควรเป็นอะไร"):**

| แบบ | ไอเดีย | สูงจริงที่วัดได้ |
| --- | --- | --- |
| ปัจจุบัน | แบบ C ที่ขึ้นเว็บ (ตัวตั้งเทียบ) | 132px (32+44+56) |
| **1 · ค้นหาครองแถว** ← ค่าเริ่มต้น | ตัวเลือกเกม›ชุด **ย้ายเข้าไปเป็นหัวช่องค้นหา** (ท่า Lazada/Amazon) ช่องค้นหาเลยยืดเต็มแถว · แถวล่างเหลือเมนู+บัญชี | 132px (32+64+36) เท่าเดิมเป๊ะ |
| 2 · แถบชุดการ์ด | แถวล่างกลายเป็น **ชั้นวางชุดล่าสุด 8 ชุด** (OP15 ใหม่ · OP14 · OP13 · EB04 · ST26 · PRB02 · OP12 · EB03) + ปุ่ม "ทุกชุด" — หมวดหมู่แบบ Lazada แต่หมวดของเราคือชุด · เมนูหลักขึ้นไปแถวกลาง | 132px (32+64+36) |
| 3 · สองแถวจบ | แถบชีพจร + แถวเดียวจบ (โลโก้·ชุด·เมนู·ค้นหายืดเต็มกลาง·ไอคอน·บัญชี) | **96px** — คืนที่ให้เนื้อหา 36px |

**เหตุผลเบื้องหลังแบบ 2:** คนเล่นเช็คราคาโดย**เลือกชุดก่อนเสมอ** — ชุดคือหมวดหมู่จริงของเรา เลยยกขึ้นเป็นทางลัดกดเดียวถึงแทนที่จะซ่อนใน dropdown (ชุดจริงจาก `src/lib/constants/sets.ts`)

**ตาราง "ของเดิมแต่ละชิ้นไปอยู่ตรงไหน" ท้ายหน้า** ไล่ครบ 15 ชิ้นต่อแบบ

**ตรวจรับแล้ว:** `npx eslint` ไฟล์ใหม่ 0 errors · `npx tsc --noEmit` ผ่าน · เปิดจริงครบ 4 แบบ × 2 ธีม ไม่มี console error · สายพานวิ่งจริง · รูป R2 ขึ้นครบไม่มีรูปเสีย · ไม่มีป้ายไทยโดนตัดที่ 1360px · วัดความสูงจริงตรงกับที่เขียนในคำอธิบายทุกแบบ

**ข้อมูลตายตัวทั้งหมด** ไม่มี fetch/DB/สุ่ม ตามกติกา proto · ปุ่มทุกปุ่มกดไม่ได้จริง

## สถานะของจริงบนเว็บ (ไม่ถูกแตะรอบนี้)

master = `d296571` · แถบเมนูแบบ C 132px + แถบชีพจรมีสายพาน + popup ค้นหาโฉม CoinMarketCap + หน้าการ์ดบอกชุดบน navbar — ทั้งหมด live อยู่แล้ว

## ⚠️ ของค้างจากรอบก่อน (ยังไม่ได้ตัดสิน)

1. **`priceValidUntil` ใน `src/lib/seo/json-ld.ts`** — ตั้งไว้ `scrapedAt + 24 ชม.` ซึ่งตอนนี้หมดอายุไปแล้วเกือบทุกหน้าการ์ด (Google อาจมองว่าราคาไม่ valid) — รอเบสตัดสิน: เอาออก / ขยายเป็น 7 วัน / อย่างอื่น
2. **`npm run lint` เต็มยังแดง** — errors ทั้งหมดมาจาก `.claude/worktrees/.../.next/build/` (ขยะ build ของ worktree อีก session) ไม่ใช่โค้ดจริง · จะให้เงียบต้องเพิ่ม ignore ใน eslint config (เป็น config ต้องถามเบสก่อน) · **ใช้ `npx eslint src` เป็นตัวตัดสินแทน**
3. `getHomeData().rarityRows` query ทิ้งเปล่า · "อัปเดตล่าสุด:" hardcode ไทยที่ trending + most-expensive
4. branch เก่า `feat/navbar-direction-r2` ยังอยู่บน remote (ไม่ได้ลบ)

## NEXT

1. **เบสเปิด `http://localhost:3000/proto/navbar-ecom` แล้วเคาะ** ว่าเอาทิศทางไหน (หรือผสม เช่น เอาหัวช่องค้นหาของแบบ 1 + แถบชุดของแบบ 2)
2. ถ้ายังไม่โดนอีก — บอกได้ว่า "ไม่โดนตรงไหน" (ช่องค้นหา? แถวล่าง? ความสูง? ความรู้สึกรวม?) จะได้ยิงตรงจุดกว่าเดารอบสาม
3. เคาะแล้วค่อยลงของจริง: แก้ `src/components/layout/header.tsx` + `header-market-ticker.tsx` in-place · ถ้าเลือกแบบ 3 ต้องแก้ `--chrome-h` ใน `globals.css` จาก 8.25rem → 6rem ด้วย
4. รอบนี้ทำเฉพาะ desktop — มือถือยังไม่ได้ออกแบบใหม่
