import {
  Award,
  BarChart3,
  BookOpen,
  Circle,
  Eye,
  Layers,
  Link,
  Package,
  Search,
  Share2,
  ShoppingBag,
  TrendingUp,
  Wallet,
} from "lucide-react";

export const MONTHLY_ICON_MAP: Record<string, typeof Award> = {
  Share2,
  Layers,
  Search,
  TrendingUp,
};

export const ICON_MAP: Record<string, typeof Award> = {
  Search, TrendingUp, Package, Layers, ShoppingBag, BarChart3, Circle, Share2, Link,
  BookOpen, Wallet, Eye,
};
