/**
 * @deprecated Import from `@/lib/billing` instead. This file is kept as a
 * thin re-export so existing imports keep working while the codebase is
 * migrated. New code should reference `@/lib/billing` directly.
 */
export * from "./billing/limits";
